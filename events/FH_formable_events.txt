add_namespace = formable_event
add_namespace = formable_flavor_event

country_event = { #SCS requests to be given the land of the other puppets
    id = formable_event.1
    title = formable_event.1.t
    desc = formable_event.1.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.1"}

    is_triggered_only = yes

    option = { #Sure thing, totally trustworthy puppet!
        name = formable_event.1.a
        ai_chance = {
            factor = 1
            modifier = {
                factor = 5
                strength_ratio = {
                    tag = FROM
                    ratio > 0.66
                }
            }
        }
        FROM = {
            country_event = {
                id = formable_event.2
                days = 1
            }
        }
    }

    option = { #Are you mad?!
        name = formable_event.1.b
        ai_chance = {
            factor = 2
        }
        FROM = {
            country_event = {
                id = formable_event.3
                days = 1
            }
        }
    }
}

country_event = { #CHN is happy and agrees to give over the other puppets
    id = formable_event.2
    title = formable_event.2.t
    desc = formable_event.2.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.2"}

    is_triggered_only = yes

    option = { #Huzzah! The formables have been doubled!
        name = formable_event.2.a
        if = {
            limit = {
                NCA = {
                    exists = yes
                    is_subject_of = FROM
                    is_ai = yes
                }
            }
            annex_country = {
                target = NCA
                transfer_troops = yes
            }
            every_state = {
                limit = {
                    is_core_of = NCA
                }
                add_core_of = ROOT
            }
        }
        if = {
            limit = {
                CCA = {
                    exists = yes
                    is_subject_of = FROM
                    is_ai = yes
                }
            }
            annex_country = {
                target = CCA
                transfer_troops = yes
            }
            every_state = {
                limit = {
                    is_core_of = CCA
                }
                add_core_of = ROOT
            }
        }
    }
}

country_event = { #CHN be madde and says the big no to giving over puppets to SCS
    id = formable_event.3
    title = formable_event.3.t
    desc = formable_event.3.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.3"}

    is_triggered_only = yes

    option = { #Buck you and the horse you rode in on!
        name = formable_event.3.a
        if = {
            limit = {
                is_in_faction_with = FROM
            }
            leave_faction = yes
        }
        if = {
            limit = {
                is_subject_of = FROM
            }
            FROM = {
                end_puppet = ROOT
            }
        }
        if = {
            limit = {
                NCA = {
                    exists = yes
                    is_ai = yes
                }
            }
            create_wargoal = {
                target = NCA
                type = annex_everything
            }
            every_state = {
                limit = {
                    is_core_of = NCA
                }
                add_core_of = ROOT
            }
        }
        if = {
            limit = {
                CCA = {
                    exists = yes
                    is_ai = yes
                }
            }
            create_wargoal = {
                target = CCA
                type = annex_everything
            }
            every_state = {
                limit = {
                    is_core_of = CCA
                }
                add_core_of = ROOT
            }
        }
    }
}

country_event = { #CCA sends an offer to buy out the other puppets' lands to CHN
    id = formable_event.4
    title = formable_event.4.t
    desc = formable_event.4.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.4"}

    is_triggered_only = yes

    option = { #Stonks!
        name = formable_event.4.a
        ai_chance = {
            factor = 1
            modifier = {
                factor = 5
                strength_ratio = {
                    tag = FROM
                    ratio > 0.66
                }
            }
        }
        FROM = {
            country_event = {
                id = formable_event.5
                days = 1
            }
        }
    }

    option = { #We can exploit them better...
        name = formable_event.4.b
        ai_chance = {
            factor = 2
        }
        FROM = {
            country_event = {
                id = formable_event.6
                days = 1
            }
        }
    }
}

country_event = { #CHN agrees to be given gobs of cash for puppets
    id = formable_event.5
    title = formable_event.5.t
    desc = formable_event.5.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.5"}

    is_triggered_only = yes

    option = { #The art of the deal.
        name = formable_event.5.a
        if = {
            limit = {
                NCA = {
                    exists = yes
                    is_subject_of = FROM
                    is_ai = yes
                }
            }
            annex_country = {
                target = NCA
                transfer_troops = yes
            }
            every_state = {
                limit = {
                    is_core_of = NCA
                }
                add_core_of = ROOT
            }
        }
        if = {
            limit = {
                SCS = {
                    exists = yes
                    is_subject_of = FROM
                    is_ai = yes
                }
            }
            annex_country = {
                target = SCS
                transfer_troops = yes
            }
            every_state = {
                limit = {
                    is_core_of = SCS
                }
                add_core_of = ROOT
            }
        }
    }
}

country_event = { #CHN will never be bought out for filth lucre
    id = formable_event.6
    title = formable_event.6.t
    desc = formable_event.6.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.6"}

    is_triggered_only = yes

    option = { #Time for a free intervention!
        name = formable_event.6.a
        if = {
            limit = {
                is_in_faction_with = FROM
            }
            leave_faction = yes
        }
        if = {
            limit = {
                is_subject_of = FROM
            }
            FROM = {
                end_puppet = ROOT
            }
        }
        if = {
            limit = {
                NCA = {
                    exists = yes
                    is_ai = yes
                }
            }
            create_wargoal = {
                target = NCA
                type = annex_everything
            }
            every_state = {
                limit = {
                    is_core_of = NCA
                }
                add_core_of = ROOT
            }
        }
        if = {
            limit = {
                SCS = {
                    exists = yes
                    is_ai = yes
                }
            }
            create_wargoal = {
                target = SCS
                type = annex_everything
            }
            every_state = {
                limit = {
                    is_core_of = SCS
                }
                add_core_of = ROOT
            }
        }
    }
}

country_event = { #NCA politely asks if they can mess up some of the non-changeling nations
    id = formable_event.7
    title = formable_event.7.t
    desc = formable_event.7.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.7"}

    is_triggered_only = yes

    option = { #Who are we to prevent a bit of fun?
        name = formable_event.7.a
        FROM = {
            country_event = {
                id = formable_event.8
                days = 1
            }
        }
        set_country_flag = CHN_chrysalis_approves
    }

    option = { #No fun for you!
        name = formable_event.7.b
        ai_chance = {
            factor = 1
            modifier = {
                is_historical_focus_on = yes
                FROM = { is_ai = no }
                factor = 0
            }
            modifier = {
                FROM = { is_ai = yes }
                factor = 3
            }
        }
        FROM = {
            country_event = {
                id = formable_event.8
                days = 1
            }
        }
        set_country_flag = CHN_chrysalis_be_mean
    }
}

country_event = { #CHN is happy and agrees to give over the other puppets
    id = formable_event.8
    title = formable_event.8.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = CHN_chrysalis_approves
            }
        }
        text = formable_event.8.d_a
    }
    desc = {
        trigger = {
            FROM = {
                has_country_flag = CHN_chrysalis_be_mean
            }
        }
        text = formable_event.8.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.8"}

    is_triggered_only = yes

    option = { #Love for the love god!
        name = formable_event.8.a
        trigger = {
            FROM = {
                has_country_flag = CHN_chrysalis_approves
            }
        }
        set_country_flag = NCA_chryssy_agrees
    }
    option = { #Oh, okay.
        name = formable_event.8.b
        trigger = {
            FROM = {
                has_country_flag = CHN_chrysalis_be_mean
            }
        }
    }
}

country_event = { #NCA requests that they can break a few spines for CHN instead by controlling the other puppets
    id = formable_event.9
    title = formable_event.9.t
    desc = formable_event.9.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.9"}

    is_triggered_only = yes

    option = { #Gooooood
        name = formable_event.9.a
        ai_chance = {
            factor = 1
            modifier = {
                factor = 5
                strength_ratio = {
                    tag = FROM
                    ratio > 0.66
                }
            }
        }
        FROM = {
            country_event = {
                id = formable_event.10
                days = 1
            }
        }
    }

    option = { #We can exploit them better...
        name = formable_event.9.b
        ai_chance = {
            factor = 2
        }
        FROM = {
            country_event = {
                id = formable_event.11
                days = 1
            }
        }
    }
}

country_event = { #CHN is fine with losing the administrative headache for NCA to control instead
    id = formable_event.10
    title = formable_event.10.t
    desc = formable_event.10.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.10"}

    is_triggered_only = yes

    option = { #More land for the meat grinder.
        name = formable_event.10.a
        if = {
            limit = {
                CCA = {
                    exists = yes
                    is_subject_of = FROM
                    is_ai = yes
                }
            }
            annex_country = {
                target = CCA
                transfer_troops = yes
            }
            every_state = {
                limit = {
                    is_core_of = CCA
                }
                add_core_of = ROOT
            }
        }
        if = {
            limit = {
                SCS = {
                    exists = yes
                    is_subject_of = FROM
                    is_ai = yes
                }
            }
            annex_country = {
                target = SCS
                transfer_troops = yes
            }
            every_state = {
                limit = {
                    is_core_of = SCS
                }
                add_core_of = ROOT
            }
        }
    }
}

country_event = { #CHN loves bureaucracy or something
    id = formable_event.11
    title = formable_event.11.t
    desc = formable_event.11.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.11"}

    is_triggered_only = yes

    option = { #Then we shall do it ourselves.
        name = formable_event.11.a
        if = {
            limit = {
                is_in_faction_with = FROM
            }
            leave_faction = yes
        }
        if = {
            limit = {
                is_subject_of = FROM
            }
            FROM = {
                end_puppet = ROOT
            }
        }
        if = {
            limit = {
                CCA = {
                    exists = yes
                    is_ai = yes
                }
            }
            create_wargoal = {
                target = NCA
                type = annex_everything
            }
            every_state = {
                limit = {
                    is_core_of = NCA
                }
                add_core_of = ROOT
            }
        }
        if = {
            limit = {
                SCS = {
                    exists = yes
                    is_ai = yes
                }
            }
            create_wargoal = {
                target = SCS
                type = annex_everything
            }
            every_state = {
                limit = {
                    is_core_of = SCS
                }
                add_core_of = ROOT
            }
        }
    }
}

country_event = { #OLE suggests that CHN should return the old lands because they're nice, floofy deer
    id = formable_event.12
    title = formable_event.12.t
    desc = formable_event.12.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.12"}

    is_triggered_only = yes

    option = { #Eh, they ARE pretty floofy...
        name = formable_event.12.a
        FROM = {
            country_event = {
                id = formable_event.13
                days = 1
            }
        }
        set_country_flag = CHN_pet_floof
    }

    option = { #We shall not give into their floofy ways!
        name = formable_event.12.b
        ai_chance = {
            factor = 1
            modifier = {
                is_historical_focus_on = yes
                FROM = { is_ai = no }
                factor = 0
            }
            modifier = {
                FROM = { is_ai = yes }
                factor = 3
            }
        }
        FROM = {
            country_event = {
                id = formable_event.13
                days = 1
            }
        }
        set_country_flag = CHN_shave_the_deet
    }
}

country_event = { #CHN is rather decisive about their very decisiony decision
    id = formable_event.13
    title = formable_event.13.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = CHN_pet_floof
            }
        }
        text = formable_event.13.d_a
    }
    desc = {
        trigger = {
            FROM = {
                has_country_flag = CHN_shave_the_deet
            }
        }
        text = formable_event.13.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.13"}

    is_triggered_only = yes

    option = { #Oh? That's unexpected.
        name = formable_event.13.a
        trigger = {
            FROM = {
                has_country_flag = CHN_pet_floof
            }
        }
        every_state = {
            limit = {
                is_in_array = { global.original_OLE_states = THIS }
                is_owned_and_controlled_by = OVERLORD
            }
            transfer_state_to = ROOT
            add_core_of = ROOT
        }
    }
    option = { #Figures.
        name = formable_event.13.b
        set_country_flag = OLE_chyrssy_rejected
    }
}

country_event = { #Big Sister be gunning for MIT's independence
    id = formable_event.14
    title = formable_event.14.t
    desc = formable_event.14
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.14"}

    is_triggered_only = yes

    option = { #They completely outnumber us...
        name = formable_event.14.a
        ai_chance = {
            modifier = {
                factor = 8
                strength_ratio = {
                    tag = FROM
                    ratio < 0.75
                }
            }
        }
        set_country_flag = MIT_pulled_a_french
    }
    option = { #We shan't give in!
        name = formable_event.14.b
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.15
                days = 1
            }
        }
    }
}

country_event = { #MIT gives its response and ideally is smart
    id = formable_event.15
    title = formable_event.15.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = MIT_pulled_a_french
            }
        }
        text = formable_event.15.d_a
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { has_country_flag = MIT_pulled_a_french }
            }
        }
        text = formable_event.15.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.15"}

    is_triggered_only = yes

    option = { #Asterion was always a part of Marelandia
        name = formable_event.15.a
        if = {
            limit = {
                FROM = {
                    has_country_flag = MIT_pulled_a_french
                }
            }
            set_autonomy = {
                target = FROM
                autonomous_state = autonomy_puppet
            }
            set_cosmetic_tag = MIT_mareland_puppet
        }
        else = {
            remove_from_faction = MIT
            create_wargoal = {
                target = FROM
                type = puppet_wargoal_focus
            }
        }
    }
}

country_event = { #BRF asks [COUNTRY] to please join their most amazing league ever
    id = formable_event.16
    title = formable_event.16.t
    desc = formable_event.16.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.16"}

    is_triggered_only = yes

    option = { #Sure, why not?
        name = formable_event.16.a
        ai_chance = {
            factor = 3
        }
        set_country_flag = BRF_expand_faction
    }

    option = { #Nah, sounds cringe.
        name = formable_event.16.b
        ai_chance = {
            factor = 1
        }
        set_country_flag = BRF_sad_faction_noises
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.17
                days = 1
            }
        }
    }
}

country_event = { #BRF gets the response from the country they begged/requested
    id = formable_event.17
    title = formable_event.17.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = BRF_expand_faction
            }
        }
        text = formable_event.17.d_a
    }
    desc = {
        trigger = {
            FROM = {
                has_country_flag = BRF_sad_faction_noises
            }
        }
        text = formable_event.17.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event."}

    is_triggered_only = yes

    option = { #We shall stand united!
        name = formable_event.17.a
        trigger = {
            FROM = {
                has_country_flag = BRF_expand_faction
            }
        }
        add_to_faction = FROM
    }

    option = { #Welp.
        name = formable_event.17.b
        trigger = {
            FROM = {
                has_country_flag = BRF_sad_faction_noises
            }
        }
    }
}

country_event = { #MCW goes on the warpath for land they never considered core territory before now; how colonialist
    id = formable_event.18
    title = formable_event.18.t
    desc = formable_event.18.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.18"}

    is_triggered_only = yes

    option = { #They aren't that important.
        name = formable_event.18.a
        ai_chance = {
            modifier = {
                factor = 3
                strength_ratio = {
                    tag = FROM
                    ratio < 0.7
                }
            }
        }
        set_country_flag = MCW_agreed_to_transfer
    }

    option = { #They can take it from our cold, dead hooves/claws!
        name = formable_event.18.b
        ai_chance = {
            modifier = {
                factor = 0
                FROM = {
                    is_ai = yes
                }
            }
        }
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.19
                days = 1
                random_days = 3
            }
        }
    }
}

country_event = { #MCW gets the response back from any owners of their "true" core land
    id = formable_event.19
    title = formable_event.19.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = MCW_agreed_to_transfer
            }
        }
        text = formable_event.19.d_a
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { has_country_flag = MCW_agreed_to_transfer }
            }
        }
        text = formable_event.19.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.19"}

    is_triggered_only = yes

    option = { #Stonks!
        name = formable_event.19.a
        trigger = {
            FROM = {
                has_country_flag = MCW_agreed_to_transfer
            }
        }
        every_state = {
            limit = {
                OR = {
                    state = 1168
                    state = 1169
                    state = 1176
                    state = 1185
                    state = 1163
                    state = 1181
                    state = 1097
                }
                is_owned_and_controlled_by = FROM
            }
            transfer_state_to = ROOT
        }
    }

    option = { #Negative aura, man.
        name = formable_event.19.b
        trigger = {
            FROM = {
                NOT = { has_country_flag = MCW_agreed_to_transfer }
            }
        }
        create_wargoal = {
            target = FROM
            type = take_core_state
        }
    }
}

country_event = { #ZAR politely asks the nation to integrate with them, promising BAT to come in after them if they don't.
    id = formable_event.20
    title = formable_event.20.t
    desc = formable_event.20.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.20"}

    is_triggered_only = yes

    option = { #We don't have much choice, do we?
        name = formable_event.20
        ai_chance = {
            factor = 3
        }
        set_country_flag = ZAR_accepted_integration
    }

    option = { #Let the desert swallow them!
        name = formable_event.20
        ai_chance = {
            modifier = {
                factor = 2
                strength_ratio = {
                    tag = FROM
                    ratio > 0.5
                }
            }
        }
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.21
                days = 1
                random_days = 2
            }
        }
    }
}

country_event = { #ZAR gets the response from the two AZI nations for integration, and will war if denied
    id = formable_event.21
    title = formable_event.21.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = ZAR_accepted_integration
            }
        }
        text = formable_event.21.d_a
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { has_country_flag = ZAR_accepted_integration }
            }
        }
        text = formable_event.21.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.21"}

    is_triggered_only = yes

    option = { #Not like they stood a chance.
        name = formable_event.21.a
        trigger = {
            FROM = {
                has_country_flag = ZAR_accepted_integration
            }
        }
        annex_country = {
            target = FROM
            transfer_troops = yes
        }
    }

    option = { #Did they catch sun stroke?!
        name = formable_event.21.b
        trigger = {
            FROM = { NOT = { has_country_flag = ZAR_accepted_integration } }
        }
        declare_war_on = {
            target = FROM
            type = annex_everything
        }
    }

    after = {
        every_state = {
            limit = {
                is_core_of = FROM
            }
            add_core_of = ROOT
        }
    }
}

country_event = { #BAT offers to LMB to be their new overlord and fight against CTL if LMB wants their land back at all
    id = formable_event.22
    title = formable_event.22.t
    desc = formable_event.22.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.22"}

    is_triggered_only = yes

    option = { #It's a tempting offer we can't refuse.
        name = formable_event.22.a
        ai_chance = {
            modifier = {
                factor = 4
                CTL = {
                    strength_ratio = {
                        tag = FROM
                        ratio < 0.75
                    }
                }
            }
        }
        set_country_flag = BAT_backstabbing
        effect_tooltip = {
            leave_faction = yes
            FROM = {
                set_autonomy = {
                    target = ROOT
                    autonomous_state = autonomy_satellite
                }
            }
            every_state = {
                limit = {
                    is_core_of = ROOT
                    OWNER = {
                        OR = {
                            tag = FROM
                            is_in_faction_with = FROM
                        }
                    }
                }
                transfer_state_to = ROOT
            }
        }
    }

    option = { #It's a tempting offer that we can refuse.
        name = formable_event.22.b
        ai_chance = {
            modifier = {
                factor = 3
                CTL = {
                    strength_ratio = {
                        tag = FROM
                        ratio > 0.75
                    }
                }
            }
        }
    }

    after = {
        FROM = {
            country_event = formable_event.23
        }
    }
}

country_event = { #BAT receives the response from LMB regarding their offer of fealty for cores
    id = formable_event.23
    title = formable_event.23.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = BAT_backstabbing
            }
        }
        text = formable_event.23.d_a
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { has_country_flag = BAT_backstabbing}
            }
        }
        text = formable_event.23.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.23"}

    is_triggered_only = yes

    option = { #The veil expands...
        name = formable_event.23.a
        trigger = {
            FROM = {
                has_country_flag = BAT_backstabbing
            }
        }
        FROM = {
            leave_faction = yes
        }
        set_autonomy = {
            target = FROM
            autonomous_state = autonomy_satellite
        }
        every_state = {
            limit = {
                is_core_of = FROM
                OWNER = {
                    OR = {
                        tag = ROOT
                        is_in_faction_with = ROOT
                    }
                }
            }
            transfer_state_to = FROM
        }
        declare_war_on = {
            target = CTL
            type = annex_everything
        }
    }

    option = { #The veil will smother them in time...
        name = formable_event.23.b
        trigger = {
            FROM = {
                NOT = { has_country_flag = BAT_backstabbing }
            }
        }
    }
}

country_event = { #YAN receives a notice from KIR that if they submit they will gain all the land of IDO and DEL
    id = formable_event.24
    title = formable_event.24.t
    desc = formable_event.24.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.24"}

    is_triggered_only = yes

    option = { #A generous offer! We accept.
        name = formable_event.24.a
        set_country_flag = KIR_YAN_accepted
    }

    option = { #We value our independence too much for free land.
        name = formable_event.24.b
        ai_chance = {
            factor = 0.5
        }
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.25
                days = 1
            }
        }
    }
}

country_event = { #KIR receives the response from YAN
    id = formable_event.25
    title = formable_event.25.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = KIR_YAN_accepted
            }
        }
        text = formable_event.25.d_a
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { has_country_flag = KIR_YAN_accepted }
            }
        }
        text = formable_event.25.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.25"}

    is_triggered_only = yes

    option = { #It's free real estate. For both of us!
        name = formable_event.25.a
        trigger = {
            FROM = {
                has_country_flag = KIR_YAN_accepted
            }
        }
        set_autonomy = {
            target = FROM
            autonomous_state = autonomy_puppet
        }
        FROM = { set_cosmetic_tag = KIR_puppet }
        every_state = {
            limit = {
                OR = {
                    is_in_array = { IDO.original_cores = THIS }
                    is_in_array = { DEL.original_cores = THIS }
                }
                OWNER = {
                    OR = {
                        tag = ROOT
                        is_subject_of = ROOT
                    }
                }
            }
            transfer_state_to = FROM
            add_core_of = FROM
        }
    }

    option = { #Guess we can give it to somecreature else.
        name = formable_event.25.b
        trigger = {
            FROM = {
                NOT = { has_country_flag = KIR_YAN_accepted }
            }
        }
        every_state = {
            limit = {
                OR = {
                    is_in_array = { IDO.original_cores = THIS }
                    is_in_array = { DEL.original_cores = THIS }
                }
                OWNER = {
                    OR = {
                        tag = ROOT
                        is_subject_of = ROOT
                    }
                }
            }
            transfer_state_to = ZLU
        }
    }
}

country_event = { #CNT is offered the chance to become a puppet in exchange for security under KIR
    id = formable_event.26
    title = formable_event.26.t
    desc = formable_event.26.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.26"}

    is_triggered_only = yes

    option = { #They are rather fuzzy and soft...
        name = formable_event.26.a
        ai_chance = {
            modifier = {
                factor = 3
                strength_ratio = {
                    tag = FROM
                    ratio < 0.7
                }
            }
        }
        set_country_flag = KIR_CNT_accepted
    }

    option = { #Their fuzziness and softness can't sway us!
        name = formable_event.27.b
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.27
                days = 1
            }
        }
    }
}

country_event = { #KIR receives the response from CNT
    id = formable_event.27
    title = formable_event.27.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = KIR_CNT_accepted
            }
        }
        text = formable_event.27.d_a
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { has_country_flag = KIR_CNT_accepted }
            }
        }
        text = formable_event.27.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.27"}

    is_triggered_only = yes

    option = { #How wise
        name = formable_event.27.a
        trigger = {
            FROM = {
                has_country_flag = KIR_YAN_accepted
            }
        }
        set_autonomy = {
            target = FROM
            autonomous_state = autonomy_puppet
        }
        FROM = { set_cosmetic_tag = KIR_puppet }
    }

    option = { #They didn't really have much of an option
        name = formable_event.27.b
        trigger = {
            FROM = {
                NOT = { has_country_flag = KIR_YAN_accepted }
            }
        }
        create_wargoal = {
            type = puppet_wargoal_focus
            target = FROM
        }
    }
}

country_event = { #KIR and NVA both get a demand for NVA to integrate into ZLU; both must agree to have it taken into ZLU
    id = formable_event.28
    title = formable_event.28.t
    desc = formable_event.28.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.28"}

    is_triggered_only = yes

    option = { #It is agreeable.
        name = formable_event.28.a
        trigger = {
            tag = KIR
        }
        ai_chance = {
            modifier = {
                factor = 3
                FROM = {
                    strength_ratio = {
                        tag = ROOT
                        ratio > 0.5
                    }
                }
            }
        }
        set_country_flag = ZLU_KIR_agreed
    }

    option = { #So long as they respect us.
        name = formable_event.28.b
        trigger = {
            tag = NVA
        }
        ai_chance = {
            modifier = {
                factor = 2
                strength_ratio = {
                    tag = FROM
                    ratio < 0.8
                }
            }
            modifier = {
                factor = 5
                FROM = { is_ai = yes }
            }
        }
        set_country_flag = ZLU_NVA_agreed
    }

    option = { #They ask too much.
        name = formable_event.28.c
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.29
                days = 1
                random_days = 3
            }
        }
    }
}

country_event = { #ZLU gets the response from NVA and KIR
    id = formable_event.29
    title = formable_event.29.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = ZLU_KIR_agreed
            }
        }
        text = formable_event.29.d_a
    }
    desc = {
        trigger = {
            FROM = {
                has_country_flag = ZLU_NVA_agreed
            }
        }
        text = formable_event.29.d_b
    }
    desc = {
        trigger = {
            FROM = {
                tag = KIR
                NOT = { has_country_flag = ZLU_KIR_agreed }
            }
        }
        text = formable_event.29.d_c
    }
    desc = {
        trigger = {
            FROM = {
                tag = NVA
                NOT = { has_country_flag = ZLU_NVA_agreed }
            }
        }
        text = formable_event.29.d_d
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.29"}

    is_triggered_only = yes

    option = { #One down...
        name = formable_event.29.a
        trigger = {
            FROM = {
                OR = {
                    has_country_flag = ZLU_NVA_agreed
                    has_country_flag = ZLU_KIR_agreed
                }
            }
        }
        add_to_variable = { ZLU_event_tracker = 1 }
    }

    option = { #Well...
        name = formable_event.29.b
        trigger = {
            FROM = {
                OR = {
                    AND = {
                        tag = NVA
                        NOT = { has_country_flag = ZLU_NVA_agreed }
                    }
                    AND = {
                        tag = KIR
                        NOT = { has_country_flag = ZLU_KIR_agreed }
                    }
                }
            }
        }
    }

    after = {
        if = {
            limit = {
                check_variable = { ZLU_event_tracker = 2 }
            }
            country_event = {
                id = formable_event.30
                days = 1
            }
        }
    }
}

country_event = { #Final result of ZLU begging for NVA's territory
    id = formable_event.30
    title = formable_event.30.t
    desc = {
        trigger = {
            KIR = { has_country_flag = ZLU_KIR_agreed }
            NVA = { has_country_flag = ZLU_NVA_agreed }
        }
        text = formable_event.30.d_a
    }
    desc = {
        trigger = {
            OR = {
                KIR = { NOT = { has_country_flag = ZLU_KIR_agreed } }
                NVA = { NOT = { has_country_flag = ZLU_NVA_agreed } }
            }
        }
        text = formable_event.30.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.30"}

    is_triggered_only = yes

    option = { #All according to plan...
        name = formable_event.30.a
        trigger = {
            KIR = { has_country_flag = ZLU_KIR_agreed }
            NVA = { has_country_flag = ZLU_NVA_agreed }
        }
        annex_country = {
            target = NVA
            transfer_troops = yes
        }
        every_state = {
            limit = {
                is_core_of = NVA
            }
            add_core_of = ROOT
        }
    }

    option = { #Well darn...
        name = formable_event.30.b
        trigger = {
            OR = {
                KIR = { NOT = { has_country_flag = ZLU_KIR_agreed } }
                NVA = { NOT = { has_country_flag = ZLU_NVA_agreed } }
            }
        }
    }
}

country_event = { #IDO asks CNT if they wish to be best buds by joining a faction
    id = formable_event.31
    title = formable_event.31.t
    desc = formable_event.31.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.32"}

    is_triggered_only = yes

    option = { #More friends is always good.
        name = formable_event.32.a
        ai_chance = {
            modifier = {
                factor = 2
                FROM = {
                    strength_ratio = {
                        tag = ROOT
                        ratio > 0.75
                    }
                }
            }
        }
        set_country_flag = IDO_CNT_agreed
    }

    option = { #Notaur is home. Please leave a message.
        name = formable_event.32.b
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.32
                days = 1
            }
        }
    }
}

country_event = { #IDO gets CNT's response regarding joining a faction
    id = formable_event.32
    title = formable_event.32.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = IDO_CNT_agreed
            }
        }
        text = formable_event.32.d_a
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { has_country_flag = IDO_CNT_agreed }
            }
        }
        text = formable_event.32.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.32"}

    is_triggered_only = yes

    option = { #Centaurs are friends. Not enemies.
        name = formable_event.32.a
        trigger = {
            FROM = {
                has_country_flag = IDO_CNT_agreed
            }
        }
        add_to_faction = FROM
    }

    option = { #Centaurs could be friends...
        name = formable_event.32.b
        trigger = {
            FROM = {
                NOT = { has_country_flag = IDO_CNT_agreed }
            }
        }
    }
}

country_event = { #YAN debates which continental power to align with to eventually retake their homeland in FEA
    id = formable_event.33
    title = formable_event.33.t
    desc = formable_event.33.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.33"}

    is_triggered_only = yes

    option = { #The Chiropterrans have maintained stability all these years. They could help.
        name = formable_event.33.a
        ai_chance = {
            factor = 4
        }
        trigger = {
            BAT = {
                exists = yes
                is_faction_leader = yes
            }
        }
        BAT = {
            country_event = {
                id = formable_event.34
                days = 3
            }
        }
    }

    option = { #The Colthaginians keep a sizeable military. They could fight the Empire with us.
        name = formable_event.33.b
        ai_chance = {
            factor = 3
            modifier = {
                factor = 2
                BAT = {
                    OR = {
                        exists = no
                        is_subject_of = CTH
                    }
                }
            }
        }
        trigger = {
            CTH = {
                exists = yes
                is_faction_leader = yes
            }
        }
        CTH = {
            country_event = {
                id = formable_event.34
                days = 3
            }
        }
    }

    option = { #Hindia is a superpower. They can easily aid us.
        name = formable_event.33.b
        ai_chance = {
            factor = 3
        }
        trigger = {
            CTL = {
                exists = yes
                is_faction_leader = yes
            }
        }
        CTL = {
            country_event = {
                id = formable_event.34
                days = 3
            }
        }
    }

    option = { #The Abyssinians desire vengeance against the Wingbardians. They will help us.
        name = formable_event.33.e
        trigger = {
            ABY = {
                exists = yes
                is_subject = no
                is_in_faction = no
            }
        }
        ABY = {
            country_event = {
                id = formable_event.34
                days = 3
            }
        }
    }
}

country_event = { #The chosen recipient of YAN's request gets an ask to let them join or form a faction
    id = formable_event.34
    title = formable_event.34.t
    desc = {
        trigger = {
            NOT = {
                tag = ABY
            }
        }
        text = formable_event.34.d_a
    }
    desc = {
        trigger = {
            tag = ABY
        }
        text = formable_event.34.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.34"}

    is_triggered_only = yes

    option = { #Sure, they can join.
        name = formable_event.34.a
        ai_chance = {
            factor = 2.5
        }
        trigger = {
            NOT = {
                tag = ABY
            }
        }
        set_country_flag = YAN_new_buddies
    }

    option = { #We could use any friends we can get.
        name = formable_event.34.b
        ai_chance = {
            factor = 4
        }
        trigger = {
            tag = ABY
        }
        set_country_flag = YAN_new_buddies
    }

    option = { #What have they done at all in the last forty years? Ignore them.
        name = formable_event.34.c
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.35
                days = 1
            }
        }
    }
}

country_event = { #YAN receives the response from one of the faction leaders or ABY about their request
    id = formable_event.35
    title = formable_event.35.t
    desc = {
        trigger = {
            FROM = {
                NOT = { tag = ABY }
                has_country_flag = YAN_new_buddies
            }
        }
        text = formable_event.35.d_a
    }
    desc = {
        trigger = {
            FROM = {
                tag = ABY
                has_country_flag = YAN_new_buddies
            }
        }
        text = formable_event.35.d_b
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { tag = ABY }
                NOT = { has_country_flag = YAN_new_buddies }
            }
        }
        text = formable_event.35.d_c
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { tag = ABY }
                NOT = { has_country_flag = YAN_new_buddies }
            }
        }
        text = formable_event.35.d_d
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.35"}

    is_triggered_only = yes

    option = { #We'll be great friends!
        name = formable_event.35.
        trigger = {
            FROM = {
                NOT = { tag = ABY }
                has_country_flag = YAN_new_buddies
            }
        }
        FROM = {
            add_to_faction = ROOT
        }
    }

    option = { #New best friends!
        name = formable_event.35.b
        trigger = {
            FROM = {
                tag = ABY
                has_country_flag = YAN_new_buddies
            }
        }
        create_faction_from_template = faction_template_industrial_focus
        hidden_effect = {
            set_faction_name = YAN_griffon_reconquest_faction_name
        }
        add_to_faction = FROM
    }

    option = { #Well, we'll still defeat them by ourselves!
        name = formable_event.35.c
        trigger = {
            FROM = {
                NOT = { has_country_flag = YAN_new_buddies }
            }
        }
        add_war_support = 0.15
        add_political_power = 100
        add_manpower = 50000
    }
}

country_event = { #The owner or owners of HAU's states receive a demand from YAN to cede the islands or face war, which they can accept or refuse
    id = formable_event.36
    title = formable_event.36.t
    desc = formable_event.36.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.36"}

    is_triggered_only = yes

    option = { #It's not worth the hassle.
        name = formable_event.36.a
        ai_chance = {
            modifier = {
                factor = 5
                FROM = {
                    strength_ratio = {
                        tag = ROOT
                        ratio > 0.6
                    }
                }
            }
        }
        set_country_flag = YAN_HAU_ceded
    }

    option = { #What are they going to do? Cross an ocean?
        name = formable_event.36.b
        ai_chance = {
            factor = 2.5
        }
    }

    after = {
        country_event = {
            id = formable_event.37
            days = 1
            random_days = 2
        }
    }
}

country_event = { #YAN receives the response from the owners of HAU's states
    id = formable_event.37
    title = formable_event.37.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = YAN_HAU_ceded
            }
        }
        text = formable_event.37.d_a
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { has_country_flag = YAN_HAU_ceded }
            }
        }
        text = formable_event.37.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.37"}

    is_triggered_only = yes

    option = { #Wait, really?
        name = formable_event.37.a
        trigger = {
            FROM = {
                has_country_flag = YAN_HAU_ceded
            }
        }
        658 = {
            transfer_state_to = ROOT
        }
        659 = {
            transfer_state_to = ROOT
        }
        660 = {
            transfer_state_to = ROOT
        }
    }

    option = { #Not surprised, to be honest.
        name = formable_event.37.b
        trigger = {
            FROM = {
                NOT = { has_country_flag = YAN_HAU_ceded }
            }
        }
        658 = {
            OWNER = {
                ROOT = {
                    create_wargoal = {
                        target = PREV
                        type = take_state_focus
                        generator = 658
                    }
                }
            }
        }
        659 = {
            OWNER = {
                ROOT = {
                    create_wargoal = {
                        target = PREV
                        type = take_state_focus
                        generator = 659
                    }
                }
            }
        }
        660 = {
            OWNER = {
                ROOT = {
                    create_wargoal = {
                        target = PREV
                        type = take_state_focus
                        generator = 660
                    }
                }
            }
        }
    }
}

country_event = { #ROA demands ZLU and will war for it if not given
    id = formable_event.38
    title = formable_event.38.t
    desc = {
        trigger = {
            tag = KIR
        }
        text = formable_event.38.d_a
    }
    desc = {
        trigger = {
            tag = ZLU
        }
        text = formable_event.38.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.38"}

    is_triggered_only = yes

    option = { #They can have it.
        name = formable_event.38.a
        trigger = {
            tag = KIR
        }
        ai_chance = {
            modifier = {
                factor = 3
                FROM = {
                    strength_ratio = {
                        tag = ROOT
                        ratio > 0.75
                    }
                }
            }
        }
        set_country_flag = ROA_KIR_agreed
    }

    option = { #How can we resist them?
        name = formable_event.38.b
        trigger = {
            tag = ZLU
        }
        ai_chance = {
            modifier = {
                factor = 2
                FROM = {
                    strength_ratio = {
                        tag = ROOT
                        ratio > 0.5
                    }
                }
            }
        }
        set_country_flag = ROA_KIR_agreed
    }

    option = { #Never!
        name = formable_event.38.c
        ai_chance = {
            factor = 2
        }
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.39
                days = 1
            }
        }
    }
}

country_event = { #ROA receives the response from the chosen country
    id = formable_event.39
    title = formable_event.39.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = ROA_KIR_agreed
            }
        }
        text = formable_event.39.d_a
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { has_country_flag = ROA_KIR_agreed }
            }
        }
        text = formable_event.39.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.39"}

    is_triggered_only = yes

    option = { #A wise choice.
        name = formable_event.39.a
        trigger = {
            FROM = {
                has_country_flag = ROA_KIR_agreed
            }
        }
        annex_country = {
            target = ZLU
        }
    }

    option = { #They dare test our legions?!
        name = formable_event.39.b
        trigger = {
            FROM = {
                NOT = { has_country_flag = ROA_KIR_agreed }
            }
        }
        create_wargoal = {
            target = ZLU
            type = annex_everything
        }
    }
}

country_event = { #ZWA/TIM asks for MTA/ROA to join their cause or face consequences
    id = formable_event.40
    title = formable_event.40.t
    desc = formable_event.40.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.40.d"}

    is_triggered_only = yes

    option = { #Might as well.
        name = formable_event.40.a
        ai_chance = {
            modifier = {
                factor = 3
                FROM = {
                    strength_ratio = {
                        tag = ROOT
                        ratio > 0.75
                    }
                }
            }
        }
        set_country_flag = ZWA_accepted_alliance
    }

    option = { #Nopony's home, leave us alone.
        name = formable_event.40.b
        ai_chance = {
            factor = 0.5
        }
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.41
                days = 1
            }
        }
    }
}

country_event = { #ZWA/TIM receives a response from MTA/ROA regarding whether or not they want to be allies
    id = formable_event.41
    title = formable_event.41.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = ZWA_accepted_alliance
            }
        }
        text = formable_event.41.d_a
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { has_country_flag = ZWA_accepted_alliance }
            }
        }
        text = formable_event.41.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.41"}

    is_triggered_only = yes

    option = { #We shall stand stronger together!
        name = formable_event.41.a
        trigger = {
            FROM = {
                has_country_flag = ZWA_accepted_alliance
            }
        }
        add_to_faction = FROM
    }

    option = { #It wasn't just a suggestion...
        name = formable_event.41.b
        trigger = {
            FROM = {
                NOT = { has_country_flag = ZWA_accepted_alliance }
            }
        }
        create_wargoal = {
            target = FROM
            type = puppet_wargoal_focus
        }
    }
}

country_event = { #ZWA/TIM asks for MTA/ROA to integrate or be destroyed
    id = formable_event.42
    title = formable_event.42.t
    desc = formable_event.42.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.42.d"}

    is_triggered_only = yes

    option = { #We let them grow too powerful for their own good.
        name = formable_event.42.a
        ai_chance = {
            modifier = {
                factor = 2.5
                FROM = {
                    strength_ratio = {
                        tag = ROOT
                        ratio > 0.75
                    }
                }
            }
        }
        set_country_flag = ZWA_accepted_integration
    }

    option = { #We will fight for our survival!
        name = formable_event.42.b
        leave_faction = yes
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.43
                days = 1
            }
        }
    }
}

country_event = { #ZWA/TIM receives a response from MTA/ROA regarding whether or not accept being integrated into the country
    id = formable_event.43
    title = formable_event.43.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = ZWA_accepted_integration
            }
        }
        text = formable_event.43.d_a
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { has_country_flag = ZWA_accepted_integration }
            }
        }
        text = formable_event.43.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.43"}

    is_triggered_only = yes

    option = { #One people! One freedom!
        name = formable_event.43.a
        trigger = {
            FROM = {
                has_country_flag = ZWA_accepted_integration
            }
        }
        annex_country = {
            target = FROM
            transfer_troops = yes
        }
    }

    option = { #Freedom isn't a choice.
        name = formable_event.43.b
        trigger = {
            FROM = {
                NOT = { has_country_flag = ZWA_accepted_alliance }
            }
        }
        create_wargoal = {
            target = FROM
            type = annex_everything
        }
    }

    after = {
       every_state = {
            limit = {
                is_in_array = { FROM.original_cores = THIS }
            }
            add_core_of = ROOT
        }
    }
}

country_event = { #ZWA/TIM/MTA receives an invite from MGY to form a new faction against ABY and they can choose to decline or accept
    id = formable_event.44
    title = formable_event.44.t
    desc = formable_event.44.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.44"}

    is_triggered_only = yes

    option = { #We must work together against tyrants!
        name = formable_event.44.a
        ai_chance = {
            modifier = {
                factor = 2.5
                OR = {
                    tag = TIM
                    tag = ZWA
                }
            }
        }
        set_country_flag = MGY_accepted_alliance
    }

    option = { #They are too bloodthirsty for their own good.
        name = formable_event.44.b
    }

    after = {
        if = {
            limit = {
                OR = {
                    tag = ZWA
                    tag = TIM
                }
            }
            FROM = {
                country_event = {
                    id = formable_event.45
                    days = 1
                }
            }
        }
        else = {
            FROM = {
                country_event = {
                    id = formable_event.45
                    days = 3
                }
            }
        }
    }
}

country_event = { #MGY receives the response from ZWA/TIM/MTA; if ZWA/TIM agrees, MGY joins their faction, and MTA will after that; if they don't, but MTA agrees, then MGY will form a new faction with MTA
    id = formable_event.45
    title = formable_event.45.t
    desc = {
        trigger = {
            FROM = {
                OR = {
                    tag = ZWA
                    tag = TIM
                }
                has_country_flag = MGY_accepted_alliance
            }
        }
        text = formable_event.45.d_a
    }
    desc = {
        trigger = {
            FROM = {
                OR = {
                    tag = ZWA
                    tag = TIM
                }
                NOT = { has_country_flag = MGY_accepted_alliance }
            }
        }
        text = formable_event.45.d_b
    }
    desc = {
        trigger = {
            FROM = {
                tag = MTA
                has_country_flag = MGY_accepted_alliance
            }
        }
        text = formable_event.45.d_c
    }
    desc = {
        trigger = {
            FROM = {
                tag = MTA
                NOT = { has_country_flag = MGY_accepted_alliance }
            }
        }
        text = formable_event.45.d_d
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.45"}

    is_triggered_only = yes

    option = { #Stronger together!
        name = formable_event.45.a
        trigger = {
            FROM = {
                OR = {
                    tag = ZWA
                    tag = TIM
                }
                has_country_flag = MGY_accepted_alliance
            }
        }
        FROM = { add_to_faction = ROOT }
    }

    option = { #I guess they didn't want to be friends...
        name = formable_event.45.b
        trigger = {
            FROM = {
                NOT = { has_country_flag = MGY_accepted_alliance }
            }
        }
    }

    option = { #We could use their help.
        name = formable_event.45.c
        trigger = {
            FROM = {
                tag = MTA
                has_country_flag = MGY_accepted_alliance
            }
        }
        if = {
            limit = {
                OR = {
                    is_in_faction_with = ZWA
                    is_in_faction_with = TIM
                }
            }
            FACTION_LEADER = {
                add_to_faction = FROM
            }
        }
        else = {
            if = {
                limit = {
                    is_in_faction = no
                }
                create_faction_from_template = faction_template_generic_dominance
                set_faction_name = MGY_anti_cat_faction
            }
            add_to_faction = FROM
        }
    }
}

country_event = { #MGY is invited by MTA to a faction since they were so kind to return all the territory to them
    id = formable_event.46
    title = formable_event.46.t
    desc = formable_event.46.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.46"}

    is_triggered_only = yes

    option = { #How kind of them. We accept!
        name = formable_event.46.a
        ai_chance = {
            factor = 3
        }
        set_country_flag = MTA_MGY_accepted
    }

    option = { #How kind of them. Yoink!
        name = formable_event.46.b
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.47
                days = 1
            }
        }
    }
}

country_event = { #MTA receives the response from MGY
    id = formable_event.47
    title = formable_event.47.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = MTA_MGY_accepted
            }
        }
        text = formable_event.47.d_a
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { has_country_flag = MTA_MGY_accepted }
            }
        }
        text = formable_event.47.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.47"}

    is_triggered_only = yes

    option = { #We shall free horse kind, together!
        name = formable_event.47.a
        trigger = {
            FROM = {
                has_country_flag = MTA_MGY_accepted
            }
        }
        if = {
            limit = {
                is_in_faction = no
            }
            create_faction_from_template = faction_template_generic_dominance
            set_faction_name = MTA_free_EMR_faction
        }
        add_to_faction = FROM
    }
}

country_event = { #WIN receives a request from WNG to be integrated so they can get a foothold in Hippogriffia, with the promise of supporting their citizens and treating them autonomously otherwise
    id = formable_event.48
    title = formable_event.48.t
    desc = formable_event.48.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.48"}

    is_triggered_only = yes

    option = { #It is agreeable.
        name = formable_event.48.a
        ai_chance = {
            modifier = {
                factor = 6
                FROM = {
                    strength_ratio = {
                        tag = ROOT
                        ratio > 0.8
                    }
                }
            }
        }
        set_country_flag = WNG_WIN_accepted
    }

    option = { #They're out of their minds.
        name = formable_event.48.b
        ai_chance = {
            factor = 2
        }
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.49
                days = 1
            }
        }
    }
}

country_event = { #WNG receives WIN's response to the request to be annexed
    id = formable_event.49
    title = formable_event.49.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = WNG_WIN_accepted
            }
        }
        text = formable_event.49.d_a
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.49"}

    is_triggered_only = yes

    option = { #Il Duce's plans never fail!
        name = formable_event.49.a
        trigger = {
            FROM = {
                has_country_flag = WNG_WIN_accepted
            }
        }
        annex_country = {
            target = FROM
            transfer_troops = yes
        }
    }

    option = { #Il Duce's plans NEVER fail!
        name = formable_event.49.b
        trigger = {
            FROM = {
                NOT = { has_country_flag = WNG_WIN_accepted }
            }
        }
        hidden_effect = {
            BAT = {
                if = {
                    limit = {
                        has_guaranteed = FROM
                    }
                    diplomatic_relation = {
                        country = FROM
                        relation = guarantee
                        active = no
                    }
                }
            }
        }
        declare_war_on = {
            target = FROM
            type = annex_everything
        }
    }

    after = {
        1204 = {
            add_core_of = ROOT
        }
    }
}

country_event = { #WNG asks politely to work with their old best pals, Colthage
    id = formable_event.50
    title = formable_event.50.t
    desc = formable_event.50.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.50"}

    is_triggered_only = yes

    option = { #They did help as best they could in fighting HIP...
        name = formable_event.50.a
        ai_chance = {
            modifier = {
                factor = 3
                FROM = {
                    strength_ratio = {
                        tag = ROOT
                        ratio > 0.5
                    }
                }
            }
        }
        set_country_flag = WNG_CTH_accepted
    }

    option = { #They fell to Marelandia, what can they hope to do?
        name = formable_event.50.b
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.51
                days = 1
            }
        }
    }
}

country_event = { #WNG to gets CTH's response
    id = formable_event.51
    title = formable_event.51.t
    desc = {
        trigger = {
            FROM = {
                has_country_flag = WNG_CTH_accepted
            }
        }
        text = formable_event.51.d_a
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { has_country_flag = WNG_CTH_accepted }
            }
        }
        text = formable_event.51.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.51"}

    is_triggered_only = yes

    option = { #Il Duce's plans never fail!
        name = formable_event.51.a
        trigger = {
            FROM = {
                has_country_flag = WNG_CTH_accepted
            }
        }
        FROM = {
            add_to_faction = ROOT
        }
    }

    option = { #Il Duce's plans never FAIL!
        name = formable_event.51.b
        trigger = {
            FROM = {
                NOT = { has_country_flag = WNG_CTH_accepted }
            }
        }
        add_political_power = 100
        add_war_support = 0.1
    }
}

country_event = { #NVA requests rather forcefully that KIR give them the old land that they once all had and KIR can accept or not
    id = formable_event.52
    title = formable_event.52.t
    desc = formable_event.52.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.52"}

    is_triggered_only = yes

    option = { #They can have it.
        name = formable_event.52.a
        ai_chance = {
            modifier = {
                factor = 3
                FROM = {
                    strength_ratio = {
                        tag = ROOT
                        ratio > 0.5
                    }
                }
            }
        }
        set_country_flag = NVA_KIR_agrees
    }

    option = { #Why would we give up our land?
        name = formable_event.52.b
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.53
                days = 1
            }
        }
    }
}

country_event = { #NVA receives the response from KIR about whether or not they have been given the land as asked
    id = formable_event.53
    title = formable_event.53.t
    desc = {
        trigger = {
            FROM = { has_country_flag = NVA_KIR_agrees }
        }
        text = formable_event.53.d_a
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { has_country_flag = NVA_KIR_agrees }
            }
        }
        text = formable_event.53.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.53"}

    is_triggered_only = yes

    option = { #Excellent!
        name = formable_event.53.a
        trigger = {
            FROM = { has_country_flag = NVA_KIR_agrees }
        }
        1057 = {
            transfer_state_to = ROOT
            add_core_of = ROOT
        }
        1086 = {
            transfer_state_to = ROOT
            add_core_of = ROOT
        }
        1058 = {
            transfer_state_to = ROOT
            add_core_of = ROOT
        }
    }

    option = { #They dare insult us?!
        name = formable_event.53.b
    }

    after = {
        hidden_effect = {
            country_event = {
                id = formable_event.54
                days = 7
                random_days = 14
            }
        }
    }
}

country_event = { #With either the integration or the rejection of the lost territories, NVA can choose to go further, or exact revenge
    id = formable_event.54
    title = formable_event.54.t
    desc = {
        trigger = {
            KIR = { has_country_flag = NVA_KIR_agrees }
        }
        text = formable_event.54.d_a #Do we want to go further?
    }
    desc = {
        trigger = {
            KIR = {
                NOT = { has_country_flag = NVA_KIR_agrees }
            }
        }
        text = formable_event.54.d_b #We must punish KIR!
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.54"}

    is_triggered_only = yes

    option = { #It was all part of the plan...
        name = formable_event.54.a
        trigger = {
            KIR = {
                has_country_flag = NVA_KIR_agrees
            }
        }
        set_country_flag = NVA_nirking_time
    }

    option = { #They must suffer for their insolence!
        name = formable_event.54.b
        trigger = {
            KIR = {
                NOT = {
                    has_country_flag = NVA_KIR_agrees
                }
            }
        }
        set_country_flag = NVA_nirking_time
    }

    option = { #We must stand strong...
        name = formable_event.54.c
    }
}

country_event = { #CRY demands that with their army matters sorted that STG/CWR surrender before a war leads to more death
    id = formable_event.55
    title = formable_event.55.t
    desc = formable_event.55.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.55"}

    is_triggered_only = yes

    option = { #They've mobilized far more than we thought...
        name = formable_event.55.a
        ai_chance = {
            modifier = {
                FROM = {
                    strength_ratio = {
                        tag = ROOT
                        ratio > 0.85
                    }
                }
                factor = 6
            }
        }
        set_country_flag = CRY_CWR_agreed
    }

    option = { #They're nothing but a paper tiger!
        name = formable_event.55.b
        ai_chance = {
            factor = 2
        }
    }

    after = {
        FROM = {
            country_event = {
                id = formable_event.56
                days = 1
            }
        }
    }
}

country_event = { #CRY gets the response from STG/CWR regarding their polite request of taking back the land they're owed
    id = formable_event.56
    title = formable_event.56.t
    desc = {
        trigger = {
            FROM = { has_country_flag = CRY_CWR_agreed }
        }
        text = formable_event.56.d_a
    }
    desc = {
        trigger = {
            FROM = {
                NOT = { has_country_flag = CRY_CWR_agreed }
            }
        }
        text = formable_event.56.d_b
    }
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.56"}

    is_triggered_only = yes

    option = { #The best we could hope for.
        name = formable_event.56.a
        trigger = {
            FROM = { has_country_flag = CRY_CWR_agreed }
        }
        annex_country = {
            target = CWR
        }
    }

    option = { #So they have chosen war...
        name = formable_event.56.b
        trigger = {
            FROM = {
                NOT = { has_country_flag = CRY_CWR_agreed }
            }
        }
        create_wargoal = {
            target = CWR
            type = annex_everything
        }
    }
}

country_event = { #CHN is informed about CRY thumbing their nose as the treaty and has the option to go after them
    id = formable_event.57
    title = formable_event.57.t
    desc = formable_event.57.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event.57"}

    is_triggered_only = yes

    option = { #So [CRY.GetLeader] has grown a spine, huh?
        name = formable_event.57.a
        ai_chance = {
            factor = 10
            modifier = {
                has_idea = CHN_idea_memory_44
                factor = 0 #Don't go into a war when you can't train units
            }
            modifier = {
                has_idea = CHN_idea_CHN_economy
                factor = 0.3 #Less likely if economy is weak
            }
            modifier = {
                FROM = {
                    strength_ratio = {
                        tag = ROOT
                        ratio > 0.7
                    }
                }
                factor = 0.5 #Less likely if high losses are possible
            }
        }
    }

    option = { #They are no longer our concern.
        name = formable_event.57.b
        ai_chance = {
            factor = 3
        }
    }
}

#Template
#country_event = {
#    id = formable_event.
#    title = formable_event.
#    desc = formable_event.
#    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_event."}
#
#    is_triggered_only = yes
#
#    option = {
#        name = formable_event.
#    }
#}

#Flavor
country_event = { #AWR culminating event
    id = formable_flavor_event.1
    title = formable_flavor_event.1.t
    desc = formable_flavor_event.1.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_flavor_event.1"}

    is_triggered_only = yes

    option = { #Freedom!
        name = formable_flavor_event.1.a
    }
}

country_event = { #EQC culminating event
    id = formable_flavor_event.2
    title = formable_flavor_event.2.t
    desc = formable_flavor_event.2.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_flavor_event.2"}

    is_triggered_only = yes

    option = { #It's with the Proles.
        name = formable_flavor_event.2.a
    }
}

country_event = { #BAK flavor event
    id = formable_flavor_event.3
    title = formable_flavor_event.3.t
    desc = formable_flavor_event.3.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_flavor_event.3"}

    is_triggered_only = yes

    option = { #Liberty, light the way.
        name = formable_flavor_event.3.a
    }
}

country_event = { #STM culminating event
    id = formable_flavor_event.4
    title = formable_flavor_event.4.t
    desc = formable_flavor_event.4.d
    immediate = {log = "[GetDateText]: [ROOT.GetName] event formable_flavor_event.4"}

    is_triggered_only = yes

    option = { #It's just good business.
        name = formable_flavor_event.4.a
    }
}
