scripted_gui = {
    STB_DEM_election_ui = {
        context_type = decision_category
        window_name = "STB_DEM_election_window"

        ai_enabled = { always = no }

        visible = { always = yes }

        effects = {
            STB_DEM_election_button_click = {
                set_country_flag = STB_DEM_gui_open
            }
        }

        properties = {
            STB_DEM_election_button = {
                image = GFX_placeholder_poster
            }
        }
    }
}
