peace_ai_desires = {
    EQC_already_owns_the_world = {
        peace_action_type = take_state
        enable = {
            ROOT = { tag = EQC }
            FROM.FROM = {
                NOT = { tag = EQC }
            }
        }

        ai_desire = 1000
    }

    EQC_hates_puppets = {
        peace_action_type = { puppet liberate force_government }
        enable = {
            ROOT = { tag = EQC }
        }

        ai_desire = -1000
    }
}
