FH_demo_south_east_skeleton_category = {
    EEE_NEI_powder_keg = {
        available = {
            date > 1049.6.1
        }
        visible = {
            OR = {
                tag = EEE
                tag = NEI
            }
            NOT = {
                OR = {
                    NEI = { has_country_flag = FH_demo_chose_war }
                    EEE = { has_country_flag = FH_demo_chose_war }
                }
            }
        }
        fire_only_once = yes
        days_remove = 14
        war_with_on_complete = NEI
        war_with_on_complete = EEE
        complete_effect = {
            set_country_flag = FH_demo_chose_war
        }
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove EEE_NEI_powder_keg"
            if = {
                limit = {
                    tag = NEI
                }
                declare_war_on = {
                    target = EEE
                    type = annex_everything
                }
            }
            if = {
                limit = {
                    tag = EEE
                }
                declare_war_on = {
                    target = NEI
                    type = annex_everything
                }
            }
        }
        ai_will_do = {
            factor = 10
            modifier = {
                date < 1049.8.1
                factor = 0
            }
        }
    }
    EEE_reform_militia = {
        available = {
            NEI = { all_cores_owned_by_ROOT_or_subject = yes }
        }
        visible = {
            tag = EEE
        }
        cost = 50 #Placeholder
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision EEE_reform_militia"
            remove_ideas = EEE_tribal_militias
        }
    }
    EEE_national_reform = {
        available = {
            NEI = { all_cores_owned_by_ROOT_or_subject = yes }
        }
        visible = {
            tag = EEE
        }
        cost = 75
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision EEE_national_reform"
            remove_ideas = EEE_tribal_politics
        }
    }
    EEE_industrial_boons = {
        available = {
            NEI = { all_cores_owned_by_ROOT_or_subject = yes }
        }
        visible = {
            tag = EEE
        }
        cost = 100
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision EEE_industrial_boons"
            remove_ideas = EEE_tribal_holdings
        }
    }
    EEE_eliminating_the_cults = {
        available = {
            NEI = { all_cores_owned_by_ROOT_or_subject = yes }
            has_manpower > 70000
            has_stability > 0.6
        }
        visible = {
            tag = EEE
        }
        cost = 75
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision EEE_reform_militia"
            add_manpower = -28395
            add_stability = -0.15
            remove_ideas = EEE_night_whispering
        }
    }
    BAL_civilize_tribals = {
        available = {
            date > 1049.11.1
        }
        visible = {
            tag = BAL
            OR = {
                country_exists = EEE
                country_exists = NEI
            }
        }
        fire_only_once = yes
        days_remove = 14
        war_with_on_complete = EEE
        war_with_on_complete = NEI
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove BAL_civilize_tribals"
            declare_war_on = {
                target = EEE
                type = annex_everything
            }
            declare_war_on = {
                target = NEI
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
            modifier = {
                date < 1050.3.1
                factor = 0
            }
        }
    }
    BAL_reclaim_new_horseleans = {
        available = {
            date > 1050.6.1
        }
        visible = {
            tag = BAL
            country_exists = MTS
        }
        days_remove = 7
        war_with_on_complete = MTS
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove BAL_reclaim_new_horseleans"
            declare_war_on = {
                target = MTS
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
            modifier = {
                has_war = yes
                factor = 0
            }
        }
    }
    BAL_finish_restructuring = {
        available = {
            date > 1050.9.1
        }
        visible = {
            tag = BAL
            OR = {
                country_exists = STB
                country_exists = NID
                country_exists = NRP
            }
        }
        days_remove = 14
        war_with_on_complete = STB
        war_with_on_complete = NID
        war_with_on_complete = NRP
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove BAL_finish_restructuring"
            declare_war_on = {
                tag = STB
                type = annex_everything
            }
            declare_war_on = {
                tag = NID
                type = annex_everything
            }
            declare_war_on = {
                tag = NRP
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
            modifier = {
                has_war = yes
                factor = 0
            }
        }
    }
    BAl_party_purge = {
        available = {
            date > 1049.3.1
        }
        visible = {
            tag = BAL
        }
        cost = 50
        days_remove = 60
        modifier = {
            political_power_gain = -0.35
            stability_weekly_factor = -0.02
        }
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove BAl_party_purge"
            remove_ideas = BAL_party_conflict
        }
    }
    BAL_restructure_quotas = {
        available = {
            date > 1050.1.1
            num_of_civilian_factories > 15
        }
        visible = {
            tag = BAL
        }
        cost = 100
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision BAL_restructure_quotas"
            remove_ideas = BAL_collapsed_economy
        }
    }
    BAL_forced_recognition = {
        available = {
            NOT = {
                any_other_country = {
                    has_country_flag = SEE
                    exists = yes
                }
            }
        }
        visible = {
            tag = BAL
        }
        cost = 150
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision BAL_forced_recognition"
            remove_ideas = BAL_no_recognition
        }
    }
    SPF_expand_hoards = {
        available = {
            date > 1049.8.1
        }
        visible = {
            tag = SPF
        }
        fire_only_once = yes
        days_remove = 14
        war_with_on_complete = CLI
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove SPF_expand_hoards"
            declare_war_on = {
                target = CLI
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    CLI_liberate_the_islands = {
        available = {
            date > 1049.2.1
        }
        visible = {
            tag = CLI
        }
        fire_only_once = yes
        days_remove = 14
        war_with_on_complete = SPF
        war_with_on_complete = NID
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove CLI_liberate_the_islands"
            declare_war_on = {
                target = SPF
                type = annex_everything
            }
            declare_war_on = {
                target = NID
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    STB_smack_insults = {
        available = {
            date > 1050.1.1
        }
        visible = {
            tag = STB
        }
        fire_only_once = yes
        days_remove = 14
        war_with_on_complete = NRP
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove STB_smack_insults"
            declare_war_on = {
                target = NRP
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
            modifier = {
                date < 1050.4.1
                factor = 0
            }
        }
    }
    STB_take_backsies = {
        available = {
            date > 1050.4.1
        }
        visible = {
            tag = STB
            country_exists = NID
        }
        fire_only_once = yes
        days_remove = 14
        war_with_on_complete = NID
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove STB_take_backsies"
            declare_war_on = {
                target = NID
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
            modifier = {
                date < 1050.7.1
                factor = 0
            }
        }
    }
    STB_securing_glory = {
        available = {
            date > 1050.10.1
        }
        visible = {
            OR = {
                tag = STB
                tag = NRP
            }
            OR = {
                country_exists = EEE
                country_exists = NEI
            }
        }
        fire_only_once = yes
        days_remove = 14
        war_with_on_complete = EEE
        war_with_on_complete = NEI
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove STB_securing_glory"
            declare_war_on = {
                target = EEE
                type = annex_everything
            }
            declare_war_on = {
                target = NEI
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
            modifier = {
                date < 1051.1.1
                factor = 0
            }
        }
    }
    STB_claiming_the_crown = {
        available = {
            date > 1051.3.1
        }
        visible = {
            OR = {
                tag = STB
                tag = NRP
            }
            OR = {
                country_exists = MTS
                country_exists = BAL
            }
        }
        fire_only_once = yes
        days_remove = 14
        war_with_on_complete = BAL
        war_with_on_complete = MTS
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove STB_claiming_the_crown"
            declare_war_on = {
                target = MTS
                type = annex_everything
            }
            declare_war_on = {
                target = BAL
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
            modifier = {
                date < 1051.5.1
                factor = 0
            }
        }
    }
    STB_refocus_efforts = {
        available = {
            date > 1049.2.1
        }
        visible = {
            tag = STB
        }
        cost = 75
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision STB_refocus_efforts"
            remove_ideas = STB_industrial_economy
        }
    }
    STB_underwhelm_results = {
        available = {
            date > 1049.5.1
            controls_state = 33
        }
        visible = {
            tag = STB
        }
        cost = 100
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision STB_underwhelm_results"
            remove_ideas = STB_overwhelmed_militia
        }
    }
    NRP_install_pesticide = {
        available = {
            date = 1049.9.1
        }
        visible = {
            tag = NRP
        }
        fire_only_once = yes
        days_remove = 14
        war_with_on_complete = NID
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove NRP_install_pesticide"
            declare_war_on = {
                target = NID
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
            modifier = {
                date < 1050.1.1
                factor = 0
            }
        }
    }
    NRP_dictatorship_toppling = {
        available = {
            date = 1050.1.1
        }
        visible = {
            tag = NRP
        }
        fire_only_once = yes
        days_remove = 14
        war_with_on_complete = NRP
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove NRP_dictatorship_toppling"
            declare_war_on = {
                target = STB
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
            modifier = {
                has_war = yes
                factor = 0
            }
        }
    }
    NID_solidify_assets = {
        available = {
            date > 1049.11.1
        }
        visible = {
            tag = NID
        }
        fire_only_once = yes
        days_remove = 14
        war_with_on_complete = SPF
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove NID_solidify_assets"
            declare_war_on = {
                target = SPF
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    NID_pay_down_squabbles = {
        available = {
            date > 1049.6.1
        }
        visible = {
            tag = NID
        }
        cost = 125
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision NID_pay_down_squabbles"
            remove_ideas = NID_merc_prt_tensions
        }
    }
    NID_shirk_restrictions = {
        available = {
            date > 1049.6.1
        }
        visible = {
            tag = NID
        }
        cost = 75
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision NID_shirk_restrictions"
            remove_ideas = NID_no_aviation_industry_idea
        }
    }
    MTS_wipe_out_thestral_resistance = {
        available = {
            date > 1049.1.1
        }
        visible = {
            tag = MTS
        }
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision MTS_wipe_out_thestral_resistance"
            save_current_manpower = yes
            84 = {
                set_demilitarized_zone = no
                add_manpower = -23790
            }
            restore_previous_manpower = yes
            add_stability = -0.075
            add_war_support = -0.05
        }
        ai_will_do = {
            factor = 10
        }
    }
    MTS_micro_compensation = {
        available = {
            date > 1049.6.1
            num_of_civilian_factories > 7
        }
        visible = {
            tag = MTS
        }
        fire_only_once = yes
        cost = 75
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision MTS_micro_compensation"
            remove_ideas = MTS_miniscule_industry
        }
    }
    MTS_reject_rejection = {
        available = {
            date > 1049.6.1
            has_idea = extensive_conscription
            num_divisions > 12
        }
        visible = {
            tag = MTS
        }
        cost = 100
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision MTS_reject_rejection"
            remove_ideas = MTS_corrupted_conscripts
        }
    }
    MTS_acquire_sunscreen = {
        available = {
            date > 1049.6.1
            has_idea = extensive_conscription
            has_war_support > 0.45
        }
        visible = {
            tag = MTS
        }
        cost = 75 #XP later
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision MTS_acquire_sunscreen"
            remove_ideas = MTS_ill_adapted_heat_tolerance
        }
    }
    MTS_forgiven_ordo = {
        available = {
            NOT = {
                any_other_country = {
                    has_country_flag = SEE
                    exists = yes
                }
            }
        }
        visible = {
            tag = MTS
        }
        cost = 150
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision MTS_forgiven_ordo"
            remove_ideas = MTS_unsanctioned_ordo
            set_cosmetic_tag = MTS_SOL_puppet
        }
    }
    MTS_end_the_reminder = {
        available = {
            date > 1051.1.1
        }
        visible = {
            tag = MTS
        }
        fire_only_once = yes
        days_remove = 21
        war_with_on_complete = STB
        war_with_on_complete = NEI
        war_with_on_complete = NID
        war_with_on_complete = BAL
        war_with_on_complete = CLI
        war_with_on_complete = SPF
        war_with_on_complete = EEE
        war_with_on_complete = NRP
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove MTS_end_the_reminder"
            every_country = {
                limit = {
                    has_country_flag = SEE
                    exists = yes
                }
                ROOT = {
                    declare_war_on = {
                        target = PREV
                        type = annex_everything
                    }
                }
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    south_east_recore_jungle = {
        available = {
            NEI = { all_cores_owned_by_ROOT_or_subject = yes }
            EEE = { all_cores_owned_by_ROOT_or_subject = yes }
        }
        visible = {
            NEI = { all_cores_owned_by_ROOT_or_subject = yes }
            EEE = { all_cores_owned_by_ROOT_or_subject = yes }
            has_global_flag = balsplosion
        }
        fire_only_once = yes
        cost = 75
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision south_east_recore_jungle"
            every_state = {
                limit = {
                    OR = {
                        is_core_of = EEE
                        is_core_of = NEI
                    }
                }
                add_core_of = ROOT
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    south_east_recore_southeastern_end = {
        available = {
            STB = { all_cores_owned_by_ROOT_or_subject = yes }
            NRP = { all_cores_owned_by_ROOT_or_subject = yes }
        }
        visible = {
            STB = { all_cores_owned_by_ROOT_or_subject = yes }
            NRP = { all_cores_owned_by_ROOT_or_subject = yes }
            has_global_flag = balsplosion
        }
        fire_only_once = yes
        cost = 75
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision south_east_recore_southeastern_end"
            every_state = {
                limit = {
                    OR = {
                        is_core_of = STB
                        is_core_of = NRP
                    }
                }
                add_core_of = ROOT
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    south_east_recore_northwest_end = {
        available = {
            MTS = { all_cores_owned_by_ROOT_or_subject = yes }
            BAL = { all_cores_owned_by_ROOT_or_subject = yes }
        }
        visible = {
            MTS = { all_cores_owned_by_ROOT_or_subject = yes }
            BAL = { all_cores_owned_by_ROOT_or_subject = yes }
        }
        fire_only_once = yes
        cost = 75
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision south_east_recore_northwest_end"
            every_state = {
                limit = {
                    OR = {
                        is_core_of = MTS
                        is_core_of = BAL
                    }
                }
                add_core_of = ROOT
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    south_east_recore_islands = {
        available = {
            NID = { all_cores_owned_by_ROOT_or_subject = yes }
            CLI = { all_cores_owned_by_ROOT_or_subject = yes }
            SPF = { all_cores_owned_by_ROOT_or_subject = yes }
        }
        visible = {
            NID = { all_cores_owned_by_ROOT_or_subject = yes }
            CLI = { all_cores_owned_by_ROOT_or_subject = yes }
            SPF = { all_cores_owned_by_ROOT_or_subject = yes }
            has_global_flag = balsplosion
        }
        fire_only_once = yes
        cost = 75
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision south_east_recore_islands"
            every_state = {
                limit = {
                    OR = {
                        is_core_of = NID
                        is_core_of = CLI
                        is_core_of = SPF
                    }
                }
                add_core_of = ROOT
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    south_east_overlooked_isles = {
        available = {
            custom_trigger_tooltip = {
                tooltip = BAL_owns_all_peninsula_tt
                MTS = { all_cores_owned_by_ROOT_or_subject = yes }
                BAL = { all_cores_owned_by_ROOT_or_subject = yes }
                EEE = { all_cores_owned_by_ROOT_or_subject = yes }
                NEI = { all_cores_owned_by_ROOT_or_subject = yes }
                NRP = { all_cores_owned_by_ROOT_or_subject = yes }
                STB = { all_cores_owned_by_ROOT_or_subject = yes }
                221 = {
                    OR = {
                        CONTROLLER = {
                            tag = ROOT
                            is_subject_of = ROOT
                        }
                    }
                }
            }
        }
        visible = {
            MTS = { all_cores_owned_by_ROOT_or_subject = yes }
            BAL = { all_cores_owned_by_ROOT_or_subject = yes }
            EEE = { all_cores_owned_by_ROOT_or_subject = yes }
            NEI = { all_cores_owned_by_ROOT_or_subject = yes }
            NRP = { all_cores_owned_by_ROOT_or_subject = yes }
            STB = { all_cores_owned_by_ROOT_or_subject = yes }
            221 = {
                OR = {
                    CONTROLLER = {
                        tag = ROOT
                        is_subject_of = ROOT
                    }
                }
            }
            OR = {
                country_exists = CLI
                country_exists = SPF
            }
            NOT = {
                tag = CLI
                tag = SPF
                date > 1052.1.1
            }
        }
        fire_only_once = yes
        days_remove = 7
        war_with_on_complete = CLI
        war_with_on_complete = SPF
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove south_east_overlooked_isles"
            declare_war_on = {
                target = CLI
                type = annex_everything
            }
            declare_war_on = {
                target = SPF
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    south_east_get_on_with_it = {
        visible = {
            date > 1052.1.1
        }
        fire_only_once = yes
        days_remove = 7
        war_with_on_complete = BAL
        war_with_on_complete = MTS
        war_with_on_complete = STB
        war_with_on_complete = NID
        war_with_on_complete = NRP
        war_with_on_complete = NEI
        war_with_on_complete = EEE
        war_with_on_complete = CLI
        war_with_on_complete = SPF
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove south_east_get_on_with_it"
            every_country = {
                limit = {
                    has_country_flag = SEE
                    exists = yes
                }
                ROOT = {
                    declare_war_on = {
                        target = PREV
                        type = annex_everything
                    }
                }
            }
        }
        ai_will_do = {
            factor = 100
            modifier = {
                has_war = yes
                factor = 0
            }
        }
    }
}

FH_demo_WGE_skeleton_category = {
    MWR_flip_STG_bird = {
        available = {
            has_war = no
            date > 1050.7.1
        }
        visible = {
            date > 1050.7.1
            tag = MWR
        }
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision MWR_flip_STG_bird"
            custom_effect_tooltip = MWR_smell_STG_later_tt
            hidden_effect = {
                leave_faction = yes
                OVERLORD = {
                    end_puppet = yes
                }
                set_politics = {
                    ruling_party = fascism
                    elections_allowed = no
                }
                add_popularity = {
                    ideology = fascism
                    popularity = 0.33
                }
                random_list = {
                    35 = {
                        STG = {
                            create_wargoal = {
                                target = ROOT
                                type = puppet_wargoal_focus
                            }
                        }
                    }
                    65 = { }
                }
                remove_from_array = { global.shield_of_steel = ROOT.id }
            }
        }
    }
    NVC_capture_the_throne = {
        available = {
            has_war = no
            is_subject = no
            date > 1051.5.1
        }
        visible = {
            date > 1051.5.1
            tag = NVC
        }
        fire_only_once = yes
        cost = 25
        days_remove = 35
        war_with_on_complete = GRF
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove NVC_capture_the_throne"
            create_wargoal = {
                target = GRF
                type = annex_everything
            }
        }
        ai_will_do = { factor = 10 }
    }
    VRG_backstab_NVC = {
        available = {
            has_war = no
            is_subject = no
            date > 1051.6.1
        }
        visible = {
            date > 1051.6.1
            tag = VRG
        }
        fire_only_once = yes
        cost = 50
        days_remove = 14
        war_with_on_complete = NVC
        war_with_on_complete = GRF
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove VRG_backstab_NVC"
            create_wargoal = {
                target = NVC
                type = annex_everything
            }
            create_wargoal = {
                target = GRF
                type = annex_everything
            }
        }
        ai_will_do = { factor = 10 }
    }
    VRG_NVC_recapture_southern_GRF = {
        available = {
            owns_state = 1217
            is_subject = no
            date > 1052.8.10
        }
        visible = {
            date > 1052.8.10
            OR = {
                AND = {
                    tag = NVC
                    NOT = { country_exists = VRG }
                }
                AND = {
                    tag = VRG
                    NOT = { country_exists = NVC }
                }
            }
            country_exists = MWR #For now
        }
        fire_only_once = yes
        cost = 50
        days_remove = 35
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove VRG_NVC_recapture_southern_GRF"
            create_wargoal = {
                type = annex_everything
                target = MWR
            }
        }
        ai_will_do = { factor = 10 }
    }
    VRG_NVC_punch_the_bear = {
        available = {
            owns_state = 1217
            is_subject = no
            date > 1054.2.14
        }
        visible = {
            date > 1054.2.14
            OR = {
                AND = {
                    tag = NVC
                    NOT = { country_exists = VRG }
                }
                AND = {
                    tag = VRG
                    NOT = { country_exists = NVC }
                }
            }
            OR = {
                country_exists = SCH
                country_exists = JGR
            }
        }
        cost = 50
        fire_only_once = yes
        days_remove = 35
        remove_effect  = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove VRG_NVC_punch_the_bear"
            if = {
                limit = {
                    country_exists = SCH
                }
                create_wargoal = {
                    target = SCH
                    type = annex_everything
                }
            }
            if = {
                limit = {
                    country_exists = JGR
                }
                create_wargoal = {
                    target = JGR
                    type = annex_everything
                }
            }
        }
        ai_will_do = { factor = 10 }
    }
    VRG_NVC_yikes_the_yaks = {
        available = {
            owns_state = 1217
            is_subject = no
            date > 1056.1.1
        }
        visible = {
            date > 1056.1.1
            OR = {
                AND = {
                    tag = NVC
                    NOT = { country_exists = VRG }
                }
                AND = {
                    tag = VRG
                    NOT = { country_exists = NVC }
                }
            }
            OR = {
                country_exists = SMH
                country_exists = YAH
            }
        }
        cost = 50
        fire_only_once = yes
        days_remove = 35
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove VRG_NVC_yikes_the_yaks"
            if = {
                limit = {
                    country_exists = SMH
                }
                create_wargoal = {
                    target = SMH
                    type = annex_everything
                }
            }
            if = {
                limit = {
                    country_exists = YAH
                }
                create_wargoal = {
                    target = YAH
                    type = annex_everything
                }
            }
        }
        ai_will_do = { factor = 10 }
    }
    VRG_NVC_clap_the_changelings = {
        available = {
            custom_trigger_tooltip = {
                tooltip = NVC_VRG_slurped_up_everything_tt
                owns_state = 1217
                owns_state = 243
                owns_state = 338
                owns_state = 281
                owns_state = 325
                owns_state = 62
                owns_state = 274
                owns_state = 11
            }
            is_subject = no
            date > 1057.6.1
        }
        visible = {
            date > 1057.6.1
            OR = {
                AND = {
                    tag = NVC
                    NOT = { country_exists = VRG }
                }
                AND = {
                    tag = VRG
                    NOT = { country_exists = NVC }
                }
            }
        }
        cost = 100
        fire_only_once = yes
        days_remove = 70
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove VRG_NVC_clap_the_changelings"
            set_temp_variable = { GRF_core_state_owner = 1221.owner }
            create_wargoal = {
                target = var:GRF_core_state_owner = 1221.owner
                type = take_claimed_state
            }
        }
    }
}

FH_demo_STG_region_skeleton_category = {
    AWR_peace_out_STG = {
        available = {
            is_subject_of = STG
            date > 1050.6.20
        }
        visible = {
            is_subject_of = STG
            tag = AWR
            date > 1050.6.20
        }
        cost = 50
        fire_only_once = yes
        days_remove = 70
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove AWR_peace_out_STG"
            country_event = equestria_activity.27
        }
    }
    STG_end_AWRs_crap = {
        visible = {
            tag = STG
            has_country_flag = STG_spurned_AWR_puppet
        }
        cost = 100
        fire_only_once = yes
        days_remove = 35
        war_with_on_complete = AWR
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove STG_end_AWRs_crap"
            create_wargoal = {
                target = AWR
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
            modifier = {
                date < 1052.1.1
                factor = 0
            }
        }
    }
    STG_integrate_chaos = {
        visible = {
            tag = STG
            AWR = { all_cores_owned_by_ROOT = yes }
        }
        cost = 75
        fire_only_once = yes
        days_remove = 140
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove STG_integrate_chaos"
            every_state = {
                limit = { is_core_of = AWR }
                add_core_of = ROOT
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
}

FH_demo_equus_heartland_skeleton_category = {
    BLS_appoint_nebula_excavation = {
        visible = {
            tag = BLS
            NOT = { has_country_flag = BLS_recently_recalled_nebula }
            BLS_Nebula_Flare = { NOT = { has_character_flag = BLS_nebula_leading_excavation } }
        }
        cost = 25
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision BLS_appoint_nebula_excavation"
            custom_effect_tooltip = BLS_nebula_nerdy_tho_tt
            BLS_Nebula_Flare = { set_character_flag = BLS_nebula_leading_excavation }
            add_to_variable = { BLS_excavation_research_var = 0.075 }
            add_to_variable = { BLS_excavation_consumer_goods_var = 0.075 }
            add_to_variable = { BLS_excavation_pp_var = -0.1 }
            force_update_dynamic_modifier = yes
            set_country_flag = {
                flag = BLS_recently_appointed_nebula
                value = 1
                days = 180
            }
        }
        ai_will_do = {
            factor = 0
        }
    }
    BLS_recall_nebula_excavation = {
        visible = {
            tag = BLS
            NOT = { has_country_flag = BLS_recently_appointed_nebula }
            BLS_Nebula_Flare = { has_character_flag = BLS_nebula_leading_excavation }
        }
        cost = 25
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision BLS_recall_nebula_excavation"
            custom_effect_tooltip = BLS_sup_nerd_tt
            BLS_Nebula_Flare = { clr_character_flag = BLS_nebula_leading_excavation }
            add_to_variable = { BLS_excavation_research_var = -0.075 }
            add_to_variable = { BLS_excavation_consumer_goods_var = -0.075 }
            add_to_variable = { BLS_excavation_pp_var = 0.1 }
            force_update_dynamic_modifier = yes
            set_country_flag = {
                flag = BLS_recently_recalled_nebula
                value = 1
                days = 180
            }
        }
        ai_will_do = {
            factor = 0
        }
    }
    BLS_delve_deeper_into_necropolis = {
        visible = {
            tag = BLS
            NOT = { check_variable = { BLS_excavation_var > 3 } }
        }
        cost = 75
        days_remove = 90
        days_re_enable = 150
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove BLS_delve_deeper_into_necropolis"
            custom_effect_tooltip = BLS_for_science_tt
            add_to_variable = { BLS_excavation_research_var = 0.04 }
            add_to_variable = { BLS_excavation_consumer_goods_var = 0.075 }
            add_to_variable = { BLS_excavation_stability_var = -0.025 }
            add_to_variable = { BLS_excavation_construction_speed_var = -0.025 }
            if = {
                limit = { check_variable = { BLS_excavation_var = 3 } }
                add_to_variable = { BLS_excavation_conscription_var = -0.001 }
            }
            force_update_dynamic_modifier = yes
        }
        ai_will_do = {
            factor = 0
        }
    }
    BLS_finalize_dig = {
        visible = {
            tag = BLS
        }
        cost = 150
        days_remove = 180
        fire_only_once = yes
        modifier = {
            political_power_gain = -0.15
            production_speed_buildings_factor = -0.1
            research_speed_factor = 0.1
            conscription_factor = -0.25
        }
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision BLS_finalize_dig"
            custom_effect_tooltip = BLS_how_boring_tt
            remove_dynamic_modifier = { modifier = BLS_ancient_necropolis }
        }
        ai_will_do = {
            factor = 0
            modifier = {
                date > 1052.6.1
                add = 10
            }
        }
    }
    BLS_foal_stimulus_cheques = {
        visible = {
            tag = BLS
            date > 1050.6.1
        }
        cost = 100
        days_remove = 90
        fire_only_once = yes
        modifier = {
            stability_weekly_factor = -0.003
            consumer_goods_factor = 0.1
        }
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove BLS_foal_stimulus_cheques"
            remove_ideas = BLS_looming_population_collapse
        }
        ai_will_do = {
            factor = 10
        }
    }
    BLS_rectify_complaints = {
        visible = {
            tag = BLS
            date > 1051.3.1
        }
        cost = 100
        days_remove = 60
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove BLS_rectify_complaints"
            remove_ideas = BLS_unsafe_working_conditions
        }
    }
    BLS_secure_northern_perimeter = {
        visible = {
            tag = BLS
            date > 1051.5.1
        }
        cost = 25
        days_remove = 21
        fire_only_once = yes
        war_with_on_complete = WHY
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove BLS_secure_northern_perimeter"
            create_wargoal = {
                target = WHY
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 5
        }
    }
    BLS_eastern_buffer = {
        visible = {
            tag = BLS
            date > 1051.10.1
            country_exists = NER
        }
        cost = 25
        days_remove = 21
        fire_only_once = yes
        war_with_on_complete = NER
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove BLS_eastern_buffer"
            create_wargoal = {
                target = NER
                type = puppet_wargoal_focus
            }
        }
        ai_will_do = {
            factor = 5
            modifier = {
                has_war = yes
                factor = 0
            }
        }
    }
    BLS_foreign_student_integration = {
        visible = {
            tag = BLS
            WHY = { all_cores_owned_by_ROOT = yes }
        }
        highlight_states = {
            highlight_states_trigger = {
                is_core_of = WHY
            }
        }
        cost = 75
        days_remove = 45
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove BLS_foreign_student_integration"
            every_state = {
                limit = {
                    is_core_of = WHY
                    is_owned_by = ROOT
                }
                add_core_of = ROOT
            }
        }
        ai_will_do = {
            factor = 15
        }
    }
    BLS_buffer_around_changelings = {
        visible = {
            tag = BLS
            NER = { all_cores_owned_by_ROOT = yes }
        }
        highlight_states = {
            highlight_states_trigger = {
                is_core_of = NER
                NOT = { is_core_of = ROOT }
            }
        }
        cost = 30
        days_remove = 15
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove BLS_buffer_around_changelings"
            release_autonomy = {
                target = NER
                autonomy_state = autonomy_puppet
            }
            hidden_effect = {
                70 = {
                    add_core_of = ROOT
                    transfer_state_to = ROOT
                }
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    BLS_expand_the_safety_net = {
        visible = {
            tag = BLS
            date > 1052.2.24
            NER = {
                is_subject_of = ROOT
            }
            OR = {
                country_exists = CER
                country_exists = CLD
            }
        }
        cost = 25
        days_remove = 35
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove BLS_expand_the_safety_net"
            if = {
                limit = {
                    country_exists = CLD
                }
                create_wargoal = {
                    target = CLD
                    type = puppet_wargoal_focus
                }
            }
            if = {
                limit = {
                    country_exists = CER
                }
                create_wargoal = {
                    target = CER
                    type = puppet_wargoal_focus
                }
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    BLS_neutral_forts = {
        visible = {
            tag = BLS
            OR = {
                CER = { all_cores_owned_by_ROOT = yes }
                CLD = { all_cores_owned_by_ROOT = yes }
            }
        }
        highlight_states = {
            highlight_states_trigger = {
                OR = {
                    is_core_of = CER
                    is_core_of = CLD
                }
                is_owned_by = ROOT
            }
        }
        cost = 25
        days_remove = 35
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove BLS_neutral_forts"
            if = {
                limit = {
                    CER = { all_cores_owned_by_ROOT = yes }
                }
                release_autonomy = {
                    target = CER
                    autonomy_state = autonomy_puppet
                }
            }
            if = {
                limit = {
                    CLD = { all_cores_owned_by_ROOT = yes }
                }
                release_autonomy = {
                    target = CLD
                    autonomy_state = autonomy_puppet
                }
            }
        }
        ai_will_do = {
            factor = 20
            modifier = {
                has_war = yes
                factor = 0
            }
        }
    }
    BLS_secure_science = {
        visible = {
            tag = BLS
            date > 1053.8.1
        }
        cost = 25
        days_remove = 35
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove BLS_secure_science"
            3 = {
                ROOT = {
                    create_wargoal = {
                        target = PREV
                        type = annex_everything
                    }
                }
            }
        }
        ai_will_do = {
            factor = 10
            modifier = {
                NOT = {
                    NER = { is_subject_of = ROOT }
                    CER = { is_subject_of = ROOT }
                    CLD = { is_subject_of = ROOT }
                }
                factor = 0
            }
        }
    }
    BLS_integrate_glory = {
        visible = {
            tag = BLS
            owns_state = 3
            owns_state = 75
            owns_state = 12
        }
        highlight_states = {
            highlight_states_trigger = {
                is_core_of = EQS
                NOT = {
                    state = 3
                    state = 39
                    state = 186
                    state = 223
                    state = 40
                    state = 104
                }
            }
        }
        cost = 70
        days_remove = 70
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision BLS_integrate_glory"
            custom_effect_tooltip = BLS_steal_sanctum_univeristy_tt
            hidden_effect = {
                release_autonomy = {
                    target = EQS
                    autonomy_state = autonomy_puppet
                }
                3 = {
                    add_core_of = ROOT
                    transfer_state_to = ROOT
                }
                39 = {
                    add_core_of = ROOT
                    transfer_state_to = ROOT
                }
                186 = {
                    add_core_of = ROOT
                    transfer_state_to = ROOT
                }
                223 = {
                    add_core_of = ROOT
                    transfer_state_to = ROOT
                }
                40 = {
                    add_core_of = ROOT
                    transfer_state_to = ROOT
                }
                104 = {
                    add_core_of = ROOT
                    transfer_state_to = ROOT
                }
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    WHY_the_final_push = {
        visible = {
            tag = WHY
            date > 1049.6.1
        }
        cost = 100
        fire_only_once = yes
        days_remove = 70
        modifier = {
            weekly_manpower = -750
        }
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove WHY_the_final_push"
            add_manpower = -7283
            save_current_manpower = yes
            70 = {
                add_manpower = -18492
                damage_building = {
                    type = arms_factory
                    damage = 0.5
                }
            }
            remove_ideas = WHY_communist_agitators
        }
        ai_will_do = {
            factor = 10
        }
    }
    WHY_cleanse_the_heretics = {
        visible = {
            tag = WHY
            has_country_leader = { character = WHY_Opaline_Arcana ruling_only = yes }
        }
        cost = 75
        fire_only_once = yes
        days_remove = 35
        modifier = {
            political_power_gain = -0.1
            stability_weekly_factor = -0.01
        }
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove WHY_cleanse_the_heretics"
            add_stability = -0.05
            add_popularity = {
                ideology = neutrality
                popularity = -0.15
            }
            retire_character = WHY_Horizon_Sunrise
            remove_ideas = WHY_heretic_cult
        }
        ai_will_do = {
            factor = 10
        }
    }
    WHY_legalize_pluralistic_teachings = {
        visible = {
            tag = WHY
            NOT = { has_country_leader = { character = WHY_Opaline_Arcana ruling_only = yes } }
            date > 1051.1.1
        }
        cost = 125
        fire_only_once = yes
        days_remove = 35
        modifier = {
            political_power_gain = -0.25
        }
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove WHY_legalize_pluralistic_teachings"
            add_stability = -0.075
            add_popularity = {
                ideology = neutrality
                popularity = 0.075
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    WHY_formalize_control = {
        visible = {
            tag = WHY
            has_country_leader = { character = WHY_Solar_Strike ruling_only = yes }
            date > 1051.1.1
        }
        cost = 75
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision WHY_formalize_control"
            remove_ideas = WHY_imperial_military_junta
        }
        ai_will_do = {
            factor = 10
        }
    }
    WHY_formal_representation = {
        visible = {
            tag = WHY
            date > 1051.4.1
        }
        cost = 60
        fire_only_once = yes
        days_remove = 35
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove WHY_formalize_control"
            remove_ideas = WHY_ephemeral_rule
        }
        ai_will_do = {
            factor = 10
        }
    }
    WHY_brainwash_the_conscripts = {
        visible = {
            tag = WHY
            date > 1052.2.1
        }
        cost = 100
        fire_only_once = yes
        days_remove = 35
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove WHY_brainwash_the_conscripts"
            remove_ideas = WHY_forced_conscription
        }
        ai_will_do = {
            factor = 10
        }
    }
    WHY_finish_the_war = {
        visible = {
            tag = WHY
            date > 1051.10.1
        }
        cost = 25
        fire_only_once = yes
        days_remove = 21
        war_with_on_complete = NER
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove WHY_finish_the_war"
            create_wargoal = {
                target = NER
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    WHY_violate_neutrality = {
        visible = {
            tag = WHY
            date > 1052.4.1
        }
        cost = 25
        fire_only_once = yes
        days_remove = 21
        war_with_on_complete = BLS
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove WHY_violate_neutrality"
            create_wargoal = {
                target = BLS
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
            modifier = {
                has_war = yes
                factor = 0
            }
        }
    }
    WHY_crash_the_clouds = {
        visible = {
            tag = WHY
            date > 1052.9.12
            has_country_leader = { character = WHY_Opaline_Arcana ruling_only = yes }
        }
        cost = 25
        fire_only_once = yes
        days_remove = 21
        war_with_on_complete = CLD
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove WHY_crash_the_clouds"
            create_wargoal = {
                target = CLD
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
            modifier = {
                has_war = yes
                factor = 0
            }
        }
    }
    WHY_harmonic_mistakes = {
        visible = {
            tag = WHY
            date > 1053.2.4
            has_country_leader = { character = WHY_Opaline_Arcana ruling_only = yes }
        }
        cost = 25
        fire_only_once = yes
        days_remove = 21
        war_with_on_complete = CER
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove WHY_harmonic_mistakes"
            create_wargoal = {
                target = CER
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 10
            modifier = {
                has_war = yes
                factor = 0
            }
        }
    }
    WHY_the_new_generation = {
        visible = {
            tag = WHY
            CER = { all_cores_owned_by_ROOT_or_subject = yes }
            has_country_leader = { character = WHY_Opaline_Arcana ruling_only = yes }
        }
        cost = 10
        fire_only_once = yes
        days_remove = 30
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove WHY_the_new_generation"
            custom_effect_tooltip = WHY_mistybetes_tt
            hidden_effect = {
                every_possible_country = {
                    limit = {
                        has_character = CER_Misty_Brightdawn
                    }
                    CER_Misty_Brightdawn = {
                        set_nationality = ROOT
                    }
                }
            }
        }
        ai_will_do = {
            factor = 1000 #Misty's too wholesome and cute
        }
    }
    WHY_ascend_to_alicornhood = {
        visible = {
            tag = WHY
            date > 1055.1.1
            has_country_leader = { character = WHY_Opaline_Arcana ruling_only = yes }
        }
        cost = 25
        fire_only_once = yes
        days_remove = 35
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove WHY_ascend_to_alicornhood"
            3 = {
                ROOT = {
                    create_wargoal = {
                        target = PREV
                        type = annex_everything
                    }
                }
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    WHY_government_expansion = {
        visible = {
            tag = WHY
            NER = { all_cores_owned_by_ROOT_or_subject = yes }
        }
        highlight_states = {
            highlight_states_trigger = {
                is_core_of = NER
                NOT = { is_core_of = ROOT }
            }
        }
        cost = 50
        fire_only_once = yes
        days_remove = 70
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove WHY_government_expansion"
            every_state = {
                limit = {
                    is_core_of = NER
                    NOT = { is_core_of = ROOT }
                }
                add_core_of = ROOT
            }
        }
        ai_will_do = {
            factor = 15
        }
    }
    WHY_university_integration = {
        visible = {
            tag = WHY
            BLS = { all_cores_owned_by_ROOT_or_subject = yes }
        }
        highlight_states = {
            highlight_states_trigger = {
                is_core_of = BLS
            }
        }
        cost = 50
        fire_only_once = yes
        days_remove = 35
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove WHY_university_integration"
            every_state = {
                limit = {
                    is_core_of = BLS
                }
                add_core_of = ROOT
            }
        }
        ai_will_do = {
            factor = 15
        }
    }
    WHY_harmonys_gasp = {
        visible = {
            tag = WHY
            CER = { all_cores_owned_by_ROOT_or_subject = yes }
            CLD = { all_cores_owned_by_ROOT_or_subject = yes }
        }
        highlight_states = {
            highlight_states_trigger = {
                OR = {
                    is_core_of = CLD
                    is_core_of = CER
                }
            }
        }
        cost = 100
        days_remove = 105
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove WHY_harmonys_gasp"
            every_state = {
                limit = {
                    OR = {
                        is_core_of = CER
                        is_core_of = CLD
                    }
                }
                add_core_of = ROOT
            }
        }
        ai_will_do = {
            factor = 15
        }
    }
    CLD_CER_NER_state_border_wars = {
        visible = {
            OR = {
                AND = {
                    tag = CER
                    any_state = {
                        NOT = { is_owned_by = CER }
                        OR = {
                            is_core_of = CLD
                            is_core_of = CCA
                            is_core_of = SCS
                        }
                        OR = {
                            is_claimed_by = CER
                            is_core_of = CER
                        }
                    }
                }
                AND = {
                    tag = CLD
                    any_state = {
                        NOT = { is_owned_by = CLD }
                        OR = {
                            is_core_of = CER
                            is_core_of = NER
                        }
                        OR = {
                            is_claimed_by = CLD
                            is_core_of = CLD
                        }
                    }
                }
                AND = {
                    tag = NER
                    any_state = {
                        NOT = { is_owned_by = NER }
                        OR = {
                            is_core_of = CLD
                            is_core_of = NCA
                        }
                        OR = {
                            is_claimed_by = NER
                            is_core_of = NER
                        }
                    }
                }
            }
        }
        state_target = yes
        target_trigger = {
            FROM = {
                OR = {
                    is_claimed_by = ROOT
                    is_core_of = ROOT
                }
                OR = {
                    is_core_of = CLD
                    is_core_of = NCA
                    is_core_of = CCA
                    is_core_of = NER
                    is_core_of = CER
                    is_core_of = SCS
                }
                NOT = { is_owned_by = ROOT }
            }
        }
        days_re_enable = 180
        cost = 35
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove CLD_CER_NER_state_border_wars"
            FROM = {
                set_temp_variable = { FH_defending_state = id }
                set_state_flag = FH_defender_state
            }
            random_state = {
                limit = {
                    any_neighbor_state = {
                        state = var:FH_defending_state
                    }
                    is_owned_by = ROOT
                }
                set_temp_variable = { FH_attacking_state = id }
            }
            start_border_war = {
                change_state_after_war = no
                attacker = {
                    state = var:FH_attacking_state
                    num_provinces = 4
                    on_win = fh_demo.3
                    on_lose = fh_demo.4
                    on_cancel = fh_demo.5
                }
                defender = {
                    state = var:FH_defending_state
                    num_provinces = 4
                    on_win = fh_demo.6
                    on_lose = fh_demo.4
                    on_cancel = fh_demo.5
                }
            }
        }
        ai_will_do = {
            factor = 5
        }
    }
    CER_crack_down_on_protests = {
        visible = {
            tag = CER
            date < 1049.4.1
            is_ai = no
        }
        cost = 50
        days_remove = 35
        days_re_enable = 90
        modifier = {
            political_power_gain = -0.15
            stability_weekly_factor = 0.007
            war_support_weekly_factor = 0.007
            communism_drift = -0.03
            fascism_drift = -0.03
        }
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove CER_crack_down_on_protests"
        }
    }
    CER_end_the_hunt = {
        visible = {
            tag = CER
            date > 1049.4.1
        }
        cost = 150
        days_remove = 70
        fire_only_once = yes
        modifier = {
            stability_weekly_factor = -0.002
            political_power_gain = -0.25
        }
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove CER_end_the_hunt"
            remove_ideas = CER_legitimacy_crisis
            remove_ideas = CER_infinite_regency
        }
        ai_will_do = { factor = 1 }
    }
    CER_council_funds_for_industry = {
        visible = {
            tag = CER
            date > 1050.9.1
        }
        cost = 150
        days_remove = 70
        fire_only_once = yes
        modifier = {
            political_power_gain = -0.15
            consumer_goods_factor = 0.25
        }
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove CER_council_funds_for_industry"
            remove_ideas = CER_lacking_urbanization
            remove_ideas = CER_equestrias_breadbasket
        }
        ai_will_do = { factor = 1 }
    }
    CER_recognize_daybreakers_threat = {
        visible = {
            tag = CER
            date > 1051.4.17
            is_subject = no
            CLD = {
                exists = yes
                is_subject = no
            }
        }
        cost = 75
        days_remove = 35
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove CER_recognize_daybreakers_threat"
            CLD = { country_event = equestria_activity.19 }
        }
        ai_will_do = {
            factor = 100
        }
    }
    CER_expand_defense_faction_membership = {
        visible = {
            tag = CER
            date > 1053.1.1
            is_faction_leader = yes
            is_subject = no
            any_other_country = {
                OR = {
                    tag = NER
                    tag = BLS
                    tag = WHY
                }
                NOT = { has_country_flag = CER_rejected_alliance }
                NOT = { has_country_leader = { character = WHY_Opaline_Arcana ruling_only = yes } }
                is_subject = no
            }
        }
        cost = 50
        days_remove = 14
        days_re_enable = 60
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove CER_expand_defense_faction_membership"
            random_other_country = {
                limit = {
                    OR = {
                        tag = NER
                        tag = BLS
                        tag = WHY
                    }
                    is_subject = no
                    NOT = { has_country_flag = CER_rejected_alliance }
                    NOT = { has_country_leader = { character = WHY_Opaline_Arcana ruling_only = yes } }
                }
                country_event = equestria_activity.20
            }
        }
        ai_will_do = {
            factor = 100
        }
    }
    CER_topple_the_mad_inquisitor = {
        visible = {
            tag = CER
            date > 1054.1.1
            is_faction_leader = yes
            is_subject = no
            WHY = {
                exists = yes
                NOT = { is_subject_of = SLE }
                has_country_leader = { character = WHY_Opaline_Arcana ruling_only = yes }
            }
        }
        cost = 25
        days_remove = 35
        war_with_on_remove = WHY
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove CER_topple_the_mad_inquisitor"
            create_wargoal = {
                target = WHY
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 100
            modifier = {
                has_war = yes
                factor = 0
            }
        }
    }
    CER_a_complete_front = {
        visible = {
            tag = CER
            CLD = {
                OR = {
                    exists = no
                    is_in_faction_with = ROOT
                }
            }
            BLS = {
                OR = {
                    exists = no
                    is_in_faction_with = ROOT
                }
            }
            NER = {
                OR = {
                    exists = no
                    is_in_faction_with = ROOT
                }
            }
            WHY = {
                OR = {
                    exists = no
                    is_in_faction_with = ROOT
                }
            }
        }
        cost = 75
        days_remove = 70
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove CER_a_complete_front"
            every_other_country = {
                limit = {
                    OR = {
                        tag = NER
                        tag = CLD
                        tag = BLS
                        tag = WHY
                    }
                    is_in_faction_with = ROOT
                    exists = yes
                }
                country_event = equestria_activity.22
            }
        }
        ai_will_do = {
            factor = 100
        }
    }
    CER_beat_down_EQS = {
        visible = {
            tag = CER
            date > 1055.1.1
            is_subject = no
            country_exists = SLE
        }
        cost = 50
        days_remove = 70
        war_with_on_remove = EQS
        war_with_on_remove = SLW
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove CER_beat_down_EQS"
            create_wargoal = {
                target = SLE
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 100
            modifier = {
                has_war = yes
                factor = 0
            }
        }
    }
    CER_united_at_last = {
        visible = {
            tag = CER
            owns_state = 3
            NOT = { country_exists = SLE }
        }
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision CER_united_at_last"
            set_cosmetic_tag = EQS_NEW
            every_state = {
                limit = {
                    is_core_of = EQS
                }
                add_core_of = ROOT
            }
            set_capital = { state = 3 }
            set_province_name = { id = 179 name = renamed_canterlot }
            hidden_effect = {
                news_event = fhequusnews.5
            }
        }
        ai_will_do = {
            factor = 100
        }
    }
    CLD_redelegate_responsibilities = {
        visible = {
            tag = CLD
            date > 1051.5.7
        }
        cost = 75
        days_remove = 70
        modifier = {
            stability_weekly_factor = -0.005
            political_power_gain = -0.25
        }
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove CLD_redelegate_responsibilities"
            remove_ideas = CLD_extreme_centralization
        }
        ai_will_do = { factor = 1 }
    }
    NER_singular_purposes = {
        visible = {
            tag = NER
            WHY = { all_cores_owned_by_ROOT = yes }
        }
        cost = 25
        days_remove = 14
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove NER_singular_purposes"
            remove_ideas = NER_splintered_community
        }
        ai_will_do = { factor = 1 }
    }
    NER_community_intercessions = {
        visible = {
            tag = NER
            date > 1052.1.1
        }
        cost = 75
        days_remove = 35
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove NER_community_intercessions"
            remove_ideas = NER_inter_commune_rivalries
        }
        ai_will_do = { factor = 1 }
    }
    NER_centralize_military_command = {
        visible = {
            tag = NER
            date > 1050.9.1
        }
        cost = 100
        days_remove = 35
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove NER_centralize_military_command"
            remove_ideas = NER_commune_militias
        }
        ai_will_do = { factor = 1 }
    }
    NER_retire_vascular_brood = {
        visible = {
            tag = NER
            date > 1051.6.1
        }
        cost = 125
        days_remove = 35
        modifier = {
            consumer_goods_factor = 0.25
            war_support_weekly_factor = -0.02
            experience_gain_army = -0.03
        }
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove NER_retire_vascular_brood"
            remove_ideas = NER_veteran_changeling_commanders
        }
        ai_will_do = { factor = 1 }
    }
}

FH_demo_CRY_skeleton_category = {
    CRY_the_practical_leader = {
        visible = {
            date > 1049.7.20
        }
        cost = 25
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision CRY_the_practical_leader"
            promote_character = CRY_luna
            hidden_effect = {
                remove_ideas = luna_advisor
                add_ideas = princess_cadance
                set_cosmetic_tag = CRY_LUN
            }
            add_popularity = {
                ideology = fascism
                popularity = 0.02
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    CRY_end_the_protests = {
        visible = {
            has_country_leader = { character = CRY_luna ruling_only = yes }
        }
        cost = 75
        days_remove = 30
        fire_only_once = yes
        modifier = {
            stability_weekly_factor = -0.005
            weekly_manpower = -750
            production_output_factory = -0.15
        }
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove CRY_end_the_protests"
            remove_ideas = CRY_union_unrest
        }
        ai_will_do = {
            factor = 15
        }
    }
    CRY_earmark_military_funding = {
        visible = {
            has_country_leader = { character = CRY_luna ruling_only = yes }
            date > 1050.2.4
        }
        cost = 100
        days_remove = 70
        fire_only_once = yes
        modifier = {
            consumer_goods_factor = 0.35
            political_power_gain = -0.15
        }
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove CRY_earmark_military_funding"
            remove_ideas = CRY_dilapidated_army
        }
        ai_will_do = {
            factor = 10
        }
    }
    CRY_shirk_the_treaty = {
        visible = {
            has_country_leader = { character = CRY_luna ruling_only = yes }
            date > 1050.10.30
        }
        cost = 75
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision CRY_shirk_the_treaty"
            remove_ideas = CRY_treaty_of_gossamer_hills
            1220 = {
                set_demilitarized_zone = no
            }
            CHN = {
                add_opinion_modifier = {
                    target = ROOT
                    modifier = hostility
                }
                hidden_effect = {
                    remove_ideas = CHN_gossamer_hills
                }
            }
        }
        ai_will_do = {
            factor = 10
        }
    }
    CRY_retake_the_east = {
        visible = {
            date > 1051.1.1
        }
        cost = 25
        days_remove = 14
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision remove CRY_retake_the_east"
            98 = {
                ROOT = {
                    create_wargoal = {
                        target = PREV
                        type = annex_everything
                    }
                }
            }
        }
        ai_will_do = {
            factor = 20
            modifier = {
                has_war = yes
                factor = 0
            }
        }
    }
}
FH_demo_BRF_skeleton_category = {
    BRF_propose_unification = {
        available = {
            date > 1051.12.31
            is_subject = no
            is_faction_leader = yes
            any_other_country = {
                OR = {
                    tag = LUS
                    tag = GRY
                    tag = GRU
                }
                is_in_faction_with = ROOT
            }
        }
        cost = 50
        fire_only_once = yes
        complete_effect = {
            log = "[GetDateText]: [ROOT.GetName]: decision BRF_propose_unification"
            every_other_country = {
                limit = {
                    OR = {
                        tag = LUS
                        tag = GRY
                        tag = GRU
                    }
                    is_in_faction_with = ROOT
                }
                country_event = griffonia_activity.1
            }
        }
        ai_will_do = {
            factor = 1000
        }
    }
}
