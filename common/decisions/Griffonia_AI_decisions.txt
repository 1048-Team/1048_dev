EQC_AI_decision_category = {
    EQC_fix_economy_1 = {
        available = {
            date > 1052.1.1
        }
        days_remove = 730
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [Root.GetName]: decision remove EQC_fix_economy_1"
            remove_ideas = EQC_always_correct_quotas
        }

        ai_will_do = {
            base = 10
            modifier = {
                date > 1052.1.1
                has_war_with = GRI
                add = 10
            }
        }
    }
    EQC_fix_economy_2 = {
        available = {
            date > 1050.1.1
        }
        days_remove = 365
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [Root.GetName]: decision remove EQC_fix_economy_2"
            clr_country_flag = EQC_MARESOC_laws
        }

        ai_will_do = {
            base = 10
        }
    }
    EQC_fix_military = {
        available = {
            date > 1050.1.1
        }
        days_remove = 500
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [Root.GetName]: decision remove EQC_fix_military"
            remove_ideas = EQC_eager_recruits
        }

        ai_will_do = {
            base = 0
            modifier = {
                date > 1051.1.1
                add = 10
            }
        }
    }
    EQC_start_limited_war_1 = {
        available = {
            griffonia_its_time = yes
            NOT = { has_country_flag = FH_block_limited_war }
            check_variable = { FH_limited_war_escalator < 4 }
        }
        fire_only_once = yes
        days_remove = 20
        war_with_on_remove = GRI
        war_with_on_remove = BRF
        war_with_on_remove = RCT
        complete_effect = {
            griffonia_alert_nations = yes
            griffonia_trigger_cooldown = yes
            GRI = { activate_mission = GRI_limited_war_1_mission }
            RCT = { activate_mission = RCT_limited_war_1_mission }
        }
        remove_effect = {
            log = "[GetDateText]: [Root.GetName]: decision remove EQC_start_limited_war_1"
            add_to_variable = { FH_limited_war_escalator = 1 }
            set_country_flag = FH_block_limited_war
            #GRI & Puppets
            declare_war_on = {
                target = BOI
                type = annex_everything
            }
            #League of Free Nations
            declare_war_on = {
                target = BRF
                type = annex_everything
            }
            #River Republic
            declare_war_on = {
                target = LNS
                type = annex_everything
            }
            activate_mission = EQC_war_GRI_mission
            activate_mission = EQC_war_LFN_mission
            griffonia_calm_down_a_bit = yes
            if = {
                limit = {
                    GRI = { is_ai = no }
                }
                RCT = {
                    activate_mission = RCT_limited_war_1_mission
                }
            }
        }

        ai_will_do = {
            base = 100
        }
    }
    EQC_war_GRI_mission = {
        activation = {
            always = no
        }
        available = {
            owns_needed_GRI_territory_EQC = yes
        }
        days_mission_timeout = 100
        complete_effect = {
            log = "[GetDateText]: [Root.GetName]: mission complete EQC_war_GRI_mission"
            griffonia_GRI_land_transfer_EQC = yes
        }
        timeout_effect = {
            log = "[GetDateText]: [Root.GetName]: mission timeout EQC_war_GRI_mission"
            every_enemy_country = {
                white_peace = EQC
                white_peace = SIC
                white_peace = MIT
                white_peace = HPS
            }
        }
    }
    EQC_war_LFN_mission = {
        activation = {
            always = no
        }
        available = {
            owns_needed_BRF_territory_EQC = yes
        }
        days_mission_timeout = 80
        complete_effect = {
            log = "[GetDateText]: [Root.GetName]: mission complete EQC_war_LFN_mission"
            griffonia_BRF_land_transfer_EQC = yes
        }
        timeout_effect = {
            log = "[GetDateText]: [Root.GetName]: mission timeout EQC_war_LFN_mission"
            every_enemy_country = {
                limit = {
                    NOT = {
                        OR = {
                            tag = GRI
                            is_in_faction_with = GRI
                        }
                    }
                }
                white_peace = EQC
                white_peace = SIC
                white_peace = MIT
                white_peace = HPS
            }
        }
    }
}

GRI_AI_decision_category = {
    #Only triggers if EQC is played by a human
    GRI_start_limited_war_1 = {
        available = {
            griffonia_its_time = yes
            EQC = { is_ai = no }
            NOT = { has_country_flag = FH_block_limited_war }
            check_variable = { FH_limited_war_escalator < 4 }
        }
        fire_only_once = yes
        days_remove = 20
        war_with_on_remove = EQC
        war_with_on_remove = BRF
        war_with_on_remove = RCT
        complete_effect = {
            griffonia_alert_nations = yes
            griffonia_trigger_cooldown = yes
            RCT = { activate_mission = RCT_limited_war_1_mission }
        }
        remove_effect = {
            log = "[GetDateText]: [Root.GetName]: decision remove GRI_start_limited_war_1"
            add_to_variable = { FH_limited_war_escalator = 1 }
            set_country_flag = FH_block_limited_war
            #EQC & Puppets
            declare_war_on = {
                target = SIC
                type = annex_everything
            }
            #League of Free Nations
            declare_war_on = {
                target = BRF
                type = annex_everything
            }
            #River Republic
            declare_war_on = {
                target = LNS
                type = annex_everything
            }
            griffonia_calm_down_a_bit = yes
            activate_mission = GRI_limited_war_1_mission
            if = {
                limit = {
                    EQC = {
                        is_ai = no
                    }
                    BRF = {
                        is_ai = yes
                    }
                }
                BRF = {
                    declare_war_on = {
                        target = SIC
                        type = annex_everything
                    }
                    griffonia_calm_down_a_bit = yes
                }
            }
        }

        ai_will_do = { base = 100 }
    }
    GRI_limited_war_1_mission = {
        activation = {
            always = no
        }
        available = {
            always = no
        }
        war_with_on_timeout = RCT
        days_mission_timeout = 20
        timeout_effect = {
            log = "[GetDateText]: [Root.GetName]: mission timeout GRI_limited_war_1_mission"
            #River Republic
            declare_war_on = {
                target = LNS
                type = annex_everything
            }
            activate_mission = GRI_war_RCT_mission
            activate_mission = GRI_war_EQC_mission
            add_threat = -11.1
            if = {
                limit = {
                    RCT = {
                        is_ai = no
                    }
                    BRF = {
                        is_ai = yes
                    }
                }
                BRF = {
                    declare_war_on = {
                        target = LNS
                        type = annex_everything
                    }
                    add_threat = -11
                }
            }
        }
    }
    GRI_war_RCT_mission = {
        activation = {
            always = no
        }
        available = {
            owns_needed_RCT_territory_GRI = yes
        }
        days_mission_timeout = 100
        complete_effect = {
            log = "[GetDateText]: [Root.GetName]: mission complete GRI_war_RCT_mission"
            griffonia_RCT_land_transfer_GRI = yes
        }
        timeout_effect = {
            log = "[GetDateText]: [Root.GetName]: mission timeout GRI_war_RCT_mission"
            every_enemy_country = {
                white_peace = GRI
                white_peace = BOI
                white_peace = HLQ
                white_peace = NTR
            }
        }
    }
    GRI_war_EQC_mission = {
        activation = {
            always = no
        }
        available = {
            owns_needed_EQC_territory_GRI = yes
        }
        days_mission_timeout = 100
        complete_effect = {
            log = "[GetDateText]: [Root.GetName]: mission complete GRI_war_EQC_mission"
            griffonia_EQC_land_transfer_GRI = yes
        }
        timeout_effect = {
            log = "[GetDateText]: [Root.GetName]: mission timeout GRI_war_EQC_mission"
            every_enemy_country = {
                white_peace = GRI
                white_peace = BOI
                white_peace = HLQ
                white_peace = NTR
            }
        }
    }
    GRI_kill_FAT = {
        available = {
            date > 1050.5.1
        }
        days_remove = 30
        cost = 50
        fire_only_once = yes
        war_with_on_remove = FAT
        remove_effect = {
            log = "[GetDateText]: [Root.GetName]: decision remove GRI_kill_FAT"
            declare_war_on = {
                target = FAT
                type = annex_everything
            }
        }

        ai_will_do = {
            base = 10
        }
    }
    GRI_kill_TRD = {
        available = {
            date > 1052.11.1
        }
        days_remove = 30
        cost = 50
        fire_only_once = yes
        war_with_on_remove = TRD
        remove_effect = {
            log = "[GetDateText]: [Root.GetName]: decision remove GRI_kill_TRD"
            declare_war_on = {
                target = TRD
                type = annex_everything
            }
        }

        ai_will_do = {
            base = 10
        }
    }
    GRI_integrate_NTR = {
        available = {
            date > 1052.06.1
            is_subject = no
            NTR = {
                is_subject_of = GRI
                is_ai = yes
            }
        }
        days_remove = 10
        cost = 50
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [Root.GetName]: decision remove GRI_integrate_NTR"
            every_state = {
                limit = {
                    is_core_of = NTR
                }
                add_core_of = GRI
            }
            annex_country = {
                target = NTR
                transfer_troops = yes
            }
        }

        ai_will_do = {
            base = 10
        }
    }
    GRI_integrate_BOI = {
        available = {
            date > 1052.12.1
            is_subject = no
            BOI = {
                is_subject_of = GRI
                is_ai = yes
            }
        }
        days_remove = 10
        cost = 50
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [Root.GetName]: decision remove GRI_integrate_NTR"
            every_state = {
                limit = {
                    is_core_of = BOI
                }
                add_core_of = GRI
            }
            annex_country = {
                target = BOI
                transfer_troops = yes
            }
        }

        ai_will_do = {
            base = 10
        }
    }
}

RCT_AI_decision_category = {
    RCT_limited_war_1_mission = {
        activation = {
            always = no
        }
        available = {
            always = no
        }
        war_with_on_timeout = BRF
        days_mission_timeout = 20
        timeout_effect = {
            log = "[GetDateText]: [Root.GetName]: mission timeout RCT_limited_war_1_mission"
            #League of Free Nations
            declare_war_on = {
                target = GRY
                type = annex_everything
            }
            activate_mission = RCT_war_GRI_mission
            activate_mission = RCT_war_LFN_mission
            if = {
                limit = {
                    GRI = {
                        is_ai = no
                    }
                }
                declare_war_on = {
                    target = HLQ
                    type = annex_everything
                }
            }
            griffonia_calm_down_a_bit = yes
        }
    }
    RCT_war_GRI_mission = {
        activation = {
            always = no
        }
        available = {
            owns_needed_GRI_territory_RCT = yes
        }
        days_mission_timeout = 100
        complete_effect = {
            log = "[GetDateText]: [Root.GetName]: mission complete RCT_war_GRI_mission"
            griffonia_GRI_land_transfer_RCT = yes
        }
        timeout_effect = {
            log = "[GetDateText]: [Root.GetName]: mission timeout RCT_war_GRI_mission"
            every_enemy_country = {
                white_peace = RCT
                white_peace = LNS
                white_peace = BAR
                white_peace = NIM
            }
        }
    }
    RCT_war_LFN_mission = {
        activation = {
            always = no
        }
        available = {
            owns_needed_BRF_territory_RCT = yes
        }
        days_mission_timeout = 80
        complete_effect = {
            log = "[GetDateText]: [Root.GetName]: mission complete RCT_war_LFN_mission"
            griffonia_BRF_land_transfer_RCT = yes
        }
        timeout_effect = {
            log = "[GetDateText]: [Root.GetName]: mission timeout RCT_war_LFN_mission"
            every_enemy_country = {
                limit = {
                    NOT = {
                        OR = {
                            tag = GRI
                            is_in_faction_with = GRI
                        }
                    }
                }
                white_peace = RCT
                white_peace = LNS
                white_peace = BAR
                white_peace = NIM
            }
        }
    }
    RCT_integrate_NIM = {
        available = {
            date > 1050.05.1
            is_subject = no
            NIM = {
                is_subject_of = RCT
                is_ai = yes
            }
        }
        days_remove = 10
        cost = 50
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [Root.GetName]: decision remove RCT_integrate_NIM"
            every_state = {
                limit = {
                    is_core_of = NIM
                }
                add_core_of = RCT
            }
            annex_country = {
                target = NIM
                transfer_troops = yes
            }
        }

        ai_will_do = {
            base = 10
        }
    }
    RCT_integrate_BAR = {
        available = {
            date > 1051.1.1
            is_subject = no
            BAR = {
                is_subject_of = RCT
                is_ai = yes
            }
        }
        days_remove = 10
        cost = 50
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [Root.GetName]: decision remove RCT_integrate_BAR"
            every_state = {
                limit = {
                    is_core_of = BAR
                }
                add_core_of = RCT
            }
            annex_country = {
                target = BAR
                transfer_troops = yes
            }
        }

        ai_will_do = {
            base = 10
        }
    }
    RCT_integrate_LNS = {
        available = {
            date > 1051.11.1
            is_subject = no
            LNS = {
                is_subject_of = RCT
                is_ai = yes
            }
        }
        days_remove = 10
        cost = 50
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [Root.GetName]: decision remove RCT_integrate_LNS"
            every_state = {
                limit = {
                    is_core_of = LNS
                }
                add_core_of = RCT
            }
            annex_country = {
                target = LNS
                transfer_troops = yes
            }
        }

        ai_will_do = {
            base = 10
        }
    }
}

BAK_AI_decision_category = {
    BAK_kill_FBK = {
        available = {
            date > 1050.04.1
        }
        days_remove = 10
        cost = 10
        fire_only_once = yes
        war_with_on_remove = FBK
        complete_effect = {
            add_offsite_building = {
                type = industrial_complex
                level = 2
            }
            add_offsite_building = {
                type = arms_factory
                level = 2
            }
        }
        remove_effect = {
            log = "[GetDateText]: [Root.GetName]: decision remove BAK_kill_FBK"
            declare_war_on = {
                target = FBK
                type = annex_everything
            }
        }

        ai_will_do = {
            base = 100
        }
    }
#    BAK_core_FBK = {
#        available = {
#            date > 1050.01.1
#        }
#        days_remove = 10
#        cost = 50
#        fire_only_once = yes
#        remove_effect = {
#            every_owned_state = {
#                limit = {
#                    is_core_of = FBK
#                }
#                add_core_of = BAK
#            }
#            hidden_effect = {
#            two_random_arms_factory = yes
#            two_random_arms_factory = yes
#            two_random_arms_factory = yes
#            }
#        }
#
#        ai_will_do = {
#            base = 100
#        }
#    }
}

HLR_AI_decision_category = {
    HLR_posture_north = {
        available = {
            GRI = { has_war = yes }
            NTR = { is_ai = yes }
        }
        days_remove = 15
        cost = 50
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [Root.GetName]: decision remove HLR_posture_north"
            573 = {
                transfer_state_to = HLR
                remove_core_of = NTR
                add_core_of = HLR
            }
            602 = {
                transfer_state_to = HLR
                remove_core_of = NTR
                add_core_of = HLR
            }
            574 = {
                transfer_state_to = HLR
                remove_core_of = NTR
                add_core_of = HLR
            }
            hidden_effect = {
            two_random_industrial_complex = yes
            two_random_industrial_complex = yes
            two_random_industrial_complex = yes
            }
        }

        ai_will_do = {
            base = 100
        }
    }
#    HLR_ally_BAK = {
#        available = {
#            BAK = { exists = yes }
#            date > 1050.03.3
#        }
#        days_remove = 15
#        cost = 50
#        fire_only_once = yes
#        remove_effect = {
#            create_faction = "Harmony-Pact"
#            add_to_faction = BAK
#            add_opinion_modifier = {
#                target = BAK
#                modifier = huge_increase
#            }
#            reverse_add_opinion_modifier = {
#                target = BAK
#                modifier = huge_increase
#            }
#            add_ai_strategy = {
#                type = alliance
#                id = BAK
#                value = 200
#            }
#            two_random_industrial_complex = yes
#            two_random_dockyards = yes
#            BAK = {
#                add_ai_strategy = {
#                    type = alliance
#                    id = HLR
#                    value = 200
#                }
#                two_random_industrial_complex = yes
#                two_random_dockyards = yes
#            }
#
#        }
#
#        ai_will_do = {
#            base = 100
#        }
#    }
#
#    HLR_finish_FBK = {
#        available = {
#            FBK = {
#                exists = yes
#                has_country_flag = FBK_expelled_BAK_flag
#            }
#            is_in_faction_with = BAK
#        }
#        days_remove = 50
#        cost = 50
#        fire_only_once = yes
#        war_with_on_remove = FBK
#        remove_effect = {
#            add_state_claim = 639
#            add_state_claim = 640
#            add_state_claim = 643
#            add_state_claim = 642
#            add_state_core = 639
#            add_state_core = 640
#            add_state_core = 643
#            add_state_core = 642
#            every_state = {
#                limit = {
#                    is_core_of = FBK
#                    NOT = { is_claimed_by = HLR }
#                }
#                add_claim_by = BAK
#            }
#            declare_war_on = {
#                target = FBK
#                type = annex_everything
#            }
#        }
#
#        ai_will_do = {
#            base = 100
#        }
#    }
    HLR_posture_north_2 = {
        available = {
            GRI = { has_war = yes }
            date > 1051.09.1
            NTR = { is_ai = yes }
        }
        days_remove = 15
        cost = 50
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [Root.GetName]: decision remove HLR_posture_north_2"
            459 = {
                transfer_state_to = HLR
                add_core_of = HLR
            }
            1226 = {
                transfer_state_to = HLR
                add_core_of = HLR
            }
            476 = {
                transfer_state_to = HLR
                add_core_of = HLR
            }
        }

        ai_will_do = {
            base = 100
        }
    }
}

FBK_AI_decision_category = {
    FBK_expel_BAK = {
        available = {
            has_war_with = BAK
            controls_state = 571
            controls_state = 455
            controls_state = 603
        }
        days_remove = 5
        cost = 5
        fire_only_once = yes
        remove_effect = {
            log = "[GetDateText]: [Root.GetName]: decision remove FBK_expel_BAK"
            white_peace = BAK
            transfer_state = 571
            set_country_flag = FBK_expelled_BAK_flag
        }

        ai_will_do = {
            base = 100
        }
    }
}
