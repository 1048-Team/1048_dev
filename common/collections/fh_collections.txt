equestrian_original_states = {
    input = constant:fh_state_groups.original_equestrian_states
    name = COLLECTION_EQUESTRIAN_ORIGINAL_STATES
}

controlled_equestrian_original_states = {
    input = collection:equestrian_original_states
    name = COLLECTION_CONTROLLED_EQUESTRIAN_ORIGINAL_STATES
    operators = {
        limit = {
            is_controlled_by_ROOT_or_ally = yes
        }
    }
}

solarist_nations = {
    input = game:all_countries
    name = COLLECTION_SOLARIST_NATIONS
    operators = {
        limit = {
            has_country_leader_ideology = solarism
        }
    }
}

all_griffonia_states = {
    input = game:all_states
    name = COLLECTION_ALL_GRIFFONIA_STATES
    operators = {
        limit = {
            is_on_continent = asia
        }
    }
}

all_equus_states = {
    input = game:all_states
    name = COLLECTION_ALL_EQUUS_STATES
    operators = {
        limit = {
            is_on_continent = europe
        }
    }
}

all_zebrica_states = {
    input = game:all_states
    name = COLLECTION_ALL_ZEBRICA_STATES
    operators = {
        limit = {
            is_on_continent = africa
        }
    }
}

all_controlled_world_states = {
    input = game:all_states
    name = COLLECTION_ALL_CONTROLLED_WORLD_STATES
    operators = {
        limit = {
            is_controlled_by_ROOT_or_ally = yes
        }
    }
}
