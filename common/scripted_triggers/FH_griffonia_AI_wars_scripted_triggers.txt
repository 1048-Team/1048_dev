griffonia_its_time = {
    IF = {
        limit = {
            check_variable = { global.FH_limited_war_escalator = 0 }
        }
        date > 1048.9.1
    }
    ELSE_IF = {
        limit = {
            check_variable = { global.FH_limited_war_escalator = 1 }
        }
        date > 1049.12.1
    }
    ELSE_IF = {
        limit = {
            check_variable = { global.FH_limited_war_escalator = 2 }
        }
        date > 1051.2.1
    }
    ELSE_IF = {
        limit = {
            check_variable = { global.FH_limited_war_escalator = 3 }
        }
        date > 1053.6.1
    }
}
owns_needed_GRI_territory_EQC = {
    IF = {
        limit = {
            has_country_flag = EQC_GRI_war_flag_4
        }
        OR = {
            AND = {
                401 = { is_controlled_by_ROOT_or_ally = yes }
                470 = { is_controlled_by_ROOT_or_ally = yes }
                442 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                495 = { is_controlled_by_ROOT_or_ally = yes }
                470 = { is_controlled_by_ROOT_or_ally = yes }
                401 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                470 = { is_controlled_by_ROOT_or_ally = yes }
                439 = { is_controlled_by_ROOT_or_ally = yes }
                401 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
    ELSE_IF = {
        limit = {
            has_country_flag = EQC_GRI_war_flag_3
        }
        OR = {
            AND = {
                389 = { is_controlled_by_ROOT_or_ally = yes }
                400 = { is_controlled_by_ROOT_or_ally = yes }
                489 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                389 = { is_controlled_by_ROOT_or_ally = yes }
                489 = { is_controlled_by_ROOT_or_ally = yes }
                439 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                389 = { is_controlled_by_ROOT_or_ally = yes }
                489 = { is_controlled_by_ROOT_or_ally = yes }
                447 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
    #Original
    ELSE_IF = {
        limit = {
            has_country_flag = EQC_GRI_war_flag_2
        }
        OR = {
            AND = {
                532 = { is_controlled_by_ROOT_or_ally = yes }
                391 = { is_controlled_by_ROOT_or_ally = yes }
                489 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                532 = { is_controlled_by_ROOT_or_ally = yes }
                391 = { is_controlled_by_ROOT_or_ally = yes }
                494 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                532 = { is_controlled_by_ROOT_or_ally = yes }
                489 = { is_controlled_by_ROOT_or_ally = yes }
                494 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
    ELSE_IF = {
        limit = {
            has_country_flag = EQC_GRI_war_flag_1
        }
        OR = {
            AND = {
                439 = { is_controlled_by_ROOT_or_ally = yes }
                547 = { is_controlled_by_ROOT_or_ally = yes }
                495 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                439 = { is_controlled_by_ROOT_or_ally = yes }
                495 = { is_controlled_by_ROOT_or_ally = yes }
                398 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                439 = { is_controlled_by_ROOT_or_ally = yes }
                398 = { is_controlled_by_ROOT_or_ally = yes }
                547 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
}
owns_needed_BRF_territory_EQC = {
    #Original
    IF = {
        limit = {
            has_country_flag = EQC_LFN_war_flag_1
        }
        OR = {
            AND = {
                651 = { is_controlled_by_ROOT_or_ally = yes }
                385 = { is_controlled_by_ROOT_or_ally = yes }
                589 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                651 = { is_controlled_by_ROOT_or_ally = yes }
                385 = { is_controlled_by_ROOT_or_ally = yes }
                414 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                651 = { is_controlled_by_ROOT_or_ally = yes }
                589 = { is_controlled_by_ROOT_or_ally = yes }
                414 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
    #If escalated further
    ELSE_IF = {
        limit = {
            has_country_flag = EQC_LFN_war_flag_2
        }
        OR = {
            AND = {
                508 = { is_controlled_by_ROOT_or_ally = yes }
                559 = { is_controlled_by_ROOT_or_ally = yes }
                512 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                508 = { is_controlled_by_ROOT_or_ally = yes }
                600 = { is_controlled_by_ROOT_or_ally = yes }
                414 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                508 = { is_controlled_by_ROOT_or_ally = yes }
                599 = { is_controlled_by_ROOT_or_ally = yes }
                414 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
    ELSE_IF = {
        limit = {
            has_country_flag = EQC_LFN_war_flag_3
        }
        OR = {
            AND = {
                508 = { is_controlled_by_ROOT_or_ally = yes }
                588 = { is_controlled_by_ROOT_or_ally = yes }
                605 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                508 = { is_controlled_by_ROOT_or_ally = yes }
                588 = { is_controlled_by_ROOT_or_ally = yes }
                414 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                508 = { is_controlled_by_ROOT_or_ally = yes }
                605 = { is_controlled_by_ROOT_or_ally = yes }
                414 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
}
owns_needed_RCT_territory_GRI = {
    IF = {
        limit = {
            has_country_flag = GRI_RCT_war_flag_0
        }
        OR = {
            AND = {
                479 = { is_controlled_by_ROOT_or_ally = yes }
                633 = { is_controlled_by_ROOT_or_ally = yes }
                554 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                553 = { is_controlled_by_ROOT_or_ally = yes }
                633 = { is_controlled_by_ROOT_or_ally = yes }
                544 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                540 = { is_controlled_by_ROOT_or_ally = yes }
                479 = { is_controlled_by_ROOT_or_ally = yes }
                554 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
    ELSE_IF = {
        limit = {
            has_country_flag = GRI_RCT_war_flag_1
        }
        OR = {
            AND = {
                1211 = { is_controlled_by_ROOT_or_ally = yes }
                633 = { is_controlled_by_ROOT_or_ally = yes }
                1230 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                1211 = { is_controlled_by_ROOT_or_ally = yes }
                633 = { is_controlled_by_ROOT_or_ally = yes }
                505 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                1211 = { is_controlled_by_ROOT_or_ally = yes }
                505 = { is_controlled_by_ROOT_or_ally = yes }
                1230 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
    #Original
    ELSE_IF = {
        limit = {
            has_country_flag = GRI_RCT_war_flag_2
        }
        OR = {
            AND = {
                1210 = { is_controlled_by_ROOT_or_ally = yes }
                552 = { is_controlled_by_ROOT_or_ally = yes }
                634 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                1210 = { is_controlled_by_ROOT_or_ally = yes }
                552 = { is_controlled_by_ROOT_or_ally = yes }
                543 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                1210 = { is_controlled_by_ROOT_or_ally = yes }
                634 = { is_controlled_by_ROOT_or_ally = yes }
                543 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
    ELSE_IF = {
        limit = {
            has_country_flag = GRI_RCT_war_flag_3
        }
        OR = {
            AND = {
                635 = { is_controlled_by_ROOT_or_ally = yes }
                499 = { is_controlled_by_ROOT_or_ally = yes }
                491 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                637 = { is_controlled_by_ROOT_or_ally = yes }
                491 = { is_controlled_by_ROOT_or_ally = yes }
                387 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                491 = { is_controlled_by_ROOT_or_ally = yes }
                499 = { is_controlled_by_ROOT_or_ally = yes }
                387 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
}
owns_needed_EQC_territory_GRI = {
    IF = {
        limit = {
            has_country_flag = EQC_GRI_war_flag_0
        }
        OR = {
            AND = {
                439 = { is_controlled_by_ROOT_or_ally = yes }
                547 = { is_controlled_by_ROOT_or_ally = yes }
                448 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                439 = { is_controlled_by_ROOT_or_ally = yes }
                470 = { is_controlled_by_ROOT_or_ally = yes }
                398 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                439 = { is_controlled_by_ROOT_or_ally = yes }
                398 = { is_controlled_by_ROOT_or_ally = yes }
                547 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
    ELSE_IF = {
        limit = {
            has_country_flag = EQC_GRI_war_flag_1
        }
        OR = {
            AND = {
                532 = { is_controlled_by_ROOT_or_ally = yes }
                401 = { is_controlled_by_ROOT_or_ally = yes }
                489 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                532 = { is_controlled_by_ROOT_or_ally = yes }
                401 = { is_controlled_by_ROOT_or_ally = yes }
                672 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                532 = { is_controlled_by_ROOT_or_ally = yes }
                489 = { is_controlled_by_ROOT_or_ally = yes }
                672 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
    #Original
    ELSE_IF = {
        limit = {
            has_country_flag = EQC_GRI_war_flag_2
        }
        OR = {
            AND = {
                389 = { is_controlled_by_ROOT_or_ally = yes }
                470 = { is_controlled_by_ROOT_or_ally = yes }
                442 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                389 = { is_controlled_by_ROOT_or_ally = yes }
                470 = { is_controlled_by_ROOT_or_ally = yes }
                401 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                389 = { is_controlled_by_ROOT_or_ally = yes }
                442 = { is_controlled_by_ROOT_or_ally = yes }
                401 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
    ELSE_IF = {
        limit = {
            has_country_flag = EQC_GRI_war_flag_3
        }
        OR = {
            AND = {
                401 = { is_controlled_by_ROOT_or_ally = yes }
                470 = { is_controlled_by_ROOT_or_ally = yes }
                442 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                448 = { is_controlled_by_ROOT_or_ally = yes }
                470 = { is_controlled_by_ROOT_or_ally = yes }
                401 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                470 = { is_controlled_by_ROOT_or_ally = yes }
                672 = { is_controlled_by_ROOT_or_ally = yes }
                401 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
}
owns_needed_GRI_territory_RCT = {
    IF = {
        limit = {
            has_country_flag = GRI_RCT_war_flag_4
        }
        OR = {
            AND = {
                635 = { is_controlled_by_ROOT_or_ally = yes }
                499 = { is_controlled_by_ROOT_or_ally = yes }
                491 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                637 = { is_controlled_by_ROOT_or_ally = yes }
                491 = { is_controlled_by_ROOT_or_ally = yes }
                387 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                491 = { is_controlled_by_ROOT_or_ally = yes }
                499 = { is_controlled_by_ROOT_or_ally = yes }
                387 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
    ELSE_IF = {
        limit = {
            has_country_flag = GRI_RCT_war_flag_3
        }
        OR = {
            AND = {
                1210 = { is_controlled_by_ROOT_or_ally = yes }
                552 = { is_controlled_by_ROOT_or_ally = yes }
                634 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                1210 = { is_controlled_by_ROOT_or_ally = yes }
                552 = { is_controlled_by_ROOT_or_ally = yes }
                1230 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                1210 = { is_controlled_by_ROOT_or_ally = yes }
                552 = { is_controlled_by_ROOT_or_ally = yes }
                1211 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
    #Original
    ELSE_IF = {
        limit = {
            has_country_flag = GRI_RCT_war_flag_2
        }
        OR = {
            AND = {
                1211 = { is_controlled_by_ROOT_or_ally = yes }
                633 = { is_controlled_by_ROOT_or_ally = yes }
                554 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                1211 = { is_controlled_by_ROOT_or_ally = yes }
                633 = { is_controlled_by_ROOT_or_ally = yes }
                505 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                1211 = { is_controlled_by_ROOT_or_ally = yes }
                505 = { is_controlled_by_ROOT_or_ally = yes }
                554 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
    ELSE_IF = {
        limit = {
            has_country_flag = GRI_RCT_war_flag_1
        }
        OR = {
            AND = {
                479 = { is_controlled_by_ROOT_or_ally = yes }
                555 = { is_controlled_by_ROOT_or_ally = yes }
                423 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                479 = { is_controlled_by_ROOT_or_ally = yes }
                423 = { is_controlled_by_ROOT_or_ally = yes }
                437 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                423 = { is_controlled_by_ROOT_or_ally = yes }
                555 = { is_controlled_by_ROOT_or_ally = yes }
                424 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
}
owns_needed_BRF_territory_RCT = {
    #Original
    IF = {
        limit = {
            has_country_flag = RCT_LFN_war_flag_1
        }
        OR = {
            AND = {
                598 = { is_controlled_by_ROOT_or_ally = yes }
                608 = { is_controlled_by_ROOT_or_ally = yes }
                610 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                598 = { is_controlled_by_ROOT_or_ally = yes }
                608 = { is_controlled_by_ROOT_or_ally = yes }
                596 = { is_controlled_by_ROOT_or_ally = yes }
            }
            AND = {
                598 = { is_controlled_by_ROOT_or_ally = yes }
                610 = { is_controlled_by_ROOT_or_ally = yes }
                596 = { is_controlled_by_ROOT_or_ally = yes }
            }
        }
    }
}
