###Was Test4Echo secretly a Lunarist bat-lover all along?!###
###Opening Tree
shared_focus = {
    id = EEE_bat_into_hell
    x = 0
    y = 0

    cost = 1

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_bat_into_hell"
        #Events begin, yay!
        ###Stellar Note simply watches as Lucky's militia retreats and thestrals take to the streets to celebrate freedom, as she knows they are now on their own, and she is not in any way, shape, or form prepared to lead a fully independent bat nation
        ###Secretly trigger short event chain for Nocturne researching another death in Ayacachtli, which earns a bit of suspicion on her and also danger to her since she encounters an Ezmetzli agent who ends up killing themselves versus capture
    }

    ai_will_do = {
        factor = 640
    }
}

shared_focus = {
    id = EEE_hope_for_best
    x = -2
    y = 1
    relative_position_id = EEE_bat_into_hell

    cost = 2
    prerequisite = { focus = EEE_bat_into_hell }
    mutually_exclusive = { focus = EEE_preemptive_response }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_hope_for_best"
        ###Short event describing that it'd be best not to anger the Neighua and hope that actually coexisting will lead to them slowly de-escalating, and work defensively
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_preemptive_response
    x = 2
    y = 1
    relative_position_id = EEE_bat_into_hell

    cost = 2
    prerequisite = { focus = EEE_bat_into_hell }
    mutually_exclusive = { focus = EEE_hope_for_best }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_preemptive_response"
        ###Short event stating that at the behest of some tribal leaders in the NEI regions, the militia, which is already strained and underequipped, will be used to occupy Red Glory, since it's the main settlement of the Neighua, and that they will have to keep them in line until they can be forced to end the rivalry
    }

    ai_will_do = {
        factor = 320
    }
}

shared_focus = {
    id = EEE_encourage_migration #Should speak about regardless of what is said by the Neighua, should warn all thestrals that can leave to leave, in case the worst happens
    x = 0
    y = 2
    relative_position_id = EEE_bat_into_hell

    cost = 2
    prerequisite = {
        focus = EEE_hope_for_best
        focus = EEE_preemptive_response
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_encourage_migration"
        #Effect here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_arrest_likely
    x = 0
    y = 1
    relative_position_id = EEE_preemptive_response

    cost = 2
    prerequisite = { focus = EEE_preemptive_response }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_arrest_likely"
        ###Short event of a tribal Neighua leader getting arrested in his own home, as he has been an agitator against the thestrals for years and needs to be put on trial for his actions and hopefully make the Neighua less rowdy
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_quiet_on_home_front
    x = -1
    y = 2
    relative_position_id = EEE_hope_for_best

    cost = 2
    prerequisite = { focus = EEE_hope_for_best }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_quiet_on_home_front"
        ###Short event of Stellar simply taking a moment to not worry about politics, since things have been rather quiet; perhaps introduce family, friend, or lover for her to spend time with
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_early_warning_signs
    x = 1
    y = 1
    relative_position_id = EEE_arrest_likely

    cost = 2
    prerequisite = { focus = EEE_arrest_likely }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_early_warning_signs"
        ###Short event of Stellar feeling tense and anxious as she can sense there's growing unrest in the Neighua and she meets with the Council to try and get more military stationed there to prevent an uprising, although they question it, with Waning especially being against it, despite being penchant for violent ideas
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_support_of_elders
    x = -2
    y = 1
    relative_position_id = EEE_quiet_on_home_front

    cost = 3
    prerequisite = { focus = EEE_quiet_on_home_front }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_support_of_elders"
        ###Maybe event of Stellar meeting with some of the tribal elders to get them to throw themselves behind her, for at least the time being, to support her plans to keep the nation stable while the chaos of the Neighua and confederation are happening
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_petitioning_tribes
    x = 0
    y = 1
    relative_position_id = EEE_quiet_on_home_front

    cost = 3
    prerequisite = {
        focus = EEE_quiet_on_home_front
        focus = EEE_early_warning_signs
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_petitioning_tribes"
        ###Stellar goes to some of the remaining tribes and asks for support for keeping the land stable and protected during this crisis, and gets some pledges of a few warriors to keep the peace
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_assessing_damage
    x = -1
    y = 1
    relative_position_id = EEE_early_warning_signs

    cost = 3
    prerequisite = {
        focus = EEE_quiet_on_home_front
        focus = EEE_early_warning_signs
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_assessing_damage"
        ###Possibly event of going with Nocturne to begin a reconstruction assessment of Ayacachtli from the liberation it received after Lucky retreated, and Nocturne takes time to warn Stellar to stay the course, and that she'll support Stellar
        30 = {
            add_extra_state_shared_building_slots = 1
        }
        add_stability = 0.02
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_burning_midnight_oil
    x = 2
    y = 1
    relative_position_id = EEE_early_warning_signs

    cost = 5
    prerequisite = {
        focus = EEE_quiet_on_home_front
        focus = EEE_early_warning_signs
    }
    available = {
        custom_trigger_tooltip = {
            tooltip = EEE_bossy_boots_tt
            check_variable = { EEE_stellar_election_influence > 69.99 }
        }
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_burning_midnight_oil"
        ###Possibly short event of Stellar arguing with the Moonspeaker Council on reforms she WILL get passed, and Waning of all ponies supports her; it could also be Waning if Stellar got supported by Gushing, as a sudden change of tone, with Nocturne or just a random council member throwing in support otherwise; Waning would be a surprise
    }

    ai_will_do = {
        factor = 40
        modifier = {
            check_variable = { EEE_stellar_election_influence > 84.99 }
            factor = 8
        }
    }
}

shared_focus = {
    id = EEE_debt_for_production
    x = -2
    y = 1
    relative_position_id = EEE_petitioning_tribes

    cost = 3
    prerequisite = { focus = EEE_petitioning_tribes }
    mutually_exclusive = { focus = EEE_promissory_military_investments }
    available = {
        NID = {
            exists = yes
            has_opinion = {
                target = ROOT
                value > 55
            }
        }
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_debt_for_production"
        ###Short event of Stellar arguing with the Council about indebting themselves to NID being the best option so they can move forward, before sending off a letter to NID asking to take loans from them to help rebuild infrastructure and a dam that Cobalt never finished
    }

    ai_will_do = { factor = 80 }
}

shared_focus = {
    id = EEE_promissory_military_investments
    x = 2
    y = 1
    relative_position_id = EEE_petitioning_tribes

    cost = 3
    prerequisite = {
        focus = EEE_petitioning_tribes
        focus = EEE_assessing_damage
    }
    mutually_exclusive = { focus = EEE_debt_for_production }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_promissory_military_investments"
        ###Short event of Stellar arguing with Nocturne and being supported by Waning for the increased military funding to avoid a risk of a more bloody war with the Neighua
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_meeting_enemy
    x = 0
    y = 1
    relative_position_id = EEE_assessing_damage

    cost = 3
    prerequisite = { focus = EEE_assessing_damage }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_meeting_enemy"
        ###Tranquil Serenade and Nocturne, who wanted to come along, meet with Red Dusk and Tranquil Serenade, as Stellar tries to get on the same ground as them, so that whoever succeeds in being chosen as the new tribal head, that they will have the best interest of the nation at heart; Tranquil and Red cannot agree on anything
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_basking_in_moonlight
    x = -1
    y = 1
    relative_position_id = EEE_burning_midnight_oil

    cost = 5
    prerequisite = { focus = EEE_burning_midnight_oil }
    mutually_exclusive = { focus = EEE_eclipsing_moon }
    available = {
        custom_trigger_tooltip = {
            tooltip = EEE_bossy_boots_tt
            check_variable = { EEE_stellar_election_influence > 69.99 }
        }
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_basking_in_moonlight"
        #Trait here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_eclipsing_moon
    x = 1
    y = 1
    relative_position_id = EEE_burning_midnight_oil

    cost = 5
    prerequisite = { focus = EEE_burning_midnight_oil }
    mutually_exclusive = { focus = EEE_basking_in_moonlight }
    available = {
        custom_trigger_tooltip = {
            tooltip = EEE_bossy_boots_tt
            check_variable = { EEE_stellar_election_influence > 69.99 }
        }
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_eclipsing_moon"
        #Trait here
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_anti_corruption_campaigns
    x = 0
    y = 2
    relative_position_id = EEE_petitioning_tribes

    cost = 3
    prerequisite = {
        focus = EEE_debt_for_production
        focus = EEE_promissory_military_investments
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_anti_corruption_campaigns"
        ###Event of citizens reacting to another Moonspeaker being arrested on charges of corruption, as they were receiving instructions from the mysterious Conductor, and how it seems many of them look to be in the pocket of whoever it is
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_stability_ensured
    x = 1
    y = 1
    relative_position_id = EEE_promissory_military_investments

    cost = 3
    prerequisite = {
        focus = EEE_promissory_military_investments
        focus = EEE_meeting_enemy
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_stability_ensured"
        ###Stellar makes a short speech about how despite the string of murders and disappearances, she is going to make sure that the thestrals stay safe, and that whoever is perpetrating them, either an organization or an individual, will be brought to justice
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_earmark_civilian_funds
    x = 0
    y = 1
    relative_position_id = EEE_meeting_enemy

    cost = 2
    prerequisite = { focus = EEE_meeting_enemy }
    mutually_exclusive = { focus = EEE_dogear_military_funds }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_earmark_civilian_funds"
        one_random_industrial_complex = yes
        #Increase Red Dusk influence by 10
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_dogear_military_funds
    x = 2
    y = 1
    relative_position_id = EEE_meeting_enemy

    cost = 2
    prerequisite = { focus = EEE_meeting_enemy }
    mutually_exclusive = { focus = EEE_earmark_civilian_funds }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_dogear_military_funds"
        one_random_arms_factory = yes
        30 = {
            add_resource = {
                type = steel
                amount = 4
            }
        }
        #Increase Tranquil influence by 7.5
    }
}

shared_focus = {
    id = EEE_open_conductor_case
    x = 1
    y = 1
    relative_position_id = EEE_anti_corruption_campaigns

    cost = 3
    prerequisite = { focus = EEE_anti_corruption_campaigns }
    prerequisite = { focus = EEE_stability_ensured }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_open_conductor_case"
        ###Stellar finds a mysterious note from an unnamed pony pointing to an ancient cult called the Ezmetzli being behind what's going on with the murders, and that there is some evidence to be found in a random house, which contains the remains of the pony that Nocturne dealt with at the start; Stellar opens a case file to begin searching
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_equal_plans
    x = 1
    y = 1
    relative_position_id = EEE_earmark_civilian_funds

    cost = 3
    prerequisite = {
        focus = EEE_earmark_civilian_funds
        focus = EEE_dogear_military_funds
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_equal_plans"
        ###Event going over the two main opponents' economic proposal, with also the option of sticking with Stellar Note's; Red's gives 2 civs and 4 steel in Ayacachtli only, Tranquil gives a civ and a mil and moves to partial mobilization, Stellar adds two building slots and moves to partial mobilization
        ###Red Dusk and Tranquil gain 10 influence from either decision, Stellar 5
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_fitting_tribute
    x = 2
    y = 1
    relative_position_id = EEE_open_conductor_case

    cost = 1
    prerequisite = { focus = EEE_open_conductor_case }
    prerequisite = { focus = EEE_equal_plans }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_fitting_tribute"
        ###Stellar Note, nervous and worried the Moonspeakers won't be chosen, prays at the temple in Ayacachtli, with her friend/family/lover from earlier; she isn't very religious, and feels awkward praying to Luna, but still petitions for support regardless
    }

    ai_will_do = {
        factor = 640
    }
}

###Stellar/Gushing/Nocturne starting path###
shared_focus = {
    id = EEE_second_chances
    x = 0
    y = 1
    relative_position_id = EEE_fitting_tribute

    cost = 1
    prerequisite = { focus = EEE_fitting_tribute }
    allow_branch = { has_country_flag = EEE_status_quo }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_second_chances"
        ###Results of the election come into the Moonspeakers, and Stellar is elated that she is kept in charge, which leads to a small celebration; she may or may not thank Luna, although she still keeps her secularist mindset; alternate version if Gushing supports will include Gushing providing the news, and promising to be in touch for more help
    }

    ai_will_do = {
        factor = 640
    }
}

shared_focus = {
    id = EEE_reciprocate_in_kind
    x = -4
    y = 1
    relative_position_id = EEE_second_chances

    cost = 2
    prerequisite = { focus = EEE_second_chances }
    mutually_exclusive = { focus = EEE_eventual_coexistence }
    available = {
        if = {
            limit = {
                has_country_flag = EEE_custodien_guidance
            }
            NOT = { has_country_flag = EEE_custodien_guidance }
        }
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_reciprocate_in_kind"
        ###Event of Waning arguing in front of the Moonspeakers for wiping out the Neighua or the like, and getting pushback from Nocturne, but ultimately convincing through fear that the Neighua must be stopped, and otherwise getting Nocturne out-voted
    }

    ai_will_do = {
        factor = 20
    }
}

shared_focus = {
    id = EEE_eventual_coexistence
    x = -2
    y = 1
    relative_position_id = EEE_second_chances

    cost = 2
    prerequisite = { focus = EEE_second_chances }
    mutually_exclusive = { focus = EEE_reciprocate_in_kind }
    available = {
        if = {
            limit = {
                has_country_flag = EEE_ezmetzli_coup
            }
            NOT = { has_country_flag = EEE_ezmetzli_coup }
        }
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_eventual_coexistence"
        ###Event of Nocturne arguing in front of the Moonspeakers (like Waning), for finding a way to coexist with the Neighua, since there are plenty that could be convinced to not hate thestrals, and the peninsula would be all the better for it, as they could work together in unity; it gets pushback from Waning, but Nocturne explains how it could work, and she's out-voted
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_approach_reasonable
    x = 1
    y = 1
    relative_position_id = EEE_second_chances

    cost = 2
    prerequisite = { focus = EEE_second_chances }
    available = {
        if = {
            limit = {
                has_country_flag = EEE_ezmetzli_coup
            }
            NOT = { has_country_flag = EEE_ezmetzli_coup }
        }
    }
    on_uncomplete = {
        clr_country_flag = EEE_milkwater_thestral
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_approach_reasonable"
        ###Possibly event of Stellar speaking with Nocturne, asking her to help as best she can, as much as she doesn't approve all methods, since she is a thestral like the others; she has combat experience, Stellar can tell, and she could aid so much with defending thestrals; Nocturne is hesistant but eventually relents
        set_country_flag = EEE_milkwater_thestral
        custom_effect_tooltip = nocturne_vision_stats_tp
    }

    ai_will_do = {
        factor = 20
    }
}

shared_focus = {
    id = EEE_occupying_occupants
    x = -1
    y = 1
    relative_position_id = EEE_reciprocate_in_kind
    dynamic = yes

    cost = 2
    prerequisite = { focus = EEE_reciprocate_in_kind }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_occupying_occupants"
        if = {
            limit = {
                has_country_flag = EEE_ezmetzli_coup
            }
            ###Event of Gushing Pulse going through the Ezmetzli, to have them all partake in bloodletting prisoners, to teach them all how to best extract pain and blood from sacrifices
        }
        else = {
            ###Event of Waning training some of the militia in Ayacachtli on how to be merciless with enforcing laws and of order, specifically for the Neighua, which perturbs some of the thestrals, but they obey regardless
        }
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_raid_unclean
    x = 1
    y = 1
    relative_position_id = EEE_reciprocate_in_kind

    cost = 2
    prerequisite = {
        focus = EEE_reciprocate_in_kind
        focus = EEE_eventual_coexistence
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_raid_unclean"
        ###Event of raiding an abandoned COBSOC cache and garrison, which is already being overrun by the jungle, and some of them barely make it out because of some traps left behind or the like
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_council_benefits
    x = 0
    y = 2
    relative_position_id = EEE_second_chances

    cost = 2
    prerequisite = { focus = EEE_second_chances }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_council_benefits"
        ###Potentially event of Stellar getting into an argument with a couple of the councilors to relinquish some of their wealth, as it will be used to help the thestrals rebuild, which leads to tension before she manages to overrule them
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_tribal_feud
    x = 2
    y = 2
    relative_position_id = EEE_second_chances

    cost = 2
    prerequisite = { focus = EEE_second_chances }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_tribal_feud"
        ###Follow a thestral in their new job of handling tribal feuds, and they are currently facilitating talks between two different members of tribes that live near to each other, and tries to act like it's some therapy session, but eventually gets involved in the argument, too, which accomplishes little
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_jungle_help
    x = 0
    y = 1
    relative_position_id = EEE_occupying_occupants
    dynamic = yes

    cost = 3
    prerequisite = { focus = EEE_occupying_occupants }
    available = {
        if = {
            limit = {
                NOT = { has_country_flag = EEE_ezmetzli_coup }
            }
            has_country_flag = EEE_incredibly_polite_offer
        }
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_jungle_help"
        if = {
            limit = {
                NOT = { has_country_flag = EEE_ezmetzli_coup}
            }
            ###Event of Gushing meeting with Stellar, as she promised, and giving Stellar some advice on what she needs to do to help keep the realm stable, and offering her own services, as well as that of her tribe, which Stellar cautiously accepts, since she could use all the help she could get
        }
        else = {
            ###Event of Gushing savoring some of the minor members of the Moonspeaker council, doing all she can to extend their suffering by wounding them, piercing them, toying with them, and being almost seductive in her actions, before finally killing them and perhaps sipping on a bit of their blood herself
        }
    }

    ai_will_do = {
        factor = 40
    }
}


shared_focus = {
    id = EEE_warrior_council
    x = 0
    y = 1
    relative_position_id = EEE_raid_unclean

    cost = 3
    prerequisite = { focus = EEE_raid_unclean }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_warrior_council"
        ###Maybe short event of the Moonspeakers being constantly inerrupted in one of their meetings for the impending war for the South-East by their militia captains, who are not at all deferential to their authority, and otherwise cause tension
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_voluntary_coaching
    x = 1
    y = 2
    relative_position_id = EEE_eventual_coexistence

    cost = 2
    prerequisite = { focus = EEE_eventual_coexistence }
    available = {
        custom_trigger_tooltip = {
            tooltip = EEE_tick_tock_team_tt
            NOT = { has_country_flag = EEE_team_management }
        }
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_voluntary_coaching"
        ###Maybe short event of some thestral citizens trying to bridge some gaps with the Neighua along the border and being attacked, which they retreat from
        add_manpower = 12500
        add_stability = 0.02
        add_war_support = -0.02
    }

    ai_will_do = {
        factor = 20
    }
}

shared_focus = {
    id = EEE_moon_bathing
    x = 1
    y = 1
    relative_position_id = EEE_council_benefits

    cost = 2
    prerequisite = { focus = EEE_council_benefits }
    prerequisite = { focus = EEE_tribal_feud }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_moon_bathing"
        30 = {
            add_extra_state_shared_building_slots = 2
            add_building_construction = {
                type = industrial_complex
                level = 2
                instant_build = yes
            }
        }
        add_stability = -0.1
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_buggy_investments
    x = 1
    y = 1
    relative_position_id = EEE_tribal_feud

    cost = 2
    prerequisite = { focus = EEE_tribal_feud }
    available = {
        NID = {
            exists = yes
            is_subject = no
        }
        custom_trigger_tooltip = {
            tooltip = EEE_tick_tock_team_tt
            NOT = { has_country_flag = EEE_team_management }
        }
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_buggy_investments"
        ###Maybe event of Stellar being persuaded to work with NID, or possibly hosting a NID representative and showing some of the things they can offer in exchange for guns; triggers short event asking for guns in NID, who can agree or disagree
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_night_warrior_blade
    x = -3
    y = 1
    relative_position_id = EEE_moon_bathing

    cost = 3
    prerequisite = { focus = EEE_warrior_council }
    prerequisite = { focus = EEE_moon_bathing }
    mutually_exclusive = { focus = EEE_economically_overcome }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_night_warrior_blade"
        ###Event of Waning training some of the thestral militias to be more aggressive and take advantage of their natural abilities and night vision to eke out victory, with examples to be done on either each other or on captured Neighua, provided by Gushing, which perturbs the recruits
        ###Will increase Gushing's influence on the council
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_economically_overcome
    x = -1
    y = 1
    relative_position_id = EEE_buggy_investments

    cost = 3
    prerequisite ={
        focus = EEE_moon_bathing
        focus = EEE_buggy_investments
    }
    mutually_exclusive = { focus = EEE_night_warrior_blade }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_economically_overcome"
        ###Event of a thestral worker in either a factory or workshop being tasked far beyond his capabilities, as he has already worked extensively, into the morning hours, and a mistake he makes nearly costs the entire building, which gets him in trouble instead of being sent home to rest
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_throwing_hooves
    x = 2
    y = 1
    relative_position_id = EEE_night_warrior_blade

    cost = 2
    prerequisite = {
        focus = EEE_night_warrior_blade
        focus = EEE_economically_overcome
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_throwing_hooves"
        ###Event of Stellar Note meeting with the different captains and generals of the militia to discuss final strategy, and she is nervous about the how she will be possibly condemning many thestrals to die just to ensure their survival, which she finds detestable but necessary as there's no other way to finally be safe from the Neighua
        #Would be where, if Gushing has enough influence (if still going with that, otherwise would be just guaranteed), would launch her coup; event would be different, instead being Waning approaching Stellar, congratulating her on preparing the nation, and then killing all those around her, before claiming that her maestro wishes to thank Stellar personally, and knocks her out
        create_wargoal = {
            target = NEI
            type = annex_everything
        }
    }

    ai_will_do = {
        factor = 320
    }
}

shared_focus = {
    id = EEE_bitter_friend
    x = 0
    y = 1
    relative_position_id = EEE_throwing_hooves

    cost = 2
    prerequisite = { focus = EEE_throwing_hooves }
    available = {
        owns_state = 16
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_bitter_friend"
        ###Event of Stellar meeting with the leader of NEI to force a treaty with them, since there is still a bit of fighting in Tenochtitlan, and after a short conflict with some of the elite guards provided by Waning, she manages to break through and get them to surrender, for good
    }

    ai_will_do = {
        factor = 320
    }
}

shared_focus = {
    id = EEE_righting_wrongs
    x = -2
    y = 1
    relative_position_id = EEE_bitter_friend

    cost = 3
    prerequisite = { focus = EEE_bitter_friend }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_righting_wrongs"
        ###Event of some of the Neighua leaders and advisors being tried by the Moonspeakers for their crimes, and Stellar hesitantly convicts them and sentences them to death for their intended genocide of thestrals, which she values far more than anything else
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_lost_blood
    x = 2
    y = 1
    relative_position_id = EEE_bitter_friend

    cost = 2
    prerequisite = { focus = EEE_bitter_friend }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_lost_blood"
        ###Maybe short event of a thestral family that appeared in one of the events for the Neighua rebellion returning home and seeing how much their home has changed from the Neighua living there, and getting into a conflict with the ones that claimed it, before ultimatley having the militia that came to guard them kick the Neighua out; they are happy the entire time, as a dissonance to the acts in the event
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_less_evil
    x = -2
    y = 1
    relative_position_id = EEE_righting_wrongs

    cost = 3
    prerequisite = { focus = EEE_righting_wrongs }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_less_evil"
        ###Event of a Neighua tribe member being put through some of the educational ringers that have been set up by the thestrals to try and get some of them to acclimate to the idea of thestrals living among them, and how they are the same as regular ponies, but it only ends up making them hate thestrals more, instead
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_erase_past
    x = 0
    y = 1
    relative_position_id = EEE_righting_wrongs

    cost = 3
    prerequisite = { focus = EEE_righting_wrongs }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_erase_past"
        ###Event of some Neighua who want to visit some of their temples being barred, as the temple is currently being cleared of any teachings that their own priests had and otherwise completely destroying their history, as if they believe that their ancestors were right in hating thestrals, they could to repeat the same acts
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_danger_stellar_note
    x = 0
    y = 2
    relative_position_id = EEE_bitter_friend

    cost = 2
    prerequisite = { focus = EEE_bitter_friend }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_danger_stellar_note"
        ###Event of Stellar meeting with her generals, who have penned together another invasion plan, this time of the South-East, as there are those within it that are all preparing to go to war with each other, and them, to come out as the only successor of Cobalt Hoof, and she tries to protest against it, but is eventually convinced that it is the only way to survive and protect her thestrals
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_second_ayacachtli
    x = -2
    y = 1
    relative_position_id = EEE_less_evil

    cost = 4
    prerequisite = { focus = EEE_less_evil }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_second_ayacachtli"
        ###Maybe short event of some thestral workers arriving in Tenochtitlan and assessing their opportunities, and being greeted with hostility by the occupied Neighua, which almost makes them leave after nearly being assaulted by a couple near the entrance to the city, before they are stopped by the militia guarding the city
        16 = {
            add_extra_state_shared_building_slots = 2
            #Modifier here
        }
    }

    ai_will_do = {
        factor = 20
    }
}

shared_focus = {
    id = EEE_reintroduce_bats
    x = 1
    y = 1
    relative_position_id = EEE_less_evil

    cost = 3
    prerequisite = { focus = EEE_less_evil }
    prerequisite = { focus = EEE_erase_past }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_reintroduce_bats"
        ###Coring alert here; maybe change to flat-out getting the cores
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_future_knowledge
    x = -1
    y = 1
    relative_position_id = EEE_danger_stellar_note

    cost = 3
    prerequisite = { focus = EEE_erase_past }
    prerequisite = { focus = EEE_danger_stellar_note }
    mutually_exclusive = { focus = EEE_victorious_memories }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_future_knowledge"
        #Idea here; not lost when moving to next stage, same with other focus; all others are
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_victorious_memories
    x = 0
    y = 2
    relative_position_id = EEE_lost_blood

    cost = 3
    prerequisite = { focus = EEE_lost_blood }
    mutually_exclusive = { focus = EEE_future_knowledge }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_victorious_memories"
        #Idea here
    }

    ai_will_do = { factor = 40 }
}

shared_focus = {
    id = EEE_southern_protection
    x = -1
    y = 1
    relative_position_id = EEE_future_knowledge

    cost = 1.45
    prerequisite = {
        focus = EEE_future_knowledge
        focus = EEE_victorious_memories
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_southern_protection"
        #Make conditions for these
        create_wargoal = {
            target = STB
            type = annex_everything
        }
        create_wargoal = {
            target = NID
            type = annex_everything
        }
        create_wargoal = {
            target = NRP
            type = annex_everything
        }
    }

    ai_will_do = {
        factor = 40
        modifier = {
            has_war = yes
            factor = 0.1
        }
    }
}

shared_focus = {
    id = EEE_northern_push
    x = 1
    y = 1
    relative_position_id = EEE_future_knowledge

    cost = 1.45
    prerequisite = {
        focus = EEE_future_knowledge
        focus = EEE_victorious_memories
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_northern_push"
        create_wargoal = {
            target = BAL
            type = annex_everything
        }
        create_wargoal = {
            target = MTS
            type = annex_everything
        }
    }

    ai_will_do = {
        factor = 40
        modifier = {
            has_war = yes
            factor = 0.1
        }
    }
}

shared_focus = {
    id = EEE_reclaiming_chains
    x = 1
    y = 1
    relative_position_id = EEE_victorious_memories

    cost = 1.45
    prerequisite = {
        focus = EEE_future_knowledge
        focus = EEE_victorious_memories
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_reclaiming_chains"
        create_wargoal = {
            target = SPF
            type = annex_everything
        }
        create_wargoal = {
            target = CLI
            type = annex_everything
        }
    }

    ai_will_do = {
        factor = 40
        modifier = {
            has_war = yes
            factor = 0.1
        }
    }
}

###Ezmetzli appearance hidden focuses###
shared_focus = {
    id = EEE_monster_realized
    x = 0
    y = 1
    relative_position_id = EEE_jungle_help

    cost = 1
    prerequisite = { focus = EEE_jungle_help }
    available = {
        has_country_leader = { character = EEE_Gushing_Pulse ruling_only = yes }
    }
    #allow_branch = {
    #   has_country_flag = EEE_ezmetzli_coup
    #}

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_monster_realized"
        ###Gushing gleefully meets a slwoly awakening Stellar, who is currently strapped onto a sacrifical bed on the top of one of the Ezmetzli's temples, deep in the jungle, and Gushing plays with her, regarding how she was foolish to trust such a pony like Gushing, and Gushing eventually ends Stellar entirely, after savoring making her bleed first; Stellar is resigned, confused, but ultimately defiant
    }

    ai_will_do = {
        factor = 640
    }
}

shared_focus = {
    id = EEE_rite_of_wrath
    x = -2
    y = 1
    relative_position_id = EEE_monster_realized

    cost = 2
    prerequisite = { focus = EEE_monster_realized }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_rite_of_wrath"
        ###Possibly short event of some Ezmetzli troops being pushed to prepare for war, through gruesome rites such as leaving only one pony standing in a bloody free for all, and whoever lets their anger control them ends up being the winner
        #Idea here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_plowshare_blades
    x = 0
    y = 1
    relative_position_id = EEE_monster_realized

    cost = 3
    prerequisite = { focus = EEE_monster_realized }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_plowshare_blades"
        ###Maybe short event of innocent thestrals being forced to build new sweatshops for weapons, and the Ezmetzli being ruthless in their slave-driving
        two_random_arms_factory = yes
        two_random_arms_factory = yes
        one_random_arms_factory = yes
        #Remove 2 civ factories here
        add_stability = -0.15
        add_political_power = -50
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_rite_of_knowledge
    x = 2
    y = 1
    relative_position_id = EEE_monster_realized

    cost = 2
    prerequisite = { focus = EEE_monster_realized }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_rite_of_knowledge"
        ###Possible short event of an Ezmetzli raid on the Temple of the Moon, where they slaughter any Moonspeakers resist, so they can pillage the books and knowledge inside so they can study any and all pony groups that have been in the South-East, including Cobalt Hoof's clique, so they know exactly how to exploit them, since the Moonspeakers take meticulous records of everything in the South-East (that hasn't been erased by either cults)
        #Idea here
    }

    ai_will_do = {
        factor = 80
    }
}

#Mutually exclusive focuses here affect how much of the BoP that the two sides of the Ezmetzli have after taking the South-East
shared_focus = {
    id = EEE_blood_of_all
    x = -1
    y = 1
    relative_position_id = EEE_rite_of_wrath

    cost = 3
    prerequisite = { focus = EEE_rite_of_wrath }
    mutually_exclusive = { focus = EEE_surgical_removals }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_blood_of_all"
        ###Event of Gushing whipping her soldiers into a frenzy, promising them that they will paint the peninsula in blood, which will be a pleasing offering to the Moon Goddess, and feed the Eternal Song, which is culminated by killing another Moonspeaker to prove their superiority
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_surgical_removals
    x = 1
    y = 1
    relative_position_id = EEE_rite_of_wrath

    cost = 3
    prerequisite = { focus = EEE_rite_of_wrath }
    mutually_exclusive = { focus = EEE_blood_of_all }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_surgical_removals"
        ###Waning and Moonlight gain an audience with Gushing, who is in the middle of preparing her speech for the other event, and argue for their side of finding the right ponies on the enemy side and taking them out, to sow confusion, rather than just throwing bodies at the problem, and successfully win her over by promising that the peninsual will still drown in the blood of their enemies regardless, but with less of their own
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_follow_goddess
    x = -1
    y = 1
    relative_position_id = EEE_rite_of_knowledge

    cost = 3
    prerequisite = { focus = EEE_rite_of_knowledge }
    mutually_exclusive = { focus = EEE_follow_chosen }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_follow_goddess"
        ###Event of Waning and Moonlight hosting a grand sermon about how their cause is just and right, and that the Moon Goddess herself will bless them all for their struggles, and how if the Ezmetzli serve her without question, they will each be unstoppable warriors, and receive perhaps a smidgen of the blessings that they and Gushing have earned; they focus more on the Moon Goddess than Gushing
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_follow_chosen
    x = 1
    y = 1
    relative_position_id = EEE_rite_of_knowledge

    cost = 3
    prerequisite = { focus = EEE_rite_of_knowledge }
    mutually_exclusive = { focus = EEE_follow_goddess }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_follow_chosen"
        ###Event of Gushing forcing the Crescents to stand aside as she makes a completely off the cuff speech to the Ezmetzli, claiming how she has been directly chosen by the Moon Goddess, as hearer of the Eternal Song, to ensure its purity, and that she will ensure that every last one of them will be blessed by the Goddess, if they do what she needs them to do for bathing the whole peninsula in the blood of their enemies, and obey her whims at every turn; this irritates the Crescents
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_kill_kill_kill
    x = 0
    y = 2
    relative_position_id = EEE_plowshare_blades

    cost = 0.5
    prerequisite = { focus = EEE_plowshare_blades }
    #Maybe make it only available after making the choices for the blessings

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_kill_kill_kill"
        ###Possibly short event of Gushing promising each soldier their fill of ending the defilers of the Song, and how they will be blessed by the Moon Goddess for their service, which is applauded by all assembled before her in Ayacachtli; the moon would be coming through the one hole in the cavern, which would make her appear even more the chosen of the Moon Goddess
        declare_war_on = {
            target = NEI
            type = annex_everything
        }
        create_wargoal = {
            target = NID
            type = annex_everything
        }
        create_wargoal = {
            target = SPF
            type = annex_everything
        }
        create_wargoal = {
            target = STB
            type = annex_everything
        }
        create_wargoal = {
            target = BAL
            type = annex_everything
        }
        create_wargoal = {
            target = NRP
            type = annex_everything
        }
        create_wargoal = {
            target = MTS
            type = annex_everything
        }
    }

    ai_will_do = {
        base = 0
        modifier = {
            OR = {
                has_completed_focus = EEE_blood_of_all
                has_completed_focus = EEE_surgical_removals
            }
            add = 40
        }
        modifier = {
            OR = {
                has_completed_focus = EEE_follow_goddess
                has_completed_focus = EEE_follow_chosen
            }
            add = 40
        }
        modifier = {
            OR = {
                has_completed_focus = EEE_blood_of_all
                has_completed_focus = EEE_surgical_removals
            }
            OR = {
                has_completed_focus = EEE_follow_goddess
                has_completed_focus = EEE_follow_chosen
            }
            factor = 12
        }
    }
}

###Industry###
shared_focus = {
    id = EEE_emergency_economic_grants
    x = -21
    y = 0

    cost = 3

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_emergency_economic_grants"
        add_tech_bonus = {
            name = industry_bonus
            uses = 1
            bonus = 1
            category = industry
        }
    }

    ai_will_do = {
        factor = 80 #Temp values
    }
}

shared_focus = {
    id = EEE_connecting_tribes
    x = -11
    y = 0

    cost = 3

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_connecting_tribes"
        #Bonus here, possibly short event of the first railroad in a village making them quite curious
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_shifting_sands
    x = -1
    y = 1
    relative_position_id = EEE_emergency_economic_grants

    cost = 3
    prerequisite = { focus = EEE_emergency_economic_grants }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_shifting_sands"
        two_random_industrial_complex = yes
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_following_through
    x = 1
    y = 1
    relative_position_id = EEE_emergency_economic_grants

    cost = 3
    prerequisite = { focus = EEE_emergency_economic_grants }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_following_through"
        ###Perhaps short event of some ponies using mining reports from COBSOC to find some materials beneath the mountains in Ayacachtli, and not realizing that there are numerous dangers, such as weak walls and animals or just terrain itself, which nearly gets them killed
        37 = {
            add_resource = {
                type = tungsten
                amount = 8
            }
        }
        30 = {
            add_resource = {
                type = chromium
                amount = 4
            }
        }
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_new_jewel
    x = 4
    y = 1
    relative_position_id = EEE_emergency_economic_grants

    cost = 3
    prerequisite = { focus = EEE_emergency_economic_grants }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_new_jewel"
        ###Perhaps short event of some construction to expand Ayacachtli outside of its cavern, which leaves the thestrals working it nervous and prone to mistakes
        30 = {
            add_extra_state_shared_building_slots = 2
            #State modifier here
        }
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_peninsular_soul
    x = -4
    y = 1
    relative_position_id = EEE_connecting_tribes

    cost = 3
    prerequisite = { focus = EEE_connecting_tribes }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_peninsular_soul"
        ###Perhaps short event of some thestral visitors from elsewhere in the Peninsula, who are in awe of the culture of Ayacachtli, since they'd never visited before under COBSOC, and they may act a bit clueless because of it to the rest of the culture
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_other_mudponies
    x = -1
    y = 1
    relative_position_id = EEE_connecting_tribes

    cost = 3
    prerequisite = { focus = EEE_connecting_tribes }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_other_mudponies"
        #Bonuses here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_extracting_engineering
    x = 1
    y = 1
    relative_position_id = EEE_connecting_tribes

    cost = 3
    prerequisite = { focus = EEE_connecting_tribes }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_extracting_engineering"
        #Bonuses here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_replicating_promises
    x = 1
    y = 1
    relative_position_id = EEE_shifting_sands

    cost = 3
    prerequisite = { focus = EEE_shifting_sands }
    prerequisite = { focus = EEE_following_through }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_replicating_promises"
        ###Perhaps a short event of opening the dam and how it was still built to some of COBSOC's standards, it quickly springing a leak, while it's still being lauded
        #Dam should be for state 31
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_modern_miracle
    x = -1
    y = 1
    relative_position_id = EEE_new_jewel

    cost = 3
    prerequisite = { focus = EEE_new_jewel }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_modern_miracle"
        ###Event of opening the first, proper hospital in Ayacachtli, and how it is already being filled heavily by ponies who have been left permanently injured in the dangers of the land, or by the uprisings against COBSOC in the past
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_dedicated_industrial_sectors
    x = 1
    y = 1
    relative_position_id = EEE_new_jewel

    cost = 3
    prerequisite = { focus = EEE_new_jewel }
    prerequisite = { focus = EEE_peninsular_soul }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_dedicated_industrial_sectors"
        30 = {
            add_extra_state_shared_building_slots = 1
        }
        if = {
            limit = { has_idea = pre_industrial_society }
            swap_ideas = { add_idea = pre_industrial_society2 remove_idea = pre_industrial_society }
        }
        else_if = {
            limit = { has_idea = pre_industrial_society2 }
            swap_ideas = { add_idea = outdated_industry remove_idea = pre_industrial_society }
        }
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_correct_economic_resistance
    x = 1
    y = 1
    relative_position_id = EEE_peninsular_soul

    cost = 3
    prerequisite = { focus = EEE_peninsular_soul }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_correct_economic_resistance"
        ###Event of tension between some of the residents of Ayacachtli and owners of new factories, who are a mix of both thestrals and non-thestrals, as they believe the factories will pollute and ruin the city, and the owners try to get them to see reason, although it gets close to a proper riot before order is restored
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_floral_removal
    x = 1
    y = 1
    relative_position_id = EEE_other_mudponies

    cost = 3
    prerequisite = { focus = EEE_other_mudponies }
    prerequisite = { focus = EEE_extracting_engineering }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_floral_removal"
        ###Event following some of the ponies working for the Jungle Clearing Safety Committee, and how they observe one of the jobs to help clear the roads and railways in the jungle and finding constant faults with it, which annoys all the workers, who try to rid themselves of him
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_heart_of_jungle
    x = 0
    y = 1
    relative_position_id = EEE_dedicated_industrial_sectors

    cost = 5
    prerequisite = { focus = EEE_modern_miracle }
    prerequisite = { focus = EEE_correct_economic_resistance }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_heart_of_jungle"
        30 = {
            improve_state_category = yes
        }
        if = {
            limit = { has_idea = pre_industrial_society }
            swap_ideas = { add_idea = pre_industrial_society2 remove_idea = pre_industrial_society }
        }
        else_if = {
            limit = { has_idea = pre_industrial_society2 }
            swap_ideas = { add_idea = outdated_industry remove_idea = pre_industrial_society }
        }
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_chipping_protections
    x = 0
    y = 2
    relative_position_id = EEE_replicating_promises

    cost = 4
    prerequisite = { focus = EEE_replicating_promises }
    available = {
        NOT = { has_country_flag = EEE_recently_yoinked_tribe_land } #Country flag set by each of these focuses that reduce protections for tribal land; is set to be true for 60 days
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_chipping_protections"
        ###Perhaps short event of some tribes protesting at having factories built on land promised to them
        #Modify idea here
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_embracing_COBSOC
    x = 3
    y = 2
    relative_position_id = EEE_replicating_promises

    cost = 4
    prerequisite = { focus = EEE_replicating_promises }
    mutually_exclusive = { focus = EEE_roots_of_society }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_embracing_COBSOC"
        #Bonuses here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_roots_of_society
    x = -3
    y = 2
    relative_position_id = EEE_floral_removal

    cost = 4
    prerequisite = { focus = EEE_floral_removal }
    mutually_exclusive = { focus = EEE_embracing_COBSOC }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_roots_of_society"
        #Bonuses here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_adapting_to_world
    x = 0
    y = 2
    relative_position_id = EEE_floral_removal

    cost = 4
    prerequisite = { focus = EEE_floral_removal }
    available = {
        NOT = { has_country_flag = EEE_recently_yoinked_tribe_land }
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_adapting_to_world"
        ###Perhaps short event of negotiators from Ayacachtli meeting with some tribe delegates to strong-arm them into giving up some of their land for construction to expand the country's industry
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_minimal_compensation
    x = -1
    y = 1
    relative_position_id = EEE_embracing_COBSOC

    cost = 3
    prerequisite = { focus = EEE_embracing_COBSOC }
    available = {
        NOT = { has_country_flag = EEE_recently_yoinked_tribe_land }
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_minimal_compensation"
        ###Perhaps short event of some tribes complaining about being paid little for the land they have to give up, which are ignored by the government since they'd reduce the amount of money they'd get from taxes
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_leveraging_mountains
    x = 1
    y = 1
    relative_position_id = EEE_embracing_COBSOC

    cost = 3
    prerequisite = { focus = EEE_embracing_COBSOC }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_leveraging_mountains"
        30 = {
            add_resource = {
                type = steel
                amount = 12
            }
            add_resource = {
                type = aluminium
                amount = 6
            }
        }
        37 = {
            add_resource = {
                type = steel
                amount = 8
            }
            add_resource = {
                type = aluminium
                amount = 4
            }
        }
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_seekers_of_soil
    x = -1
    y = 1
    relative_position_id = EEE_roots_of_society

    cost = 3
    prerequisite = { focus = EEE_roots_of_society }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_seekers_of_soil"
        #Idea here
        ###Maybe short event of how thestrals have been trained to find and scope out the nearest resources through their magical senses alone
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_aggressive_negotiation
    x = 1
    y = 1
    relative_position_id = EEE_roots_of_society

    cost = 4
    prerequisite = { focus = EEE_roots_of_society }
    available = {
        NOT = { has_country_flag = EEE_recently_yoinked_tribe_land }
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_aggressive_negotiation"
        ###Potentially event of weaving and worming around negotiations and written agreements with the tribes to get them to agree to more land being given up for factories
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_industrial_charity
    x = 0
    y = 2
    relative_position_id = EEE_chipping_protections

    cost = 4
    prerequisite = { focus = EEE_chipping_protections }
    prerequisite = { focus = EEE_minimal_compensation }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_industrial_charity"
        ###Possibly short event of an accountant for a factory trying to worm out of giving more taxes than necessary, and finding it extremely difficult, since there are so many things that they must donate to to support the poor
        decrease_poverty = yes
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_immediate_responses
    x = 1
    y = 1
    relative_position_id = EEE_minimal_compensation

    cost = 3
    prerequisite = { focus = EEE_minimal_compensation }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_immediate_responses"
        two_random_industrial_complex = yes
        one_random_industrial_complex = yes
        one_random_arms_factory = yes
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_book_situation
    x = 0
    y = 3
    relative_position_id = EEE_heart_of_jungle

    cost = 4
    prerequisite = {
        focus = EEE_embracing_COBSOC
        focus = EEE_roots_of_society
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_book_situation"
        ###Event of some Moonspeakers teaching foals in a classroom, along with many adults, and being bombarded with sometimes rather inane or downright silly questions, while trying to teach them all to read more effectively, which frustrates a number of them
        decrease_illiteracy = yes
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_natural_exploitation
    x = -1
    y = 1
    relative_position_id = EEE_aggressive_negotiation

    cost = 3
    prerequisite = { focus = EEE_aggressive_negotiation }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_natural_exploitation"
        ###Maybe short event of some miners or the like reluctantly having to split their earnings from selling resources extracted from tribal land with the tribes themselves
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_artisanal_wages
    x = 0
    y = 2
    relative_position_id = EEE_adapting_to_world

    cost = 4
    prerequisite = { focus = EEE_adapting_to_world }
    prerequisite = { focus = EEE_aggressive_negotiation }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_artisanal_wages"
        ###Perhaps an event of an artisan trying to refuse being given any welfare money, as she sees it as an insult to her craft, which bemuses the government ponies, since she is having difficulty selling any of her wares, which are very niche wing shields for jungle trackers and trailblazers
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_strong_offenses
    x = -1
    y = 1
    relative_position_id = EEE_immediate_responses

    cost = 4
    prerequisite = { focus = EEE_immediate_responses }
    mutually_exclusive = { focus = EEE_stronger_defenses }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_strong_offenses"
        #Bonus here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_stronger_defenses
    x = 0
    y = 2
    relative_position_id = EEE_leveraging_mountains

    cost = 4
    prerequisite = { focus = EEE_leveraging_mountains }
    mutually_exclusive = { focus = EEE_strong_offenses }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_stronger_defenses"
        #Bonus here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_itemized_industrial_parks
    x = 0
    y = 2
    relative_position_id = EEE_seekers_of_soil

    cost = 4
    prerequisite = { focus = EEE_seekers_of_soil }
    mutually_exclusive = { focus = EEE_strategic_positioning }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_itemized_industrial_parks"
        #Bonus here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_strategic_positioning
    x = 1
    y = 1
    relative_position_id = EEE_natural_exploitation

    cost = 4
    prerequisite = { focus = EEE_natural_exploitation }
    mutually_exclusive = { focus = EEE_itemized_industrial_parks }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_strategic_positioning"
        #Bonus here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_remote_suburbs
    x = -1
    y = 1
    relative_position_id = EEE_strong_offenses

    cost = 3
    prerequisite = {
        focus = EEE_strong_offenses
        focus = EEE_stronger_defenses
    }
    available = { #Will probably need custom tooltip
        check_variable = { EEE_reduced_tribe_land_attempts = 3 }
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_remote_suburbs"
        ###Potentially short event of some thestrals traveling in from distant villages to Ayacachtli for the week, since they are so far from home that there is little point traveling back, which makes them a bit annoyed, although they have to go, to earn enough to live
        #Effect here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_black_and_blue
    x = 1
    y = 1
    relative_position_id = EEE_strong_offenses

    cost = 3
    prerequisite = {
        focus = EEE_strong_offenses
        focus = EEE_stronger_defenses
    }
    available = {
        check_variable = { EEE_reduced_tribe_land_attempts = 3 }
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_black_and_blue"
        #Effect here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_thestral_wisdom
    x = 0
    y = 2
    relative_position_id = EEE_book_situation

    cost = 5
    prerequisite = { focus = EEE_book_situation }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_thestral_wisdom"
        ###Possibly event of some of the first university classes being held, and how it seems all great and new, until homework is assigned
        Improve_Science_Base = yes
        if = {
            limit = { amount_research_slots < 5 }
            add_research_slot = 1
        }
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_shock_girders
    x = 1
    y = 1
    relative_position_id = EEE_itemized_industrial_parks

    cost = 3
    prerequisite = {
        focus = EEE_itemized_industrial_parks
        focus = EEE_strategic_positioning
    }
    available = {
        check_variable = { EEE_reduced_tribe_land_attempts = 3 }
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_shock_girders"
        #Bonus here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_hubs_of_jungle
    x = 1
    y = 1
    relative_position_id = EEE_strategic_positioning

    cost = 3
    prerequisite = {
        focus = EEE_itemized_industrial_parks
        focus = EEE_strategic_positioning
    }
    available = {
        check_variable = { EEE_reduced_tribe_land_attempts = 3 }
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_hubs_of_jungle"
        ###Event of transport technicians traveling to Ayacachtli for delivering the goods manufactured in other villages to help keep the industrial machine moving, and them dealing with some of the inspections and even customs they have to go through
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_pony_economy
    x = 1
    y = 1
    relative_position_id = EEE_remote_suburbs

    cost = 5
    prerequisite = { focus = EEE_remote_suburbs }
    prerequisite = { focus = EEE_black_and_blue }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_pony_economy"
        #Bonus here
        increase_society_development = yes
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_scouting_villages
    x = -1
    y = 1
    relative_position_id = EEE_thestral_wisdom

    cost = 4
    prerequisite = { focus = EEE_thestral_wisdom }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_scouting_villages"
        ###Possibly event on the plans for reducing illiteracy in the villages that are remote from the cities and towns, or Moonspeakers traveling to and fro, near and far, through the villages to teach, which earns them gratitude, but also slowly wears them down as they can never keep constant relationships up
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_uncharted_heights
    x = 1
    y = 1
    relative_position_id = EEE_thestral_wisdom

    cost = 4
    prerequisite = { focus = EEE_thestral_wisdom }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_uncharted_heights"
        ###Event of some new thestral scientists working on how to enhance either some of their senses aside from sight, or perhaps their wingpower to match pegasi, which involves numerous, perhaps questionable research decisions, which worries the test subjects before they're subjected to the techniques to supposedly improve their bodies
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_thestral_economy
    x = 1
    y = 1
    relative_position_id = EEE_shock_girders

    cost = 5
    prerequisite = { focus = EEE_shock_girders }
    prerequisite = { focus = EEE_hubs_of_jungle }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_thestral_economy"
        #Bonus here
        increase_society_development = yes
    }
}

###Army and Air###
shared_focus = {
    id = EEE_ayacachtli_air_college
    x = 20
    y = 0

    cost = 3

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_ayacachtli_air_college"
        #Event of choosing what the college should focus on for its doctrine
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_step_one_infrastructure
    x = -1
    y = 1
    relative_position_id = EEE_ayacachtli_air_college

    cost = 3
    prerequisite = { focus = EEE_ayacachtli_air_college }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_step_one_infrastructure"
        ###Short event that since there are effectively no air bases, they need to be built, and choose where; option will give +2 air bases, +1 inf, +1 aa in the chosen state
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_foreign_flights
    x = 2
    y = 1
    relative_position_id = EEE_ayacachtli_air_college

    cost = 3
    prerequisite = {
        focus = EEE_ayacachtli_air_college
        focus = EEE_embracing_air
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_foreign_flights"
        #Tech bonus here
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_cannibalize_COBSOC
    x = -2
    y = 1
    relative_position_id = EEE_step_one_infrastructure

    cost = 4
    prerequisite = { focus = EEE_step_one_infrastructure }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_cannibalize_COBSOC"
        ###Event of some thestrals breaking into an abandoned COBSOC air base, where they had kept a number of fighters and planes, and making off with them after dealing with some of the environmental hazards that have cropped up, since it's now unmaintained; possibly some old guard animals, too
    }

    ai_will_do = {
        factor = 40
        #Increase if low on planes
    }
}

shared_focus = {
    id = EEE_converting_canopies
    x = 1
    y = 1
    relative_position_id = EEE_step_one_infrastructure

    cost = 3
    prerequisite = { focus = EEE_step_one_infrastructure }
    prerequisite = { focus = EEE_foreign_flights }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_converting_canopies"
        #Tech bonus here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_sleeping_strikes
    x = -1
    y = 2
    relative_position_id = EEE_step_one_infrastructure

    cost = 4
    prerequisite = { focus = EEE_step_one_infrastructure }
    mutually_exclusive = { focus = EEE_extensions_of_wing }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_sleeping_strikes"
        ###Maybe short event of some proposals for how to best use natural night vision of thestrals to assist with bombing from the air and that it will keep things invisible to most ponies since they're sleeping and will never know until too late
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_extensions_of_wing
    x = 0
    y = 1
    relative_position_id = EEE_converting_canopies

    cost = 4
    prerequisite = { focus = EEE_converting_canopies }
    mutually_exclusive = { focus = EEE_sleeping_strikes }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_extensions_of_wing"
        ###Maybe short event talking about the agility and reaction times of thestrals, and that that means they should become the best aces and fighter pilots the world over
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_natural_nocturnals
    x = -4
    y = 1
    relative_position_id = EEE_sleeping_strikes

    cost = 3
    prerequisite = { focus = EEE_sleeping_strikes }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_natural_nocturnals"
        ###Event of some night-time practice bombing runs from thestrals as they use some of the jungle as targets, and may get too close to a Neigha village or the like
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = { #Loc should talk about making completely independent bomber aircraft
    id = EEE_singular_flocks
    x = -2
    y = 1
    relative_position_id = EEE_sleeping_strikes

    cost = 3
    prerequisite = { focus = EEE_sleeping_strikes }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_singular_flocks"
        #Bonus here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_unfurl_wings
    x = 1
    y = 1
    relative_position_id = EEE_sleeping_strikes

    cost = 5
    prerequisite = {
        focus = EEE_sleeping_strikes
        focus = EEE_extensions_of_wing
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_unfurl_wings"
        ###Event of expanding the college to allow more training, and that there will soon be aces in every class, as that is the crop that's being selected to be used for fighter pilot students
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_abusing_abilities
    x = 2
    y = 1
    relative_position_id = EEE_extensions_of_wing

    cost = 3
    prerequisite = { focus = EEE_extensions_of_wing }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_abusing_abilities"
        ###Event explaining how thestrals have far better reaction times than regular ponies and that they can use it to run circles around other pilots
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_flexible_skies
    x = 4
    y = 1
    relative_position_id = EEE_extensions_of_wing

    cost = 3
    prerequisite = { focus = EEE_extensions_of_wing }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_flexible_skies"
        #Bonus here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = { #Loc talks about having small observation areas underneath the bomber to be able to spot targets better
    id = EEE_aerial_spotters
    x = -2
    y = 1
    relative_position_id = EEE_singular_flocks

    cost = 3
    prerequisite = { focus = EEE_singular_flocks }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_aerial_spotters"
        #Idea here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_bigger_booms
    x = 0
    y = 1
    relative_position_id = EEE_singular_flocks

    cost = 3
    prerequisite = { focus = EEE_singular_flocks }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_bigger_booms"
        #Bonus here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_continental_outreach
    x = 2
    y = 1
    relative_position_id = EEE_singular_flocks

    cost = 3
    prerequisite = { focus = EEE_singular_flocks }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_continental_outreach"
        #Bonus here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_speeding_bullets
    x = -2
    y = 1
    relative_position_id = EEE_abusing_abilities

    cost = 3
    prerequisite = { focus = EEE_abusing_abilities }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_speeding_bullets"
        #Bonus here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_triple_checklists
    x = 0
    y = 1
    relative_position_id = EEE_abusing_abilities

    cost = 3
    prerequisite = { focus = EEE_abusing_abilities }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_triple_checklists"
        ###Event of an engineer going through checklists for the third or fourth time and growing rather tired of it, even though he's being watched over by his supervisor and he winges on it the entire time
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_interchangeable_conquests
    x = 0
    y = 1
    relative_position_id = EEE_flexible_skies

    cost = 3
    prerequisite = { focus = EEE_flexible_skies }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_interchangeable_conquests"
        #Bonus here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_bats_of_sky
    x = 0
    y = 2
    relative_position_id = EEE_unfurl_wings

    cost = 3
    prerequisite = { focus = EEE_unfurl_wings }
    prerequisite = {
        focus = EEE_aerial_spotters
        focus = EEE_bigger_booms
        focus = EEE_continental_outreach
        focus = EEE_speeding_bullets
        focus = EEE_triple_checklists
        focus = EEE_interchangeable_conquests
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_bats_of_sky"
        #Idea here
    }

    ai_will_do = {
        factor = 320
    }
}

shared_focus = {
    id = EEE_embracing_air
    x = 24
    y = 0

    cost = 3
    mutually_exclusive = { focus = EEE_embracing_jungle }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_embracing_air"
        air_experience = 20
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_reaching_clouds
    x = 1
    y = 1
    relative_position_id = EEE_embracing_air

    cost = 3
    prerequisite = { focus = EEE_embracing_air }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_reaching_clouds"
        #Gain ace and airbases here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_what_parachutes
    x = 4
    y = 1
    relative_position_id = EEE_embracing_air

    cost = 3
    prerequisite = {
        focus = EEE_embracing_air
        focus = EEE_embracing_jungle
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_what_parachutes"
        #Tech bonus here
        ###Possibly an event of training paratroopers for EEE, and how they simply use their wings so they can scream down (perhaps literally) to the surface before breaking out of a dive
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_batty_integrations
    x = 0
    y = 1
    relative_position_id = EEE_reaching_clouds

    cost = 3
    prerequisite = { focus = EEE_reaching_clouds }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_batty_integrations"
        #Bonus here
        ###Possibly short event of a thestral willingly being analyzed to be used for tests on radar, which does leave her a bit worse for wear, but ultimately better for the nation
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_streamlined_barriers
    x = -1
    y = 1
    relative_position_id = EEE_what_parachutes

    cost = 3
    prerequisite = { focus = EEE_reaching_clouds }
    prerequisite = { focus = EEE_what_parachutes }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_streamlined_barriers"
        #Idea here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_dedicated_college_courses
    x = 2
    y = 1
    relative_position_id = EEE_what_parachutes

    cost = 3
    prerequisite = { focus = EEE_what_parachutes }
    mutually_exclusive = { focus = EEE_badlands_survival_training }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_dedicated_college_courses"
        #Idea here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_proper_support_personnel
    x = 0
    y = 1
    relative_position_id = EEE_dedicated_college_courses

    cost = 3
    prerequisite = { focus = EEE_dedicated_college_courses }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_proper_support_personnel"
        #Bonuses here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = { #This and the next will probably need custom techs for the stats and a fake spirit
    id = EEE_death_from_above
    x = -1
    y = 1
    relative_position_id = EEE_proper_support_personnel

    cost = 5
    prerequisite = {
        focus = EEE_proper_support_personnel
        focus = EEE_refined_prototypes
    }
    mutually_exclusive = {
        focus = EEE_death_from_within
        focus = EEE_death_from_the_sea
    }
    available = {
        has_country_flag = EEE_unified_southeast
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_death_from_above"
        ###Event of thestrals going in on a commando raid from the sky against one of the hold-outs of COBSOC or some other pre-unification nation that has been a pain thus far and overwhelming with paradrops
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_death_from_within
    x = 1
    y = 1
    relative_position_id = EEE_proper_support_personnel

    cost = 5
    prerequisite = {
        focus = EEE_proper_support_personnel
        focus = EEE_refined_prototypes
    }
    mutually_exclusive = {
        focus = EEE_death_from_above
        focus = EEE_death_from_the_sea
    }
    available = {
        has_country_flag = EEE_unified_southeast
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_death_from_within"
        ###Event of thestral commandos going through the jungles in the deep South-East, near some of the former Neighua territories, to fight against an encampent of Neighua who refused to stand down after being beaten, and with their training, they easily overcome the camp
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_opportune_defenses
    x = 7
    y = 1
    relative_position_id = EEE_embracing_air

    cost = 3
    prerequisite = {
        focus = EEE_embracing_air
        focus = EEE_embracing_jungle
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_opportune_defenses"
        #Idea here
        ###Maybe short event of trying to work around the jungle for defenses, and how it is hard on the workers and troopers as they have to keep everything in place for a new airfield, which will also make it dangerous for planes
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_embracing_jungle
    x = 38
    y = 0

    cost = 3
    mutually_exclusive = { focus = EEE_embracing_air }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_embracing_jungle"
        army_experience = 20
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_soaring_through_hills
    x = -4
    y = 1
    relative_position_id = EEE_embracing_jungle

    cost = 3
    prerequisite = {
        focus = EEE_embracing_air
        focus = EEE_embracing_jungle
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_soaring_through_hills"
        #Tech bonus here #This will change to rangers come 1.17
        ###Possibly some jungle and mountain training regimens, with the thestral troops finding the terrain difficult, even for them, since it's the heart of the jungle, and it's where they rarely venture, due to dangers and the thickness of the terrain; probably have one of the soldiers tell ghost stories or the like to purposefully freak them out before running across the actual thing
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_badlands_survival_training
    x = -2
    y = 1
    relative_position_id = EEE_soaring_through_hills

    cost = 3
    prerequisite = { focus = EEE_soaring_through_hills }
    mutually_exclusive = { focus = EEE_dedicated_college_courses }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_badlands_survival_training"
        #Idea here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_refined_prototypes
    x = 0
    y = 1
    relative_position_id = EEE_badlands_survival_training

    cost = 3
    prerequisite = { focus = EEE_badlands_survival_training }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_refined_prototypes"
        #Bonuses here
    }

    ai_will_do = {
        factor = 80
    }
}


shared_focus = {
    id = EEE_death_from_the_sea
    x = 1
    y = 1
    relative_position_id = EEE_refined_prototypes

    cost = 5
    prerequisite = {
        focus = EEE_proper_support_personnel
        focus = EEE_refined_prototypes
    }
    mutually_exclusive = {
        focus = EEE_death_from_above
        focus = EEE_death_from_within
    }
    available = {
        has_country_flag = EEE_unified_southeast
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_death_from_the_sea"
        ###Event of thestral commandos making an invasion on a captured port by some pirates or the like in the Spa Islands, as it is a small village, and couldn't fight off Blue Flame, who is making a last stand, and although they're shot at from both ships and regular firearms, they get through and overpower Blue Flame and his bands and capture him
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_jungle_quagmires
    x = -1
    y = 1
    relative_position_id = EEE_embracing_jungle

    cost = 3
    prerequisite = { focus = EEE_embracing_jungle }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_jungle_quagmires"
        #Effect here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_night_time_dominance
    x = -1
    y = 1
    relative_position_id = EEE_jungle_quagmires

    cost = 3
    prerequisite = { focus = EEE_jungle_quagmires }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_night_time_dominance"
        ###Event of a thestral or two worming their way through the jungle at night, as they sneak up on some of their fellow troopers, as part of their training for night-time campaigns, and they almost do before messing up, which requires them to start again, and also be the ones working the kitchen for the next month
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_lobbed_superiority
    x = 1
    y = 1
    relative_position_id = EEE_jungle_quagmires

    cost = 3
    prerequisite = { focus = EEE_jungle_quagmires }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_lobbed_superiority"
        #Bonus here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_centralize_militia_authority
    x = 42
    y = 0

    cost = 3

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_centralize_militia_authority"
        #Idea here and maybe first high command meeting where there is much bickering between the different tribal groups
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_ignored_environs
    x = -2
    y = 1
    relative_position_id = EEE_centralize_militia_authority

    cost = 3
    prerequisite = {
        focus = EEE_embracing_jungle
        focus = EEE_centralize_militia_authority
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_ignored_environs"
        ###Thestral troopers check out osme of the latest firearms developments, which promise that they will be unable to be affected by the jungle, which is mostly true, until one gets disassembled and some of the magic used effectively attracts all the gunk around it, which embarrasses the spokespony talking about the guns
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_bolstered_recruits
    x = 1
    y = 1
    relative_position_id = EEE_centralize_militia_authority

    cost = 3
    prerequisite = { focus = EEE_centralize_militia_authority }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_bolstered_recruits"
        #Bonus here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_rediscovering_synergy
    x = 1
    y = 1
    relative_position_id = EEE_ignored_environs

    cost = 3
    prerequisite = {
        focus = EEE_ignored_environs
        focus = EEE_bolstered_recruits
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_rediscovering_synergy"
        #Effect here
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_saturating_jungle
    x = -1
    y = 1
    relative_position_id = EEE_rediscovering_synergy

    cost = 3
    prerequisite = { focus = EEE_rediscovering_synergy }
    mutually_exclusive = { focus = EEE_invisible_guards }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_saturating_jungle"
        #Bonus here
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_invisible_guards
    x = 1
    y = 1
    relative_position_id = EEE_rediscovering_synergy

    cost = 3
    prerequisite = { focus = EEE_rediscovering_synergy }
    mutually_exclusive = { focus = EEE_saturating_jungle }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_invisible_guards"
        #Bonus here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_modern_mud_slingers
    x = 4
    y = 1
    relative_position_id = EEE_rediscovering_synergy

    cost = 3
    prerequisite = { focus = EEE_rediscovering_synergy }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_modern_mud_slingers"
        #Bonus here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_trench_scrapers
    x = -1
    y = 1
    relative_position_id = EEE_saturating_jungle

    cost = 3
    prerequisite = { focus = EEE_saturating_jungle }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_trench_scrapers"
        ###Short event of somepony being pushed through the engineering corps program because they need as many engineers as possible to help defend the country, even though she's admittedly not good at what they need to do
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_secondary_campuses
    x = 1
    y = 1
    relative_position_id = EEE_saturating_jungle

    cost = 5
    prerequisite = {
        focus = EEE_saturating_jungle
        focus = EEE_invisible_guards
    }
    available = {
        has_completed_focus = EEE_ayacachtli_air_college
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_secondary_campuses"
        ###Short event of some army students being snubbed by some of the air students, since they're seen as lesser soldiers since they aren't fighting with planes, which causes a small hoof-fight, where the army students use their training to trounce the air students
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_mobile_base_camps
    x = 1
    y = 1
    relative_position_id = EEE_invisible_guards

    cost = 3
    prerequisite = { focus = EEE_invisible_guards }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_mobile_base_camps"
        ###Follow a thestral as they begin their first day on the job in one of the logistic bases of the nation and them trying to understand all the of the complex and sometimes seemingly contradictory requirements for sending certain supplies, which leaves them only confused
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_logistical_shortcuts
    x = 0
    y = 1
    relative_position_id = EEE_modern_mud_slingers

    cost = 3
    prerequisite = { focus = EEE_modern_mud_slingers }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_logistical_shortcuts"
        ###Event of one of the quartermasters in an armory getting confronted by two soldiers, both promised the exact same model of weapon, but only one getting it, despite promising there is more than enough stock, and the quartermaster curses about how logistics failed her, since they were supposed to get more later in the week, and it didn't arrive, and she overpromises to get more things handled faster, and another pair come in afterward with even more demands, and she curses the decisions that she was forced to make
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_minor_solarist_implementations
    x = -1
    y = 1
    relative_position_id = EEE_trench_scrapers

    cost = 3
    prerequisite = { focus = EEE_trench_scrapers }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_minor_solarist_implementations"
        ###Event of some troopers in training effectively being in open revolt about what they are required to use, since they have been given flamethrowers, and everypony knows that those are weapons that Solarists deeply love, and they are hesistant to use weapons that burned so many of their families, and they are forced to go through, after a couple are reamed out for starting it all
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_nocturnal_strike_teams
    x = 1
    y = 1
    relative_position_id = EEE_trench_scrapers

    cost = 3
    prerequisite = { focus = EEE_trench_scrapers }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_nocturnal_strike_teams"
        #Effect here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_fortified_food_networks
    x = -1
    y = 1
    relative_position_id = EEE_mobile_base_camps

    cost = 3
    prerequisite = { focus = EEE_mobile_base_camps }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_fortified_food_networks"
        #Effect here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_augmented_night_vision
    x = 1
    y = 1
    relative_position_id = EEE_mobile_base_camps

    cost = 3
    prerequisite = { focus = EEE_mobile_base_camps }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_augmented_night_vision"
        ###Event of some thestrals in a Moonspeakers chamber, nervously waiting for the Moonspeakers to finish some of their magic, which uses a combination of unicorn and thestral magic, and when the spell is done, they are left in total darkness, which they can see well in, but as regular light is brought back, they're effectively blinded since it's enhanced their night vision to the point of being harmful in the day
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_synthesized_military_doctrines
    x = 0
    y = 2
    relative_position_id = EEE_trench_scrapers

    cost = 3
    prerequisite = { focus = EEE_trench_scrapers }
    mutually_exclusive = {
        focus = EEE_bits_and_pieces
        focus = EEE_native_doctrines
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_synthesized_military_doctrines"
        #Idea here
        ###Possibly short event of thestrals practicing a human wave charge with some different flame devices, and it going wrong as one explodes after being punctured by accident, and killing most of the squad
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_bits_and_pieces
    x = 0
    y = 2
    relative_position_id = EEE_secondary_campuses

    cost = 3
    prerequisite = { focus = EEE_secondary_campuses }
    mutually_exclusive = {
        focus = EEE_synthesized_military_doctrines
        focus = EEE_native_doctrines
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_bits_and_pieces"
        #Idea here
        ###Perhaps small event of thestral soldiers using some of the different armies' doctrines to which they've been exposed to take down another stronghold of one of the old nations, as they remain completely unpredictable in what they will do
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_native_doctrines
    x = 0
    y = 2
    relative_position_id = EEE_mobile_base_camps

    cost = 3
    prerequisite = { focus = EEE_mobile_base_camps }
    mutually_exclusive = {
        focus = EEE_synthesized_military_doctrines
        focus = EEE_bits_and_pieces
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_native_doctrines"
        #Idea here
        ###Possibly short event of some commandos going through during the deepest parts of the night to infiltrate and then take down a Solarist scouting camp that survived everything with Scorched Earth and is seeking to do what it can with what they have, and they get completely ambushed given how dark it is, and the thestrals beat them easily
    }

    ai_will_do = {
        factor = 80
    }
}

###Navy###
shared_focus = {
    id = EEE_what_are_ships
    x = 54
    y = 0

    cost = 3

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_what_are_ships"
        navy_experience = 50
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_restealing_booty
    x = -4
    y = 1
    relative_position_id = EEE_what_are_ships

    cost = 3
    prerequisite = { focus = EEE_what_are_ships }
    available = {
        OR = {
            controls_state = 24
            controls_state = 16
        }
        has_country_flag = EEE_angy_natives
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_restealing_booty"
        ###Event of some thestral commandos doing what they can to capture some of the unused ships that were left in drydock in one of the ports in former EEE territory, as they were taken by the Neighua when they rebelled
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_doubling_production
    x = 0
    y = 1
    relative_position_id = EEE_what_are_ships

    cost = 3
    prerequisite = { focus = EEE_what_are_ships }
    available = {
        16 = { is_owned_and_controlled_by = ROOT }
        has_country_flag = EEE_angy_natives
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_doubling_production"
        16 = {
            add_extra_state_shared_building_slots = 2
            add_building_construction = {
                type = dockyard
                level = 2
                instant_build = yes
            }
        }
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_artery_to_sea
    x = 4
    y = 1
    relative_position_id = EEE_what_are_ships

    cost = 3
    prerequisite = { focus = EEE_what_are_ships }
    available = {
        24 = { is_owned_and_controlled_by = ROOT }
        has_country_flag = EEE_angy_natives
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_artery_to_sea"
        24 = {
            add_extra_state_shared_building_slots = 1
            add_building_construction = {
                type = dockyard
                level = 1
                instant_build = yes
            }
            add_building_construction = {
                type = naval_base
                level = 2
                instant_build = yes
                province = 11264
            }
            add_building_construction = {
                type = coastal_bunker
                level = 3
                instant_build = yes
                province = 11264
            }
            add_building_construction = {
                type = bunker
                level = 1
                instant_build = yes
                province = 11264
            }
        }
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_learning_from_cheapest
    x = -2
    y = 1
    relative_position_id = EEE_restealing_booty

    cost = 3
    prerequisite = { focus = EEE_restealing_booty }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_learning_from_cheapest"
        #Idea here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_embracing_sea
    x = -2
    y = 1
    relative_position_id = EEE_doubling_production

    cost = 4
    prerequisite = { focus = EEE_doubling_production }
    mutually_exclusive = { focus = EEE_emulate_greats }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_embracing_sea"
        #Bonus here
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_emulate_greats
    x = 2
    y = 1
    relative_position_id = EEE_doubling_production

    cost = 4
    prerequisite = { focus = EEE_doubling_production }
    mutually_exclusive = { focus = EEE_embracing_sea }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_emulate_greats"
        #Bonus here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_required_guidance
    x = 2
    y = 1
    relative_position_id = EEE_artery_to_sea

    cost = 3
    prerequisite = { focus = EEE_artery_to_sea }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_required_guidance"
        ###Event of Squeaky Wheel effectively begging some thestrals or non-thestrals she knows to help with leading the fleet, as she wants to see them be a force to be reckoned with, but is entirely incapable on her own, which eventually gets a few to relent, including one retired admiral who served under Luna's tenure as princess with Celestia
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_diving_deeper
    x = -1
    y = 1
    relative_position_id = EEE_embracing_sea

    cost = 3
    prerequisite = { focus = EEE_embracing_sea }
    mutually_exclusive = { focus = EEE_hidden_battleships }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_diving_deeper"
        ###Event of thestrals testing a new sub design, trying to go as deep as possible in the Mareanas Trench, and getting close to imploding before finally going up, perhaps encountering a siren or two who aren't the Dazzlings, as a nod to the lore change; maybe kelpies instead
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_hidden_battleships
    x = 1
    y = 1
    relative_position_id = EEE_embracing_sea

    cost = 3
    prerequisite = { focus = EEE_embracing_sea }
    mutually_exclusive = { focus = EEE_diving_deeper }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_hidden_battleships"
        ###Event of some field tests with subs that are designed to go toe-to-toe with screen ships, anyway, and how the testers go a bit overboard with the live-fire torpedo attacks, which end up causing untold damage along the shore, but at least the explosions looked cool
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_middling_option
    x = -1
    y = 1
    relative_position_id = EEE_emulate_greats

    cost = 3
    prerequisite = { focus = EEE_emulate_greats }
    mutually_exclusive = { focus = EEE_modern_option }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_middling_option"
        #Tech bonus for BCs here, or heavy ships if MTG
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_modern_option
    x = 1
    y = 1
    relative_position_id = EEE_emulate_greats

    cost = 3
    prerequisite = { focus = EEE_emulate_greats }
    mutually_exclusive = { focus = EEE_middling_option }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_modern_option"
        #Tech bonus here
    }

    ai_will_do = {
        factor = 160
    }
}

shared_focus = {
    id = EEE_slinking_through_water
    x = 1
    y = 1
    relative_position_id = EEE_diving_deeper

    cost = 3
    prerequisite = {
        focus = EEE_diving_deeper
        focus = EEE_hidden_battleships
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_slinking_through_water"
        #Bonus and idea here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_high_seeeeeees
    x = 0
    y = 3
    relative_position_id = EEE_doubling_production

    cost = 3
    prerequisite = { focus = EEE_doubling_production }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_high_seeeeeees"
        #Tech bonus here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_crashing_through_waves
    x = 1
    y = 1
    relative_position_id = EEE_middling_option

    cost = 3
    prerequisite = {
        focus = EEE_middling_option
        focus = EEE_modern_option
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_crashing_through_waves"
        #Bonus and idea here
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_shadow_strikes
    x = 1
    y = 3
    relative_position_id = EEE_learning_from_cheapest

    cost = 3
    prerequisite = { focus = EEE_learning_from_cheapest }
    prerequisite = { focus = EEE_diving_deeper }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_shadow_strikes"
        ###Short event of some sub captains trying new techniques put in place by the admiralty, which are risky by their very nature, and it ends up leading to a close call with one of the other subs, but the simulated targets were destroyed without ever detecting the thestrals
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_insider_knowledge
    x = 0
    y = 2
    relative_position_id = EEE_diving_deeper

    cost = 3
    prerequisite = { focus = EEE_diving_deeper }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_insider_knowledge"
        #Bonuses here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_solar_applications
    x = 0
    y = 2
    relative_position_id = EEE_hidden_battleships

    cost = 3
    prerequisite = { focus = EEE_hidden_battleships }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_solar_applications"
        #Bonuses here
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_heavy_destroyers
    x = 0
    y = 2
    relative_position_id = EEE_middling_option

    cost = 3
    prerequisite = { focus = EEE_middling_option }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_heavy_destroyers"
        ###Event of a pirate preparing to go in on a couple of civilian ships, then coming into a battle with a single battlecruiser, which is markedly nimble for its size, which eventually manages to out-class them, and sink them to the bottom of the ocean as they're being used as convoy protection and as screen ships
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_carrying_waves
    x = 0
    y = 2
    relative_position_id = EEE_modern_option

    cost = 3
    prerequisite = { focus = EEE_modern_option }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_carrying_waves"
        ###Event of a new carrier design being tested, with both expanded landing strips and also a prototypical storage system, and the admiral on deck watches as the demonstration of it finishes up, and it nearly goes awry when there's a mechanical issue, although it does show the promise the system has, and it's greenlit anyway
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_tribes_to_themselves
    x = -1
    y = 3
    relative_position_id = EEE_required_guidance

    cost = 3
    prerequisite = { focus = EEE_modern_option }
    prerequisite = { focus = EEE_required_guidance }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_tribes_to_themselves"
        ###Event of a carrier responding all on itself to an incursion by some pirates or perhaps some remnant naval forces from COBSOC or one of the COBSOC splinters, and they end up cleaing house despite also taking some hits, thanks to new armor plating and techniques, which earns the gratitude of the civilians they rescued
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_claws_of_dark
    x = 1
    y = 1
    relative_position_id = EEE_insider_knowledge

    cost = 4
    prerequisite = {
        focus = EEE_insider_knowledge
        focus = EEE_solar_applications
    }
    mutually_exclusive = {
        focus = EEE_natural_weapons
        focus = EEE_fangs_of_night
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_claws_of_dark"
        #Idea here
        ###Perhaps event of some subs responding to a naval exercise by another nation, and using it as a chance to warn other countries about the thestrals being a serious force to contend with, and sending off a few stray torpedoes and appearing visible briefly to inspire fear before running away with their superior speed
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_natural_weapons
    x = 0
    y = 2
    relative_position_id = EEE_high_seeeeeees

    cost = 4
    prerequisite = {
        focus = EEE_insider_knowledge
        focus = EEE_solar_applications
        focus = EEE_heavy_destroyers
        focus = EEE_carrying_waves
    }
    prerequisite = { focus = EEE_high_seeeeeees }
    mutually_exclusive = {
        focus = EEE_claws_of_dark
        focus = EEE_fangs_of_night
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_natural_weapons"
        #Idea here
        ###Potentially event of some marines in training landing on a shoreline and getting into melee combat, using their fangs as weapons as taught, and some of them being a bit hesitant since that is effectively acting no more than like an animal, which they would rather avoid, but get forced to do it anyway, lest they are drummed out of service
    }

    ai_will_do = {
        factor = 80
    }
}

shared_focus = {
    id = EEE_fangs_of_night
    x = 1
    y = 1
    relative_position_id = EEE_heavy_destroyers

    cost = 4
    prerequisite = {
        focus = EEE_heavy_destroyers
        focus = EEE_carrying_waves
    }
    mutually_exclusive = {
        focus = EEE_claws_of_dark
        focus = EEE_natural_weapons
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_fangs_of_night"
        #Idea here
        ###Possibly event of a training exercise between two fleets being interrupted by a small pirate fleet, as it was thought that they'd be too weak fighting each other to prevent them all from being captured, and the ships end up getting into a cooridnated fist fight, which the thestrals ships manage to win, with more prizes afterward
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_volatile_mixtures
    x = 0
    y = 2
    relative_position_id = EEE_solar_applications

    cost = 3
    prerequisite = { focus = EEE_solar_applications }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_volatile_mixtures"
        ###Event of a sub using some of the new torpedoes with napalm and other mixtures of magic and chemicals preparing to do a final test of its weapons, as requested by the admiralty, and one of the torpedoes exploding before being loaded because a pony sneezed on it, and quickly leading to a chain reaction that destroys most of the sub, although it's still deemed mostly successful
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_bristling_barnacles
    x = 0
    y = 2
    relative_position_id = EEE_heavy_destroyers

    cost = 3
    prerequisite = { focus = EEE_heavy_destroyers }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_bristling_barnacles"
        ###A captain of a new battlecruiser, which has had its armament against other heavy ships markedly weakened, is ordered to try destroying one of the old destroyers or other ships that are no longer used due to age, and he is eager to get underway, to the point he orders the attack on the wrong ship, which is utterly destroyed, adn tries to pass the blame elsewhere, although the test shows that they are deadly versus light sihps
    }

    ai_will_do = {
        factor = 40
    }
}

shared_focus = {
    id = EEE_a_new_glory
    x = 0
    y = 2
    relative_position_id = EEE_natural_weapons

    cost = 5
    prerequisite = {
        focus = EEE_claws_of_dark
        focus = EEE_natural_weapons
        focus = EEE_fangs_of_night
    }

    completion_reward = {
        log = "[GetDateText]: [ROOT.GetName]: Focus EEE_a_new_glory"
        #Idea here
    }

    ai_will_do = {
        factor = 160
    }
}
