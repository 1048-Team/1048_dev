###Daybreaker's tree###
shared_focus = {
	id = flames_never_snuffed
	x = 38
	y = 15
	
	available = {
		hidden_trigger = {
			has_country_flag = daybreaker_reigns
		}
	}
	
	bypass = {
		}
		
	allow_branch = {
		hidden_trigger = {
			has_country_flag = daybreaker_reigns
		}
	}
	
	cost = 1
	
	prerequisite = {
		}
		
	cancel_if_invalid = yes
	available_if_capitulated = no
	
	completion_reward = {
		add_stability = 0.03
		clr_country_flag = daybreaker_crisis
		###Event for Daybreaker returning to health###
		###Remove bad spirits###
		}
	ai_will_do = {
		factor = 100
	}
}